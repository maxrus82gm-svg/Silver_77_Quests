# 03_ULTRA_TASK

Этот документ является постоянной оболочкой текущей задачи Ultra.

Ultra выполняет только содержимое блока:

`БЛОК U1 — ТЕКУЩАЯ ЗАДАЧА ULTRA`

Постоянные правила и контекст роли находятся отдельно:

- [[00_Главная]]
- [[01_Старт_Ultra]]
- [[02_Правила_Ultra]]

Содержимое этих постоянных документов не нужно копировать внутрь каждой задачи.

При постановке следующей задачи заменяется только содержимое между заголовком
`БЛОК U1 — ТЕКУЩАЯ ЗАДАЧА ULTRA`
и маркером
`КОНЕЦ БЛОКА U1`.

Сам заголовок, границы блока и остальная оболочка файла не удаляются и не дублируются.

================================================================================

# 🟪 БЛОК U1 — ТЕКУЩАЯ ЗАДАЧА ULTRA

ULTRA TASK 001 — Проверка согласованности lifecycle и scope после TASK 181

РЕЖИМ:
ANALYSIS ONLY.

ЦЕЛЬ:

Независимо проверить, нет ли противоречия между правилами scope/allowlist и обязательным lifecycle после успешной задачи.

Нужно ответить только на один основной вопрос:

Может ли после успешной IMPLEMENTATION или DOCUMENTATION задачи стандартный lifecycle законно изменить служебные документы цикла, если они не перечислены в allowlist конкретной TASK?

КАНОНИЧЕСКИЙ ПРОЕКТ:

`P:\Silver_77_Quests`

ЧИТАТЬ В ТАКОМ ПОРЯДКЕ:

1. `P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\01_Старт_Ultra.md`
2. `P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\02_Правила_Ultra.md`
3. этот БЛОК U1
4. `P:\Silver_77_Quests\Documentation_DayZ_Qwest\12_Старт_агента.md`
5. `P:\Silver_77_Quests\Documentation_DayZ_Qwest\20_SplitDoc\TASK_LIFECYCLE_RULES.md`

Другие документы проекта автоматически НЕ читать.

Если для ответа действительно окажется нужен ещё один документ, сначала определить точную причину, почему без него нельзя проверить поставленный вопрос, и указать это в отчёте. Не расширять чтение «на всякий случай».

ЧТО ИМЕННО ПРОВЕРИТЬ:

1. Найти в `12_Старт_агента.md` правило о том, какие файлы и разделы агент имеет право менять по scope / allowlist.

2. Найти в `12_Старт_агента.md` правило о выполнении `TASK_LIFECYCLE_RULES.md` перед успешным завершением задачи.

3. Найти в `TASK_LIFECYCLE_RULES.md` правила, которые требуют после успешной задачи:
   - записать новый AGENT REPORT в БЛОК 2 документа 11;
   - добавить одну запись в `TASK_HISTORY.md`;
   - очистить завершённый БЛОК 1.

4. Отдельно найти условие о том, что разрешение задачи должно включать lifecycle-обновления.

5. Сопоставить эти правила друг с другом и определить:
   - образуют ли они однозначный контракт;
   - есть ли противоречие;
   - есть ли неоднозначность, при которой агент может выбрать два разных допустимых поведения.

КОНТРОЛЬНЫЙ ПРИМЕР:

Представить обычную implementation-задачу, в которой написано:

`Разрешено менять: migration.js`

При этом `11_Задача_агенту.md` и `TASK_HISTORY.md` отдельно в allowlist не перечислены.

Ответить:

- имеет ли агент право после успешной работы изменить БЛОК 2;
- имеет ли право добавить запись TASK_HISTORY;
- имеет ли право очистить БЛОК 1;
- на какие точные правила опирается каждый ответ.

ВАЖНО:

Не считать предположение фактом.

Если текст допускает два толкования, так и написать:
`контракт неоднозначен`.

Не выбирать удобное толкование только потому, что оно кажется логичным.

ОГРАНИЧЕНИЯ:

- ANALYSIS ONLY.
- Никаких изменений основного проекта.
- Не изменять `12_Старт_агента.md`.
- Не изменять `TASK_LIFECYCLE_RULES.md`.
- Не изменять `11_Задача_агенту.md`.
- Не изменять `TASK_HISTORY.md`.
- Не менять этот БЛОК U1.
- Git не использовать и не изменять.
- Не запускать build, runtime, editor или сервер.
- Не анализировать всю TASK 181.
- Не переделывать документационную архитектуру.
- Не искать соседние проблемы.
- Не предлагать крупный рефакторинг.

РАЗРЕШЁННАЯ ЗАПИСЬ:

Разрешено записать только итоговый анализ в:

`P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\04_ULTRA_REPORT.md`

ФОРМАТ ОТЧЁТА:

1. `VERDICT`
   - `ПРОТИВОРЕЧИЯ НЕТ`
   - `ЕСТЬ НЕОДНОЗНАЧНОСТЬ`
   - `ЕСТЬ ПРОТИВОРЕЧИЕ`

2. `ПРОВЕРЕННЫЕ ПРАВИЛА`
   Для каждого важного вывода:
   - документ;
   - раздел или точная фраза;
   - что именно это правило разрешает или запрещает.

3. `КОНТРОЛЬНЫЙ ПРИМЕР`
   Разобрать случай, где allowlist содержит только `migration.js`.

4. `МИНИМАЛЬНОЕ ИСПРАВЛЕНИЕ`
   Если проблема существует:
   - максимум 1–3 коротких правила;
   - без переписывания архитектуры;
   - отдельно указать, где именно правило должно находиться.

5. `РИСК ИСПРАВЛЕНИЯ`
   Проверить, не позволит ли предложенное правило lifecycle случайно расширять функциональный scope обычной задачи.

6. `НЕОПРЕДЕЛЁННОСТИ`
   Только реальные ограничения анализа.

После записи `04_ULTRA_REPORT.md` остановиться.

Ничего больше не выполнять.

================================================================================

# КОНЕЦ БЛОКА U1