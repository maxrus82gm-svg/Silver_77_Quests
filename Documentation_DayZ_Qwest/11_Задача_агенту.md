# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `DayZ .c / Enforce Script / runtime compile` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_ENFORCE_SCRIPT_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`

## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки под заголовком `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не должно оставаться содержимого завершённой задачи.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 149 — Runtime-анализ Migration.log после успешного запуска сервера`.

STATUS:
- `RUNTIME REVIEWED`.
- Анализ выполнен read-only; код, JSON, runtime profile, PBO и сервер не изменялись.
- Git write-команды не выполнялись.

LOG SOURCE:
- Логический путь: `$profile:Silver_77_Migrate/Migration.log`.
- Фактический путь: `\\192.168.0.77\Server\Dayz\Shitler_00\profiles\Silver_77_Migrate\Migration.log`.
- Сессия: `DayZServer_x64_2026-09-01_21-54-37.RPT` / `script_2026-09-01_21-54-43.log`.
- Открытый файл через SMB metadata временно показывал `Length=0`; содержимое безопасно прочитано через `FileShare.ReadWrite`.
- Срез анализа: `timeMs=27535–2431884`; последний полный state-batch — `2425884/2425885`. Лог продолжает дописываться.

SESSION / STARTUP:
- `SESSION_START`: `loggingEnabled=1`, `stuckDebugLoggingEnabled=1`.
- AI stimulus готов: `CfgVehicles SurvivorBase NoiseShout`.
- В `timeMs=94751–94766` созданы `MIGRATION_TEST_001_RUN_1` (`10/10`, `routePoints=2`) и `MIGRATION_TEST_002_RUN_2` (`10/10`, `routePoints=4`).
- Итог: `All enabled scenarios started; scenarios=2 totalSpawned=20`.
- `Formula too complex` больше не блокирует runtime compile/startup.

WEATHER DECISION:
- `globalEnabled=1 requestingScenarios=0 enabledScenarios=2 result=SKIP_NO_SCENARIO_REQUEST`.
- Погода пропущена; группы стартовали после `30 sec`.
- Source JSON содержит `weatherChangeEnabled=1`, серверный runtime JSON этих полей ещё не содержит. В runtime отсутствующее поле прочиталось как `0`, поэтому default `1` не сохранил старое поведение.
- Это отдельное runtime/config-migration несоответствие; TASK 149 его не исправляла.

STUCK SAMPLE RESULTS:
- `STUCK_SAMPLE_START=2441`, `STUCK_SAMPLE_CHECK=2034`, `STUCK_SAMPLE_RESET=526`.
- Scenario 001: `1076 MOVING`, `71 STUCK`; MOVING `movedXZ min/avg/max=2.139/3.301/6.382 m`, окно в среднем `6.004 sec`.
- Scenario 002: `839 MOVING`, `48 STUCK`; MOVING `movedXZ min/avg/max=2.243/3.432/6.349 m`, окно в среднем `6.003 sec`.
- Все `1915` MOVING-checks имеют `movedXZ >= 2.0`; detector реально доходит от START до CHECK примерно через 6 секунд.
- Reset reasons: `WAYPOINT_CHANGED=235`, `STUCK_RECOVERY=123`, `AGGRO=108`, `ROUTE_POINT_CHANGED=60`. Постоянного reset без CHECK нет.

Representative samples (`threshold=2`):

| scenario/group/id | positionA → positionB | movedXZ | result |
|---|---|---:|---|
| `001/RUN_1/INF_1` | `<13198.347656, 81.377190, 13368.301758>` → `<13197.714844, 81.552650, 13365.503906>` | `2.86852` | `MOVING` |
| `001/RUN_1/INF_3` | `<13152.255859, 104.804871, 13179.473633>` → `<13151.578125, 104.844017, 13178.898438>` | `0.888917` | `STUCK` |
| `001/RUN_1/INF_5` | `<13187.541992, 83.423294, 13338.250000>` → `<13187.147461, 83.579132, 13335.358398>` | `2.91839` | `MOVING` |
| `002/RUN_2/INF_1` | `<12847.848633, 133.146896, 13063.277344>` → та же позиция | `0` | `STUCK` |
| `002/RUN_2/INF_5` | `<12839.449219, 127.615219, 13091.425781>` → `<12841.712891, 127.771088, 13090.978516>` | `2.30743` | `MOVING` |
| `002/RUN_2/INF_10` | `<12843.377930, 127.171913, 13093.988281>` → `<12846.260742, 127.399544, 13093.343750>` | `2.95399` | `MOVING` |

STUCK DETECTED COUNT:
- `STUCK_DETECTED=119`: `71` в первой группе, `48` во второй.
- Максимум — по `10` incidents у `001/INF_3` и `002/INF_1`; бесконечного recovery одного id нет.

STIMULUS RESULTS:
- `119 STUCK_DETECTED + 4 ROUTE_PROGRESS_LOST = 123` recovery incidents.
- `RELEASE=123`, `ATTEMPT=123`, `OK=123`, `FAILED=0`, `SKIPPED=0`, `RESUME=123`.
- `WAIT_BUSY=81`, `CALM=129`; шесть дополнительных CALM объясняются новым vanilla-busy внутри cooldown.
- Каждый incident получил собственный directed stimulus; все bool results успешны, все incidents завершились RESUME.

ROUTE PROGRESS WATCHDOG:
- `ROUTE_PROGRESS_LOST=4`, по два на группу:
  - `002/INF_3 @609768`: `previous=49.6116`, `current=59.3707`, `badChecks=2`;
  - `002/INF_4 @658768`: `previous=50.7367`, `current=59.711`, `badChecks=2`;
  - `001/INF_7 @660518`: progress `1.38441 m`, `badChecks=2`;
  - `001/INF_2 @722768`: progress `1.74211 m`, `badChecks=2`.
- Все прошли `RELEASE → ATTEMPT → OK → CALM → RESUME`; постоянного loop нет.

FINAL HOLD BEHAVIOR:
- `HOLD_ENTER=20`: до hold дошли все 20 infected.
- `HOLD_OUTSIDE=0`, `HOLD_RETURN_WAIT_BUSY=0`, `HOLD_RETURN_START=0`, `HOLD_RETURNED=0`.
- `RETURN_TO_HOLD` не активировался.
- Визуальное отходят/возвращаются соответствует свободному vanilla-перемещению в `HOLD_FREE` внутри радиуса, но НЕ подтверждает manager-chain `HOLD_OUTSIDE → HOLD_RETURN_START → HOLD_RETURNED`.

FINAL HOLD DISTANCE TRACKING:
- `distance=` проверена как дискретная X/Z-distance от `state.m_FinalTargetCenter`; `finalHoldRadius=200 m`.
- Для каждого id разобрана последовательность `position/distance/mode` после `HOLD_ENTER`.
- На последнем полном batch живы `14`: группа 001 — `6`, группа 002 — `8`.
- У всех 20 tracked members максимум после arrival остался `<=200 m`; случаев `distance > finalHoldRadius` нет.
- Группа 001: living `6`, missing `4`, never over 200 `10/10`, max `156.453 m` (`INF_5`, позже missing).
- Группа 002: living `8`, missing `2`, never over 200 `10/10`, max `133.687 m` (`INF_10`).
- Ожидаемый возврат нигде не отсутствует: условие `>200 m` не возникало.

Living at cutoff; последние modes `HOLD_FREE`, `target=NO`, `mind=CALM`:

| group | id | max logged | last logged |
|---|---|---:|---:|
| `001/RUN_1` | `INF_2` | `79.469` | `71.899` |
| `001/RUN_1` | `INF_3` | `88.144` | `57.088` |
| `001/RUN_1` | `INF_6` | `63.235` | `45.295` |
| `001/RUN_1` | `INF_8` | `96.096` | `82.329` |
| `001/RUN_1` | `INF_9` | `65.457` | `35.942` |
| `001/RUN_1` | `INF_10` | `122.224` | `122.224` |
| `002/RUN_2` | `INF_1` | `74.900` | `63.729` |
| `002/RUN_2` | `INF_2` | `127.184` | `127.184` |
| `002/RUN_2` | `INF_3` | `83.863` | `73.348` |
| `002/RUN_2` | `INF_4` | `75.957` | `62.079` |
| `002/RUN_2` | `INF_5` | `104.191` | `51.669` |
| `002/RUN_2` | `INF_8` | `100.215` | `88.777` |
| `002/RUN_2` | `INF_9` | `103.030` | `94.867` |
| `002/RUN_2` | `INF_10` | `133.687` | `113.382` |

Missing after HOLD_ENTER:

| group/id | last state | max logged | last mode/target/mind |
|---|---:|---:|---|
| `001/INF_4` | `1905883` | `61.245` | `HOLD_FREE/NO/CALM` |
| `001/INF_1` | `1955883` | `140.734` | `HOLD_FREE/NO/CALM` |
| `001/INF_7` | `2015883` | `80.910` | `HOLD_FREE/NO/CALM` |
| `001/INF_5` | `2225883` | `156.453` | `HOLD_FREE/NO/CALM` |
| `002/INF_6` | `2305884` | `105.991` | `HOLD_FREE/NO/CALM` |
| `002/INF_7` | `2385884` | `119.616` | `HOLD_FREE/NO/CALM` |

Five farthest samples:

| group/id | maxDistance | mode | returnResult |
|---|---:|---|---|
| `001/INF_5` | `156.453` | `HOLD_FREE` | `NO RETURN REQUIRED (<200); later missing` |
| `001/INF_1` | `140.734` | `HOLD_FREE` | `NO RETURN REQUIRED (<200); later missing` |
| `002/INF_10` | `133.687` | `HOLD_FREE` | `NO RETURN REQUIRED (<200)` |
| `002/INF_2` | `127.184` | `HOLD_FREE` | `NO RETURN REQUIRED (<200)` |
| `001/INF_10` | `122.224` | `HOLD_FREE` | `NO RETURN REQUIRED (<200)` |

- Это дискретные log maxima, не математический максимум между samples.
- Подтверждён случай 1: свободное движение внутри `<=200 m`.
- Случаи 2–4 (`>200` с возвратом, удалением либо vanilla busy) не наблюдались; return-ветка пока не проверена.

VANILLA AGGRO:
- `VANILLA_AGGRO_HANDOFF=114`, `VANILLA_AGGRO_RESUME=114` (`73/73` в 001, `41/41` в 002).
- Все handoff завершились возвратом к migration lifecycle.

ERRORS / WARNINGS:
- `PATH_ERROR=0`, `WARNING=0`, stimulus `FAILED=0`, stimulus `SKIPPED=0`.
- `BUILD_PATH=317`: `20 initial + 60 route-point transitions + 123 recovery resumes + 114 aggro resumes`. Необъяснимого rebuild loop нет.
- Новая проблема: после исчезновения entity `LogUnitStates()` повторяет generic `ERROR: unit state or infected entity is missing` без scenario/group/id.
- Первая ошибка: `timeMs=1909883`; к cutoff `2431884` накопилось `1765` строк. После шести missing entity пишется примерно шесть ошибок за секундный вызов.
- Защита не обращается к InputController отсутствующей entity, поэтому crash не подтверждён. Проблема — stale states, отсутствие однократного идентифицированного death/missing marker и быстрое засорение лога.
- Причину исчезновения (смерть, cleanup или удаление) generic marker не доказывает; `DEATH` marker для null reference не появляется.

REPRESENTATIVE INFECTED TRACES:
- `001/INF_1`: `5` recoveries, `7` aggro pairs, `HOLD_ENTER=1137565`; после `1955883` missing.
- `001/INF_3`: `10` recoveries, `14` aggro pairs, `HOLD_ENTER=1483635`; жив, `HOLD_FREE`.
- `001/INF_5`: `5` recoveries, `2` aggro pairs, `HOLD_ENTER=795518`; max hold `156.453`, затем missing.
- `002/INF_1`: `10` recoveries, `6` aggro pairs, `HOLD_ENTER=1304098`; жив, `HOLD_FREE`.
- `002/INF_7`: `2` recoveries, `0` aggro pairs, `HOLD_ENTER=501017`; max `119.616`, затем missing.
- `002/INF_10`: `1` recovery, `5` aggro pairs, `HOLD_ENTER=719018`; жив, `HOLD_FREE`.

Detailed chains:
- `001/INF_3 @513768`: CHECK `0.888917 STUCK` → DETECTED → RELEASE → ATTEMPT → OK → CALM `543768` → RESUME `549768`; далее `2.28858/3.01038 MOVING`.
- `002/INF_1 @101036`: CHECK `0 STUCK` → RELEASE → ATTEMPT → OK → три WAIT_BUSY → CALM `140044` → RESUME `146045`.
- `001/INF_2 @722768`: ROUTE_PROGRESS_LOST → RELEASE/ATTEMPT/OK → CALM `752768` → RESUME `758768`; далее `2.7599 MOVING`.
- `002/INF_3 @609768`: ROUTE_PROGRESS_LOST → RELEASE/ATTEMPT/OK → CALM `639768` → RESUME `645768`; далее `3.04346/3.25417 MOVING`.

CONCLUSION:
- `D) Есть другая конкретная runtime-проблема`.
- Detector/stimulus/recovery работают: `123/123 ATTEMPT=OK`, `123/123 RESUME`; B и C не подтверждены.
- Все 20 вошли в HOLD, ни один tracked sample не превысил 200 м. Визуальное возвращение — vanilla `HOLD_FREE`, не `HOLD_RETURN_*`.
- Проблемы:
  1. missing entity остаются stale states и создают непрерывный generic ERROR spam;
  2. runtime JSON без `weatherChangeEnabled` дал `requestingScenarios=0` и пропустил погоду.

NEXT ACTION:
1. Отдельной микрозадачей сделать однократную идентифицированную обработку missing/dead state и убрать spam, сохранив безопасную controller-защиту.
2. Отдельно решить backward-compatible migration `weatherChangeEnabled` либо вручную добавить `1` в оба runtime scenarios; TASK 149 этого не делала.
3. Для проверки return logic провести контролируемый тест с реальным пересечением 200 м и дождаться `HOLD_OUTSIDE → HOLD_RETURN_START → HOLD_RETURNED`.
4. Сохранить текущий `Migration.log` как evidence до нового запуска.

BLOCK 1 CLEANUP CHECK: PASS
- БЛОК 1 уже был в каноническом состоянии: один заголовок и три стандартные строки.
- Разделитель и БЛОК 2 сохранены.

TASK149_REPLACE_SENTINEL
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
