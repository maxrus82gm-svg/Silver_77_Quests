# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`.

Правила:

- короткое обязательное ядро: `Documentation_DayZ_Qwest/12_Старт_агента.md`;
- подготовка TASK: `Documentation_DayZ_Qwest/20_SplitDoc/TASK_PREPARATION_RULES.md`;
- маршрутизация контекста: `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`;
- завершение TASK: `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`.

## Стартовое чтение исполнителя

Агент начинает с:

- только текущего `БЛОКА 1` этого документа;
- короткого `Documentation_DayZ_Qwest/12_Старт_агента.md`;
- документов, прямо перечисленных в текущем БЛОКЕ 1;
- явно обозначенных обязательных зависимостей выбранного маршрута.

`БЛОК 2` и `БЛОК 3` автоматически не читать. Они открываются только для связанного продолжения, review или по прямому указанию.

Тематическая матрица находится только в `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`. Обычная ссылка не означает рекурсивного чтения всего связанного графа.

## Завершение

Перед успешным завершением implementation/documentation-задачи использовать `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`.

При `BLOCKED`, неполном результате или нерешённой критичной ошибке БЛОК 1 не очищать.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

TASK 182 — FIX workflow: единый БЛОК 1 для Codex / Ultra и однозначный lifecycle

================================================================================
DISPATCH HEADER — СНАЧАЛА ЧИТАЕТ CODEX

EXECUTOR: ULTRA
CODEX MODE: PAYLOAD BUILD + DISPATCH ONLY
ULTRA MODE: DOCUMENTATION IMPLEMENTATION

USER TASK SOURCE:
P:\Silver_77_Quests\Documentation_DayZ_Qwest\11_Задача_агенту.md
БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

ULTRA REPORT TARGET:
P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\04_ULTRA_REPORT.md

ULTRA LAUNCH PAYLOAD:
P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\03_ULTRA_TASK.md

AUTO-DELEGATION: OFF

Если этот header читает Codex:
после строки END DISPATCH HEADER прочитать body и REQUIRED-документы только в объёме, необходимом для сборки полного launch-payload.
Содержательную задачу вместо Ultra не выполнять.
Полностью пересобрать `03_ULTRA_TASK.md`, проверить его непустоту, вызвать `gigachat_file_task` с парой `03_ULTRA_TASK.md` + `04_ULTRA_REPORT.md` и после прямого запуска не читать ULTRA REPORT.

END DISPATCH HEADER
================================================================================

ИСПОЛНИТЕЛЬ ДАЛЬНЕЙШЕЙ ЧАСТИ:
ULTRA

ВАЖНО О СЛОЯХ ЗАДАЧИ:

- БЛОК 1 этого документа — единственный пользовательский источник текущей задачи.
- `Ultra/03_ULTRA_TASK.md` — одноразовый технический launch-payload конкретного вызова, полностью сформированный Codex из БЛОКА 1 и обязательных правил.
- `Ultra/04_ULTRA_REPORT.md` — результат конкретного вызова Ultra.

Пользователь не ведёт `03` вручную. Между запусками файл может быть пустым; перед каждым вызовом он пересобирается полностью и не наследует старое содержимое.

================================================================================

ТИП:
DOCUMENTATION WORKFLOW FIX

BASELINE:

Repository:
maxrus82gm-svg/Silver_77_Quests

Branch:
main

Commit message:
94

Full SHA:
f07a773118f23bf67a34947fb68579c892a14991

Канонический рабочий проект:
P:\Silver_77_Quests

Каноническая документация:
P:\Silver_77_Quests\Documentation_DayZ_Qwest

================================================================================

ЦЕЛЬ:

Сделать один однозначный агентский workflow для трёх режимов исполнения:

- CODEX;
- ULTRA.
- CODEX + ULTRA.

Текущая TASK всегда должна жить только в одном месте:

`Documentation_DayZ_Qwest/11_Задача_агенту.md`
→ `БЛОК 1`.

`Ultra/03_ULTRA_TASK.md` не является вторым пользовательским источником TASK: это производный технический payload одного запуска.

В начале каждого будущего БЛОКА 1 должен существовать короткий dispatch-header, по которому сразу определяется исполнитель.

Если `EXECUTOR: CODEX`, Codex после header читает и выполняет основной body задачи обычным способом.

Если `EXECUTOR: ULTRA`, Codex является только техническим dispatcher:
он читает body и REQUIRED-документы только для полной сборки `03`, не выполняет содержательную задачу, запускает Ultra через `03 + 04` и после прямого запуска не читает результат.

Если `EXECUTOR: CODEX + ULTRA`, Codex остаётся основным исполнителем, формирует для Ultra отдельную подзадачу в `03`, запускает `03 + 04`, затем читает и критически проверяет `04` до использования результата.

Также устранить найденную после TASK 181 неоднозначность между functional allowlist и стандартным lifecycle.

================================================================================

ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ЛЮБЫМИ ИЗМЕНЕНИЯМИ:

Не экономить контекст на перечисленных ниже правилах.
Прочитать документы ПОЛНОСТЬЮ, а не только найденные поиском строки.

Сначала постоянный Ultra-контекст:

1. `Documentation_DayZ_Qwest/Ultra/00_Главная.md`
2. `Documentation_DayZ_Qwest/Ultra/01_Старт_Ultra.md`
3. `Documentation_DayZ_Qwest/Ultra/02_Правила_Ultra.md`

`03_ULTRA_TASK.md` сейчас не читать и не изменять: он будет полностью сформирован Codex только непосредственно перед будущим вызовом Ultra. `04_ULTRA_REPORT.md` — только выход вызова и входным контекстом не является.

Затем общий workflow проекта:

4. `Documentation_DayZ_Qwest/10_Правила_агента.md`
5. `Documentation_DayZ_Qwest/12_Старт_агента.md`
6. `Documentation_DayZ_Qwest/20_SplitDoc/00_INDEX.md`
7. `Documentation_DayZ_Qwest/20_SplitDoc/TASK_PREPARATION_RULES.md`
8. `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`
9. `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`
10. `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md`
11. `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
12. `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
13. `Documentation_DayZ_Qwest/21_Project_Docs/PROJECT_PATHS.md`

Текущий БЛОК 1 документа 11 уже является самой TASK и повторно как отдельный исторический документ не нужен.

`41_ANALYSIS_REPORT_TASK 180.md`, TASK_HISTORY, БЛОК 2, БЛОК 3 и предметные документы Quest / Migration / Zones / Map сейчас автоматически НЕ читать: они не нужны для решения этой конкретной workflow-задачи.

Если после чтения перечисленного набора обнаружится реальная обязательная зависимость ещё одного документа, разрешено дочитать его, но в ULTRA REPORT нужно назвать точную причину.

================================================================================

СНАЧАЛА ПРОВЕРИТЬ ТЕКУЩИЕ ПРОТИВОРЕЧИЯ:

Перед исправлением самостоятельно подтвердить минимум следующие точки.

1. `Ultra/01_Старт_Ultra.md` должен считать `03_ULTRA_TASK.md` launch-payload текущего вызова, а не пользовательским источником задачи.

2. `Ultra/02_Правила_Ultra.md` должен предписывать выполнять только launch-payload текущего вызова.

3. Правило о чтении и проверке `04` должно принадлежать dispatcher-маршрутизации, а не правилам Ultra.

4. После TASK 181 в `12_Старт_агента.md` functional allowlist разрешает менять только явно разрешённые TASK файлы/разделы.

5. Одновременно `12_Старт_агента.md` требует стандартный `TASK_LIFECYCLE_RULES` перед успешным завершением implementation/documentation TASK.

6. `TASK_LIFECYCLE_RULES.md` требует стандартные служебные изменения:
   - новый AGENT REPORT в БЛОК 2;
   - одну запись TASK_HISTORY;
   - очистку БЛОКА 1.

7. Проверить, однозначно ли сейчас разрешены эти lifecycle-изменения, если функциональный allowlist TASK содержит, например, только `migration.js`.

Не принимать перечисленные пункты на веру.
Подтвердить их фактическим текстом текущих документов.

================================================================================

СДЕЛАТЬ:

1. Ввести единый dispatch-контракт БЛОКА 1.

Будущая TASK должна в самом начале содержать короткий фиксированный header минимум с полями:

- `EXECUTOR: CODEX`, `EXECUTOR: ULTRA` или `EXECUTOR: CODEX + ULTRA`;
- режим роли Codex;
- task source;
- report target, если исполнитель Ultra;
- `AUTO-DELEGATION`.

Header должен находиться ДО подробного body задачи, чтобы Codex сразу определил роль и применил нужный режим чтения.

Если executor отсутствует, неизвестен или header повреждён — не угадывать исполнителя.

2. Зафиксировать правило для CODEX.

При `EXECUTOR: CODEX`:

- Codex продолжает читать body текущего БЛОКА 1;
- выполняет обычный маршрут задачи;
- использует стандартный Codex lifecycle.

При `EXECUTOR: ULTRA`:

- Codex читает body БЛОКА 1 и REQUIRED-документы только в объёме, необходимом для сборки полного launch-payload;
- содержательную задачу вместо Ultra не выполняет;
- полностью пересобирает `Documentation_DayZ_Qwest/Ultra/03_ULTRA_TASK.md` и не использует старое содержимое;
- включает в payload задачу, обязательные правила, ограничения, входные данные, критерии результата, необходимые пути и сведения;
- до запуска проверяет, что `03_ULTRA_TASK.md` существует и непустой;
- вызывает `gigachat_file_task`;
- task_file = `Documentation_DayZ_Qwest/Ultra/03_ULTRA_TASK.md`;
- report_file = `Documentation_DayZ_Qwest/Ultra/04_ULTRA_REPORT.md`;
- после возврата инструмента ULTRA REPORT не открывает;
- выводы Ultra не анализирует;
- пользователю сообщает только технический статус запуска и путь report_file;
- после этого останавливается.

Текущий bridge предоставляет Ultra текстовый вход и принимает текстовый ответ. Ultra может вернуть анализ, готовый текст, инструкции, diff или patch, но без файловых инструментов bridge это не означает физического изменения repository.

Независимую проверку ULTRA REPORT и, если Ultra меняла repository, фактического GitHub diff выполняет ChatGPT после передачи результата пользователем.

При `EXECUTOR: CODEX + ULTRA`:

- Codex остаётся основным исполнителем и определяет точную подзадачу для Ultra;
- полностью формирует новый непустой `03_ULTRA_TASK.md` и вызывает ту же пару `03 + 04`;
- после успешного вызова обязан прочитать `04`, сопоставить его с payload, критически проверить полноту и выводы;
- только после проверки использует результат Ultra и при необходимости применяет его в разрешённом scope основной задачи.

3. Автоматическую делегацию пока НЕ включать.

На этом этапе:

`AUTO-DELEGATION: OFF`

Codex НЕ решает самостоятельно, что задачу лучше отдать Ultra.

Исполнителя задаёт ChatGPT / пользователь в dispatch-header.

Возможность будущего режима AUTO можно оставить как будущую идею, но не реализовывать сейчас.

4. Разделить пользовательский источник и технический payload.

Обновить правила так, чтобы:

- пользовательская CODEX TASK живёт в БЛОКЕ 1;
- пользовательская ULTRA TASK тоже живёт в том же БЛОКЕ 1;
- `Ultra/03_ULTRA_TASK.md` хранит только одноразовый технический payload конкретного запуска, сформированный Codex.

Не удалять `03_ULTRA_TASK.md`.

Сейчас `03_ULTRA_TASK.md` не изменять. Он может оставаться пустым между запусками. Перед каждым будущим вызовом Codex обязан полностью заменить его новым payload и проверить непустоту.

Не создавать второй U1-блок и второй task shell.

5. Обновить постоянные документы Ultra.

`Ultra/00_Главная.md`:
обновить карту процесса: `03` — launch-payload, сформированный Codex; пользователь вручную его не поддерживает.

`Ultra/01_Старт_Ultra.md`:
зафиксировать:
- канонический project root P:;
- текущий launch-payload берётся из `03_ULTRA_TASK.md`, полностью сформированного Codex перед вызовом;
- старое содержимое предыдущего запуска не является текущей задачей;
- затем читать собственные правила и документы, назначенные payload;
- предметные задачи требуют актуальных боевых документов соответствующей подсистемы.

`Ultra/02_Правила_Ultra.md`:
закрепить выполнение только launch-payload текущего вызова из `03`;
не помещать сюда dispatcher-логику чтения `04`;
зафиксировать, что:
- Ultra выполняет только переданный launch-payload;
- report пишет только в `04_ULTRA_REPORT.md`;
- Ultra не пишет AGENT REPORT в БЛОК 2;
- Ultra не чистит БЛОК 1;
- Ultra не пишет TASK_HISTORY автоматически;
- Ultra не меняет БЛОК 3;
- после ULTRA REPORT останавливается;
- независимый review делает ChatGPT / пользовательский цикл, а не Codex dispatcher.

6. Обновить общий workflow.

Синхронизировать минимум:

- `11_Задача_агенту.md`;
- `12_Старт_агента.md`;
- `CONTEXT_ROUTING.md`;
- `Ultra/00_Главная.md`;
- `Ultra/01_Старт_Ultra.md`;
- `Ultra/02_Правила_Ultra.md`.

Каноническим владельцем трёх режимов и логики чтения `04` является `CONTEXT_ROUTING.md`. В `12_Старт_агента.md` оставить только короткий указатель, а в Ultra-документах — только правила, необходимые самой Ultra.

Не копировать большие одинаковые правила во все документы.
Выбрать канонических владельцев, а в остальных оставить короткие ссылки/инварианты.

7. Исправить lifecycle / allowlist неоднозначность TASK 181.

Для обычной успешной задачи с:

`EXECUTOR: CODEX`

стандартные lifecycle-изменения должны иметь узкое постоянное служебное разрешение и НЕ требовать перечислять в functional allowlist каждой TASK:

- замену БЛОКА 2 новым AGENT REPORT;
- ровно одну запись текущей TASK в TASK_HISTORY;
- штатную очистку завершённого БЛОКА 1.

Это служебное разрешение НЕ расширяет functional scope задачи.

Например, если functional allowlist содержит только:

`migration.js`

это НЕ разрешает Codex менять соседний JS, JSON, Map, runtime или другие файлы.

Разрешаются только три перечисленные lifecycle-операции.

БЛОК 3 не изменяется без отдельного прямого разрешения.

TASK может прямо отключить стандартный lifecycle, если режим/задача этого требует.

`ANALYSIS ONLY` автоматически lifecycle не запускает.

Для:

`EXECUTOR: ULTRA`

стандартный Codex lifecycle автоматически НЕ выполняется:

- БЛОК 2 не менять;
- TASK_HISTORY не менять;
- БЛОК 1 не очищать;
- БЛОК 3 не менять;
- результат только в `Ultra/04_ULTRA_REPORT.md`.

После review закрытие / очистка текущей Ultra TASK выполняется отдельным явным действием следующего шага.

8. Уточнить review результата Ultra.

Если Ultra выполняла ANALYSIS ONLY и repository не меняла:
ChatGPT проверяет её ULTRA REPORT и источники без требования commit.

Если Ultra меняла repository:
пользователь делает commit/push;
после этого ChatGPT проверяет exact GitHub SHA/diff и изменённые файлы так же независимо, как обычный implementation result.

В прямом режиме `EXECUTOR: ULTRA` Codex dispatcher не должен читать ULTRA REPORT ради review. В режиме `EXECUTOR: CODEX + ULTRA` Codex обязан прочитать и проверить отчёт до использования результата.

================================================================================

РАЗРЕШЕНО МЕНЯТЬ:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`

- `Documentation_DayZ_Qwest/12_Старт_агента.md`

- `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`

- `Documentation_DayZ_Qwest/Ultra/00_Главная.md`

- `Documentation_DayZ_Qwest/Ultra/01_Старт_Ultra.md`

- `Documentation_DayZ_Qwest/Ultra/02_Правила_Ultra.md`

Другие файлы без доказанной необходимости не изменять. `03_ULTRA_TASK.md` сейчас не заполнять и не изменять; `04_ULTRA_REPORT.md` не открывать и не читать.

================================================================================

ЗАПРЕЩЕНО:

- менять БЛОК 2;
- менять БЛОК 3;
- очищать БЛОК 1;
- писать TASK_HISTORY для этой задачи;
- менять `41_ANALYSIS_REPORT_TASK 180.md`;
- менять предметные документы 14–40;
- менять Quest / Migration / Map / Zones / Medicine документы;
- менять код;
- менять JSON;
- менять runtime;
- менять PBO/build output;
- менять `.obsidian`;
- удалять или переименовывать документы;
- создавать второй пользовательский источник текущей TASK;
- включать AUTO-DELEGATION;
- выполнять Git write-команды.

Read-only Git использовать только если действительно нужен для проверки baseline/diff и это не изменяет repository.

================================================================================

КОДИРОВКА:

Полный `ENCODING_RULES.md` обязателен для этой задачи.

Не считать краткие правила из START достаточными.

Перед записью каждого Markdown-файла:
- определить и сохранить корректную кодировку;
- не повреждать кириллицу;
- не делать массовое пересохранение;
- использовать минимальные патчи;
- новые/нормализованные Markdown держать UTF-8 без BOM;
- при любом mojibake / `�` / повторяющихся `?` остановиться и действовать по аварийному протоколу.

================================================================================

ПРОВЕРИТЬ ПОСЛЕ ИЗМЕНЕНИЙ:

Сценарий A — CODEX:

БЛОК 1 начинается с:
`EXECUTOR: CODEX`

Ожидается:
- Codex читает dispatch-header;
- затем читает body;
- выполняет TASK;
- functional allowlist остаётся жёстким;
- стандартный lifecycle имеет только узкое служебное разрешение.

Сценарий B — ULTRA:

БЛОК 1 начинается с:
`EXECUTOR: ULTRA`

Ожидается:
- Codex читает body и REQUIRED-документы только для сборки полного payload;
- саму содержательную TASK не выполняет;
- полностью пересобирает и проверяет непустоту `03_ULTRA_TASK.md`;
- вызывает `gigachat_file_task` только с парой `03_ULTRA_TASK.md` + `04_ULTRA_REPORT.md`;
- Ultra выполняет launch-payload и читает назначенные в нём документы;
- Ultra выполняет TASK;
- Ultra пишет `04_ULTRA_REPORT.md`;
- Codex report не читает;
- БЛОК 1 остаётся неизменным;
- БЛОК 2/3 и TASK_HISTORY остаются неизменными.

Сценарий C — CODEX + ULTRA:

БЛОК 1 начинается с:
`EXECUTOR: CODEX + ULTRA`

Ожидается:
- Codex остаётся основным исполнителем;
- формирует отдельную подзадачу в новом непустом `03`;
- вызывает каноническую пару `03 + 04`;
- читает и критически проверяет `04` до использования результата.

Сценарий D — повреждённый header:

`EXECUTOR` отсутствует или имеет неизвестное значение.

Ожидается:
- исполнитель не угадывается;
- выполнение/dispatch не начинается;
- запрашивается исправление header.

Сценарий E — lifecycle:

Functional allowlist:
`migration.js`

Executor:
`CODEX`

Проверить, что разрешены:
- `migration.js`;
- только три стандартные lifecycle-операции.

И НЕ разрешены никакие другие функциональные файлы.

Сценарий F — ANALYSIS ONLY:

Проверить, что стандартный lifecycle не запускается автоматически.

Дополнительно проверить:

- активные документы не называют `03_ULTRA_TASK.md` вторым пользовательским источником TASK или файлом, который не участвует в запуске;
- в `EXECUTOR: ULTRA` Codex не читает отчёт, а в `EXECUTOR: CODEX + ULTRA` обязательно читает и проверяет его;
- каноническая bridge-пара везде одна: `03_ULTRA_TASK.md` + `04_ULTRA_REPORT.md`;
- нет двух пользовательских владельцев текущей TASK;
- wiki-links разрешаются;
- Markdown корректен;
- UTF-8 без BOM;
- `git diff --check` без whitespace-errors;
- нет случайного изменения БЛОКОВ 2/3;
- нет изменений вне allowlist.

================================================================================

ULTRA REPORT:

Весь итоговый отчёт записать ТОЛЬКО в:

`P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\04_ULTRA_REPORT.md`

Формат:

ULTRA REPORT

TASK:
TASK 182

STATUS:
COMPLETED / BLOCKED

READ:
- какие обязательные документы реально прочитаны полностью;
- какие дополнительные документы пришлось прочитать и почему.

CONFIRMED BEFORE FIX:
- какие реальные противоречия подтверждены до изменения.

CHANGED FILES:
- точный список.

WORKFLOW CONTRACT:
- итоговая схема CODEX;
- итоговая схема ULTRA;
- где теперь единственный источник TASK;
- где теперь единственный Ultra report.

LIFECYCLE FIX:
- как устранена неоднозначность functional allowlist / lifecycle;
- почему служебное разрешение не расширяет functional scope.

SCENARIO CHECKS:
- A CODEX;
- B ULTRA;
- C invalid header;
- D lifecycle;
- E ANALYSIS ONLY.

ENCODING CHECK:
- UTF-8 / BOM / кириллица / mojibake.

PROBLEMS:
- только реальные проблемы или незакрытые противоречия.

CONCLUSION:
- краткий итог.

После записи ULTRA REPORT:

- НЕ очищать БЛОК 1;
- НЕ писать БЛОК 2;
- НЕ писать БЛОК 3;
- НЕ писать TASK_HISTORY;
- НЕ выполнять Git write-команды;
- остановиться и ждать независимого review пользователя / ChatGPT.

Моды обновлять не нужно.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK: TASK 181 — Оптимизация контекста: разделить роли и сократить обязательное чтение.

## STATUS

COMPLETED / DOCUMENTATION ARCHITECTURE.

## BASELINE

- Канонический проект: `P:\Silver_77_Quests`.
- Локальный `main`: commit message `93`, SHA `2a36675cea2474751b32ded5ee24015f8c269415`.
- Полный `ANALYSIS REPORT — TASK 180` прочитан до начала реализации и сопоставлен с БЛОКОМ 1 и текущими документами.
- Git write-команды не выполнялись; commit/push выполняет пользователь.

## CREATED FILES

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_PREPARATION_RULES.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`
- `Documentation_DayZ_Qwest/21_Project_Docs/PROJECT_PATHS.md`

## CHANGED FILES

- `Documentation_DayZ_Qwest/10_Правила_агента.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/00_INDEX.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md` — одна запись TASK 181
- `Documentation_DayZ_Qwest/21_Project_Docs/00_INDEX.md`

## MOVED RULES

- Подготовка TASK, slices, baseline, scope, два блока, уточнения и ориентиры размера перенесены из `12` в `TASK_PREPARATION_RULES.md`.
- Тематическая матрица удалена из `11`; выбор контекста по роли, стадии и операции перенесён в `CONTEXT_ROUTING.md`.
- AGENT REPORT, TASK_HISTORY, BLOCKED, очистка БЛОКА 1 и правило об обновлении модов перенесены в `TASK_LIFECYCLE_RULES.md`.
- Рабочие roots и граница source/packed output перенесены из обязательного ядра в `PROJECT_PATHS.md`.
- Подробный REVIEW остался у существующего канонического владельца `GIT_REVIEW_RULES.md`.

## REMOVED DUPLICATION

- `12_Старт_агента.md` сокращён до обязательного agent-core; подготовка, lifecycle, проектная карта и подробный review больше не читаются исполнителем автоматически.
- `11_Задача_агенту.md` оставлен shell-документом TASK / REPORT / REVIEW без тематической матрицы.
- `30_Единая_система_проекта.md`, БЛОКИ 2/3, TASK_HISTORY и аварийный encoding-протокол исключены из постоянного старта.
- Штатное lifecycle-обновление больше не объявляет каждую code/FIX-задачу полноценной Documentation TASK.
- Тематические документы сохранены цельными владельцами предметных правил; их технические контракты в маршрутизатор не копировались.

## CONTEXT BEFORE / AFTER

- A. Обязательный старт обычного агента, без исходников и БЛОКА 1: `35 415 → 4 926` символов; сокращение `86,1%`.
- B. Маленький Migration/Editor FIX до тематического кода, включая узкий `QUEST_EDITOR_RULES` и lifecycle: `46 584 → 12 994` символа; сокращение `72,1%`.
- Для B исходный маршрут включал старую оболочку 11, полный 12, обязательный 30, `QUEST_EDITOR_RULES` и полный `ENCODING_RULES`; новый ASCII-only маршрут включает короткий 12, `QUEST_EDITOR_RULES` и `TASK_LIFECYCLE_RULES`.

## CHECKS

- Все новые документы подключены к индексам, wiki-links разрешаются.
- `12_Старт_агента.md`: 4 926 символов, целевой диапазон 3–5 тыс. соблюдён.
- Активные управляющие документы не используют `AGENT_TASK_LOOP.md` или неоднозначный `START.md` как текущие источники.
- `30_Единая_система_проекта.md` назначается только крупным межмодульным/архитектурным задачам или прямым указанием.
- БЛОК 1 очищен по новой lifecycle-схеме; БЛОК 2 обновлён; БЛОК 3 не изменён.
- `git diff --check` пройден; присутствуют только предупреждения Git о будущей нормализации LF/CRLF, ошибок whitespace нет.

## ENCODING CHECK

PASS.

- Все новые и изменённые Markdown-файлы строго декодируются как UTF-8 без BOM.
- NUL отсутствует; кириллица читается нормально.
- Примеры mojibake в правилах кодировки сохранены намеренно как диагностические маркеры, а не как повреждение текста.

## PROBLEMS

- До TASK 181 в working tree уже находились нормализация анализа TASK 180/ветки Ultra и внешнее изменение `.obsidian/graph.json`. Они сохранены и не включались в реализацию TASK 181.

## CONCLUSION

TASK 181 завершена. Постоянный контекст разделён по роли, стадии и предметной области без удаления тематических контрактов и safety-core. Код, JSON, runtime, PBO и документы 14–40 не менялись. Моды обновлять не нужно.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
