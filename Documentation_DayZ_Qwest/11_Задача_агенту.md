# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`

## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки под заголовком `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не должно оставаться содержимого завершённой задачи.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Текущая задача отсутствует.

Следующая задача должна быть вставлена сюда полностью.

После установки новой задачи отправить агенту отдельный малый блок запуска.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 147 — Управление сменой погоды на уровне migration scenario`.

STATUS:
- `IMPLEMENTED / WAITING FOR RUNTIME`.
- Git write-команды не выполнялись.

SCENARIO WEATHER FLAG:
- В `S77MigrateScenarioConfig` добавлен `int weatherChangeEnabled`.
- `1` означает, что scenario запрашивает существующий глобальный weather transition; `0` означает, что scenario сам его не инициирует.
- Флаг относится к scenario целиком, а не к отдельным infected.

DEFAULT / NORMALIZE:
- `SetBaseDefaults()` устанавливает `weatherChangeEnabled = 1`, поэтому отсутствующее поле старого runtime JSON получает совместимый default только в памяти.
- `Normalize()` сохраняет только значение `1`; любое другое значение приводит к `0`.
- Оба source scenarios содержат явное `"weatherChangeEnabled": 1`, поэтому default-поведение не изменилось.

GLOBAL MASTER SWITCH:
- Корневой `weatherEnabled` сохранён без изменения смысла и остаётся главным выключателем weather subsystem.
- При `weatherEnabled = 0` scenario flags не могут запустить transition.

BATCH WEATHER DECISION:
- Новый helper `CountWeatherRequestingScenarios()` считает запросы только среди уже отфильтрованных enabled scenarios в `m_Scenarios`.
- `global=1` и хотя бы один request дают ровно один существующий transition, после которого вызывается общая `StartAllScenarios()`.
- `global=1` и ноль requests либо `global=0` пропускают transition и планируют `StartAllScenarios()` после обычного `eventDelaySeconds`.
- Weather callbacks, transition duration, storm ramp, weather values и `ReleaseWeatherControl()` не менялись.

GLOBAL WEATHER LIMITATION:
- DayZ weather глобальна. Scenario с `weatherChangeEnabled = 0` в смешанной пачке сам не инициирует transition, но находится в погоде, запрошенной другим scenario с `1`.
- Будущий отдельный `StartEvent(eventId)` не реализован; при его будущей реализации флаг сможет управлять запросом отдельного события.

LOGGING:
- При включённом TASK 146 logging startup пишет одну event-only строку `WEATHER_DECISION` с `globalEnabled`, числом requesting/enabled scenarios и результатом.
- Возможные результаты: `TRANSITION`, `SKIP_NO_SCENARIO_REQUEST`, `SKIP_GLOBAL_DISABLED`.
- High-frequency logging не добавлялось; TASK 146 stuck/recovery logging не изменялось.

RUNTIME JSON PATCH:
- Корень существующего runtime JSON сохраняет master switch:
  - `"weatherEnabled": 1`
- В каждый нужный объект массива `scenarios` вручную добавить одно из значений:
  - `"weatherChangeEnabled": 1` — массовая migration wave запрашивает погоду;
  - `"weatherChangeEnabled": 0` — локальный patrol/quest group сам погоду не инициирует.
- Runtime profile автоматически не открывался и не изменялся.

CHANGED FILES:
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateManager.c`
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateConfig.c`
- `Silver_77_Migrate_server/config/MigrationConfig.json`
- `Silver_77_Migrate_server/config/README_RU.md`
- `Documentation_DayZ_Qwest/14_Квест_Миграция.md`
- `Documentation_DayZ_Qwest/15_Сценарий_Миграции_001.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md`

STATIC CHECKS:
- Source JSON успешно разобран; scenarios=`2`, flags=`1,1`, root `weatherEnabled=1`.
- Таблица решений проверена статически: `global 1 + 0,0 -> skip`; `global 1 + 0,1 -> transition`; `global 0 + 1,1 -> skip`.
- Все исходные weather/storm values совпадают с `HEAD`; migration AI, TASK 141/142/144 и TASK 146 semantics не менялись.
- `StartAllScenarios()` остаётся в взаимоисключающих startup-ветках и после единственного завершения weather transition; новые callbacks/scheduler не добавлены.
- Braces/parentheses/brackets сбалансированы; UTF-8 валиден, BOM отсутствует; Markdown и `git diff --check` проверены.
- Runtime profile, `.obsidian/*`, quest JSON, PBO и сервер не изменялись/не запускались.

RUNTIME CHECKLIST:
1. `weatherEnabled=1`, все scenario flags `0`: ожидать `SKIP_NO_SCENARIO_REQUEST`, отсутствие transition и старт после `eventDelaySeconds`.
2. `weatherEnabled=1`, хотя бы один scenario flag `1`: ожидать один `TRANSITION`, один weather transition и затем один общий старт групп.
3. `weatherEnabled=0`, scenario flag `1`: ожидать `SKIP_GLOBAL_DISABLED` и отсутствие transition.
4. Смешанная пачка `0 + 1`: ожидать один global transition; scenario с `0` не инициирует его, но находится в общей погоде.

RISKS / OPEN QUESTIONS:
- Все четыре ветки требуют runtime-подтверждения после пересборки только `Silver_77_Migrate_server`; PBO и сервер в TASK 147 не запускались.
- Отдельный запуск events и конкурентный weather arbitration остаются будущими задачами; текущая реализация обслуживает только одну общую startup-пачку.

UNEXPECTED CHANGES:
- `.obsidian/workspace.json` был изменён до основной работы TASK 147, находится вне scope и агентом не редактировался.

ENCODING CHECK:
- Все изменённые текстовые файлы читаются как UTF-8 без BOM; кириллица не повреждена.

BLOCK 1 CLEANUP CHECK: PASS
- Существующий заголовок БЛОКА 1 сохранён в одном экземпляре.
- TASK 147 удалена из активного БЛОКА 1.
- Оставлены три стандартные строки; разделитель и БЛОК 2 сохранены.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
