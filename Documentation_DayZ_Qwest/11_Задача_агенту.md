# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки `БЛОК 1` должен выглядеть так:

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

================================================================================
# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 134 — Многоточечный маршрут migration infected + финальный AI-стимул + release в vanilla AI`.

RULES PATH:
- `P:\Silver_77_Quests\Documentation_DayZ_Qwest`

STATUS:
- Реализация завершена и статически проверена.
- PBO не собирался, сервер и runtime profile не запускались.

CHANGED FILES:
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateConfig.c`
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateManager.c`
- `Silver_77_Migrate_server/config/MigrationConfig.json`
- `Silver_77_Migrate_server/config/README_RU.md`
- `Documentation_DayZ_Qwest/15_Сценарий_Миграции_001.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md` — отчёт обновлён, `БЛОК 1` очищен.

VANILLA AI / NOISE RESEARCH:
- Исследованы `P:\scripts\3_game\noise.c`, `P:\scripts\4_world\static\miscgameplayfunctions.c`, `P:\scripts\4_world\static\sensesaievaluate.c`, реальные usages `AddNoiseTarget` / `AddNoisePos` / `GenerateAINoiseAtPosition`, загрузка `NoiseParams` в `dayzplayer.c`, infected controller/mind-state scripts и локальные vanilla configs.
- Выбран точный API `NoiseSystem::AddNoiseTarget(vector pos, float lifetime, NoiseParams noiseParams, float external_strength_multiplier)`.
- Используется `NoiseParams.LoadFromPath("CfgVehicles SurvivorBase NoiseShout")`. Preset реально существует в `P:\DZ\characters\data\config.cpp`: `strength=40`, `type="sound"`.
- `GenerateAINoiseAtPosition()` подтверждён как server-side wrapper над `AddNoiseTarget()`, но прямой вызов выбран потому, что он поддерживает требуемый strength multiplier.
- Механизм является AI perception stimulus: код не создаёт `SoundSet`, `SoundObject`, siren, explosion или другой слышимый игроку fake sound.
- Публичного script-side API для принудительной установки infected `CHASE` не найдено. `AddNoiseTarget()` создаёт perception target, а native AI самостоятельно выбирает реакцию и mind state.
- Статически известна базовая сила preset `40`, но эффективную дальность и фактическую реакцию infected нельзя гарантировать только по script/config: они требуют runtime tuning и наблюдения native AI.

JSON FIELDS / DEFAULTS:
- В каждый scenario добавлены `routePoints=[]`, `routePointReachRadius=6.0`, `finalActivationEnabled=1`, `finalActivationDistance=50.0`, `finalStimulusLifetimeSeconds=1.0`, `finalStimulusStrengthMultiplier=1.0`.
- `routePoints` реализован как динамический nested array координат `[X,Y,Z]`; искусственного лимита количества точек нет, пустой массив валиден.
- Source `MigrationConfig.json` сохраняет оба сценария и не содержит придуманных промежуточных координат.

ROUTE POINTS:
- Путь строится последовательно: spawn -> каждый manual route point -> final target.
- Для каждого сегмента отдельно используются существующие `SampleNavmeshPosition()`, `AIWorld::FindPath()`, PGFilter и path retry.
- Каждый infected хранит собственный route point index и проходит точки независимо от остальных.
- Один individual route/target formation offset вычисляется один раз при spawn и применяется ко всем ручным точкам и индивидуальной конечной цели; новый random jitter на точках не создаётся.
- Точка считается пройденной по горизонтальной X/Z-дистанции не более `routePointReachRadius`; после этого index увеличивается и строится следующий сегмент.
- Player aggro handoff и возврат после `CALM`/cooldown сохранены: путь пересчитывается к текущей непройденной точке либо final target.

FINAL ACTIVATION / RELEASED:
- Финальная проверка разрешена только после прохождения всех manual points и выполняется отдельно для каждого живого infected.
- При горизонтальной дистанции не более `50` метров от центра `targetPosition` снимаются route overrides, stimulus создаётся точно в `targetPosition` на default `1.0` секунду со strength multiplier `1.0`, затем infected получает терминальный `RELEASED`.
- Однократность обеспечивают индивидуальные флаги `m_FinalStimulusTriggered` и `m_Released`; `RELEASED` пропускается в `UpdateRoutes()`, не получает `BuildPath()`, retry, cooldown-return или новые overrides.
- Entity не удаляется, spawn flags не изменены, `ECE_NOLIFETIME` не добавлен. После release infected остаётся vanilla AI; nearby infected могут естественно воспринять world stimulus.
- При `finalActivationEnabled=0` infected проходит manual route до обычного arrival, затем получает `RELEASED` без stimulus.
- Если preset или `NoiseSystem` недоступны, пишется одно понятное warning/error, infected не отпускается за 50 метров, продолжает обычный путь до arrival и затем получает `RELEASED`.
- Существующие `IsAlive()` guards перед `GetInputController()` и crash protection для уничтоженных infected сохранены.

RUNTIME / PRESERVED BEHAVIOR:
- Существующий `$profile:Silver_77_Migrate/MigrationConfig.json` не читался и destructively не переписывался.
- Missing TASK 134 nested fields получают constructor defaults только в памяти; loader не сохраняет и не пересортировывает существующий runtime JSON.
- Weather/storm TASK 132/133, event timeline, spawn flags, скорость, PGFilter, vanilla aggro, cooldown и периодическое безопасное логирование вне требуемого route refactor не менялись.
- Scheduler, random dates, persistence, player scaling, client code, RPC, custom sound и forced mind state не реализовывались.

VERIFICATION:
- Unified source JSON валиден; в source config-каталоге остаётся один active JSON — `MigrationConfig.json`.
- Оба сценария и прежние global weather/storm defaults сохранены.
- Проверены динамический route array, варианты `0/1/несколько` точек по общей индексной логике, последовательное переключение сегментов, стабильный offset и X/Z distance checks.
- Проверены все `GetInputController()`: route update, periodic log и stop имеют entity/`IsAlive()` guards; `Stop()` дополнительно пропускает `RELEASED`.
- Запрещённые `SoundSet`, `SoundObject`, forced mind state, delete entity и `ECE_NOLIFETIME` в реализации отсутствуют.
- Enforce Script delimiters сбалансированы.
- Все изменённые task-файлы читаются как строгий UTF-8 без BOM; кириллица корректна.
- Markdown сохранён, `git diff --check` завершён без ошибок.
- `.obsidian/workspace.json` не изменялся.

BUILD:
- Для runtime-проверки кода TASK 134 потребуется новый PBO только `Silver_77_Migrate_server`.
- Quest client/server моды обновлять не нужно.
- PBO не собирался, сервер не запускался.

PROBLEMS / RUNTIME CHECK:
- Compile/runtime-проверка запрещена задачей и не выполнялась.
- Нужно runtime подтвердить реальную реакцию native infected на `NoiseShout`, эффективную дальность/strength tuning, последовательное прохождение пользовательских route points и окончательное отсутствие migration control после `RELEASED`.

UNEXPECTED PRE-EXISTING CHANGE:
- `JSON_Quvest/Silver_77_Quests.json` был изменён до TASK 134. Агент его не редактировал и не откатывал.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================
# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
