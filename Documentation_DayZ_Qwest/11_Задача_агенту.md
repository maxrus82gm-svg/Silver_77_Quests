# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`.

Правила:

- короткое обязательное ядро: `Documentation_DayZ_Qwest/12_Старт_агента.md`;
- подготовка TASK: `Documentation_DayZ_Qwest/20_SplitDoc/TASK_PREPARATION_RULES.md`;
- маршрутизация контекста: `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`;
- завершение TASK: `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`.

## Стартовое чтение исполнителя

Агент начинает с:

- только текущего `БЛОКА 1` этого документа;
- короткого `Documentation_DayZ_Qwest/12_Старт_агента.md`;
- документов, прямо перечисленных в текущем БЛОКЕ 1;
- явно обозначенных обязательных зависимостей выбранного маршрута.

`БЛОК 2` и `БЛОК 3` автоматически не читать. Они открываются только для связанного продолжения, review или по прямому указанию.

Тематическая матрица находится только в `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`. Обычная ссылка не означает рекурсивного чтения всего связанного графа.

## Завершение

Перед успешным завершением implementation/documentation-задачи использовать `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`.

При `BLOCKED`, неполном результате или нерешённой критичной ошибке БЛОК 1 не очищать.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

TASK 182 — FIX workflow: единый БЛОК 1 для Codex / Ultra и однозначный lifecycle

================================================================================
DISPATCH HEADER — СНАЧАЛА ЧИТАЕТ CODEX

EXECUTOR: ULTRA
CODEX MODE: PAYLOAD BUILD + DISPATCH ONLY
ULTRA MODE: DOCUMENTATION PATCH PREPARATION

USER TASK SOURCE:
P:\Silver_77_Quests\Documentation_DayZ_Qwest\11_Задача_агенту.md
БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

ULTRA REPORT TARGET:
P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\04_ULTRA_REPORT.md

ULTRA LAUNCH PAYLOAD:
P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\03_ULTRA_TASK.md

AUTO-DELEGATION: OFF

Если этот header читает Codex:
после строки END DISPATCH HEADER прочитать body и REQUIRED-документы только в объёме, необходимом для сборки полного launch-payload.
Содержательную задачу вместо Ultra не выполнять.
Полностью пересобрать `03_ULTRA_TASK.md`, проверить его непустоту, вызвать `gigachat_file_task` с парой `03_ULTRA_TASK.md` + `04_ULTRA_REPORT.md` и после прямого запуска не читать ULTRA REPORT.

END DISPATCH HEADER
================================================================================

ИСПОЛНИТЕЛЬ ДАЛЬНЕЙШЕЙ ЧАСТИ:
ULTRA

ТИП:
DOCUMENTATION WORKFLOW FIX

BASELINE:

Repository:
maxrus82gm-svg/Silver_77_Quests

Branch:
main

Commit message:
99

Full SHA:
d380ce6bdd8aae344f1e6f535ba47cf2159692a2

Канонический рабочий проект:
P:\Silver_77_Quests

Каноническая документация:
P:\Silver_77_Quests\Documentation_DayZ_Qwest

================================================================================
ЦЕЛЬ
================================================================================

Сделать один однозначный агентский workflow для трёх режимов исполнения:

- CODEX;
- ULTRA;
- CODEX + ULTRA.

Текущая TASK всегда должна жить только в одном месте:

`Documentation_DayZ_Qwest/11_Задача_агенту.md`
→ `БЛОК 1`.

`Ultra/03_ULTRA_TASK.md` не является вторым пользовательским источником TASK.
Это производный технический payload одного конкретного запуска Ultra.

В начале каждого будущего БЛОКА 1 должен существовать короткий dispatch-header,
по которому сразу определяется исполнитель.

Если `EXECUTOR: CODEX`,
Codex после header читает и выполняет основной body задачи обычным способом.

Если `EXECUTOR: ULTRA`,
Codex является только техническим dispatcher:
он читает body и REQUIRED-документы только для полной сборки `03`,
не выполняет содержательную задачу,
запускает Ultra через `03 + 04`
и после прямого запуска не читает результат.

Если `EXECUTOR: CODEX + ULTRA`,
Codex остаётся основным исполнителем,
формирует для Ultra отдельную подзадачу в `03`,
запускает `03 + 04`,
затем читает и критически проверяет `04` до использования результата.

Также устранить найденную после TASK 181 неоднозначность
между functional allowlist и стандартным lifecycle.

ВАЖНО О ТЕКУЩЕМ BRIDGE:

Текущий `gigachat_file_task` передаёт Ultra текст задачи
и принимает текстовый ответ.

Он НЕ предоставляет Ultra файловых инструментов.

Следовательно, в рамках текущего запуска Ultra НЕ должна утверждать,
что физически изменила repository или документацию.

Для TASK 182 Ultra должна подготовить максимально готовую
DOCUMENTATION IMPLEMENTATION-спецификацию:

- точные изменения;
- точный список файлов;
- конкретные replacement-блоки и/или diff/patch;
- объяснение каждой правки;
- проверки после применения.

Физическое применение результата будет отдельным следующим действием
после независимого review.

================================================================================
ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ЛЮБЫМ ВЫВОДОМ
================================================================================

Не экономить контекст на перечисленных ниже правилах.

Codex обязан прочитать документы ПОЛНОСТЬЮ перед сборкой launch-payload,
а не только найденные поиском строки.

Весь контекст, реально необходимый Ultra,
Codex обязан включить в `03_ULTRA_TASK.md` текстом.

Нельзя рассчитывать, что Ultra самостоятельно откроет документы по путям.

Сначала постоянный Ultra-контекст:

1. `Documentation_DayZ_Qwest/Ultra/00_Главная.md`
2. `Documentation_DayZ_Qwest/Ultra/01_Старт_Ultra.md`
3. `Documentation_DayZ_Qwest/Ultra/02_Правила_Ultra.md`

Затем общий workflow проекта:

4. `Documentation_DayZ_Qwest/10_Правила_агента.md`
5. `Documentation_DayZ_Qwest/12_Старт_агента.md`
6. `Documentation_DayZ_Qwest/20_SplitDoc/00_INDEX.md`
7. `Documentation_DayZ_Qwest/20_SplitDoc/TASK_PREPARATION_RULES.md`
8. `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`
9. `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`
10. `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md`
11. `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
12. `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
13. `Documentation_DayZ_Qwest/21_Project_Docs/PROJECT_PATHS.md`

Текущий БЛОК 1 документа 11 уже является самой TASK
и повторно как отдельный исторический документ не нужен.

`41_ANALYSIS_REPORT_TASK 180.md`,
TASK_HISTORY,
БЛОК 2,
БЛОК 3
и предметные документы Quest / Migration / Zones / Map
автоматически НЕ читать:
они не нужны для решения этой конкретной workflow-задачи.

Если после чтения перечисленного набора обнаружится
реальная обязательная зависимость ещё одного документа,
разрешено дочитать его.

В ULTRA REPORT нужно назвать:
- какой дополнительный документ потребовался;
- почему без него нельзя было корректно решить TASK 182.

================================================================================
СНАЧАЛА ПРОВЕРИТЬ ТЕКУЩИЕ ПРОТИВОРЕЧИЯ
================================================================================

Перед подготовкой исправления самостоятельно подтвердить
фактическим текстом текущих документов минимум следующие точки.

1. `Ultra/01_Старт_Ultra.md` должен считать
   `03_ULTRA_TASK.md` launch-payload текущего вызова,
   а не пользовательским источником задачи.

2. `Ultra/02_Правила_Ultra.md` должен предписывать
   выполнять только launch-payload текущего вызова.

3. Правило о чтении и проверке `04`
   должно принадлежать dispatcher-маршрутизации,
   а не правилам самой Ultra.

4. После TASK 181 в `12_Старт_агента.md`
   functional allowlist разрешает менять только
   явно разрешённые TASK файлы/разделы.

5. Одновременно `12_Старт_агента.md`
   требует стандартный `TASK_LIFECYCLE_RULES`
   перед успешным завершением implementation/documentation TASK.

6. `TASK_LIFECYCLE_RULES.md`
   требует стандартные служебные изменения:
   - новый AGENT REPORT в БЛОК 2;
   - одну запись TASK_HISTORY;
   - очистку БЛОКА 1.

7. Проверить, однозначно ли сейчас разрешены
   эти lifecycle-изменения,
   если functional allowlist TASK содержит, например,
   только `migration.js`.

Не принимать перечисленные пункты на веру.

Сначала подтвердить фактическое состояние,
потом предлагать исправление.

================================================================================
СДЕЛАТЬ
================================================================================

1. ВВЕСТИ ЕДИНЫЙ DISPATCH-КОНТРАКТ БЛОКА 1

Будущая TASK должна в самом начале содержать
короткий фиксированный header минимум с полями:

- `EXECUTOR: CODEX`,
  `EXECUTOR: ULTRA`
  или `EXECUTOR: CODEX + ULTRA`;

- режим роли Codex;

- task source;

- report target, если используется Ultra;

- launch payload, если используется Ultra;

- `AUTO-DELEGATION`.

Header должен находиться ДО подробного body задачи,
чтобы Codex сразу определил роль
и применил правильный режим чтения.

Если `EXECUTOR` отсутствует,
имеет неизвестное значение
или header повреждён —
не угадывать исполнителя.

Выполнение и dispatch не начинать.

================================================================================
2. ЗАФИКСИРОВАТЬ РЕЖИМ EXECUTOR: CODEX
================================================================================

При:

`EXECUTOR: CODEX`

Codex:

- читает body текущего БЛОКА 1;
- читает REQUIRED-контекст выбранного маршрута;
- выполняет содержательную TASK самостоятельно;
- Ultra не запускает;
- `03_ULTRA_TASK.md` не использует;
- `04_ULTRA_REPORT.md` не использует;
- применяет стандартный Codex lifecycle,
  если TASK прямо его не отключает.

================================================================================
3. ЗАФИКСИРОВАТЬ РЕЖИМ EXECUTOR: ULTRA
================================================================================

При:

`EXECUTOR: ULTRA`

Codex является только техническим dispatcher.

Codex:

- читает body БЛОКА 1;
- читает REQUIRED-документы только в объёме,
  необходимом для качественной сборки полного payload;
- содержательную TASK вместо Ultra не выполняет;
- полностью пересобирает
  `Documentation_DayZ_Qwest/Ultra/03_ULTRA_TASK.md`;
- старое содержимое `03` не использует;
- включает в payload:
  - полную задачу;
  - подтверждаемое исходное состояние;
  - обязательные правила;
  - ограничения;
  - входные данные;
  - критерии результата;
  - необходимые пути;
  - достаточное содержимое REQUIRED-документов;
- проверяет, что `03_ULTRA_TASK.md`
  существует и непустой;
- вызывает `gigachat_file_task`.

Канонические аргументы:

task_file:
`P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\03_ULTRA_TASK.md`

report_file:
`P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\04_ULTRA_REPORT.md`

После возврата инструмента Codex:

- ULTRA REPORT не открывает;
- ULTRA REPORT не читает;
- строки внутри `04` не ищет;
- выводы Ultra не анализирует;
- предполагаемые правки Ultra не применяет;
- TASK вместо Ultra не продолжает;
- пользователю сообщает только технический статус запуска;
- после этого останавливается.

Текущий bridge предоставляет Ultra текстовый вход
и принимает текстовый ответ.

Ultra может вернуть:
- анализ;
- готовый текст;
- инструкции;
- replacement-блоки;
- diff;
- patch.

Но без файловых инструментов bridge
это НЕ означает физического изменения repository.

Независимую проверку ULTRA REPORT выполняет
ChatGPT / пользовательский review-цикл.

================================================================================
4. ЗАФИКСИРОВАТЬ РЕЖИМ EXECUTOR: CODEX + ULTRA
================================================================================

При:

`EXECUTOR: CODEX + ULTRA`

Codex остаётся основным исполнителем.

Codex:

- читает БЛОК 1 и REQUIRED-контекст;
- определяет точную отдельную подзадачу для Ultra;
- полностью формирует новый непустой `03_ULTRA_TASK.md`;
- вызывает ту же каноническую пару `03 + 04`;
- после успешного вызова ОБЯЗАН открыть и прочитать `04`;
- сопоставляет ответ Ultra с тем,
  что реально было отправлено в `03`;
- проверяет полноту;
- проверяет соответствие подзадаче;
- критически проверяет выводы;
- только после этой проверки
  использует результат Ultra;
- при необходимости применяет результат
  в разрешённом scope основной TASK.

Независимый внешний review ChatGPT
при этом всё равно остаётся допустимым
и не заменяется внутренней проверкой Codex.

================================================================================
5. АВТОМАТИЧЕСКУЮ ДЕЛЕГАЦИЮ ПОКА НЕ ВКЛЮЧАТЬ
================================================================================

На этом этапе:

`AUTO-DELEGATION: OFF`

Codex НЕ решает самостоятельно,
что задачу лучше отдать Ultra.

Исполнителя задаёт ChatGPT / пользователь
в dispatch-header.

Будущий режим AUTO
можно оставить как отдельную будущую идею,
но сейчас его не реализовывать.

================================================================================
6. РАЗДЕЛИТЬ ПОЛЬЗОВАТЕЛЬСКИЙ ИСТОЧНИК И ТЕХНИЧЕСКИЙ PAYLOAD
================================================================================

Обновить постоянные правила так, чтобы:

- пользовательская CODEX TASK
  живёт в БЛОКЕ 1;

- пользовательская ULTRA TASK
  тоже живёт в том же БЛОКЕ 1;

- `Ultra/03_ULTRA_TASK.md`
  хранит только одноразовый технический payload
  конкретного запуска,
  сформированный Codex.

Не удалять `03_ULTRA_TASK.md`.

Пользователь вручную его не ведёт.

Между запусками файл может оставаться пустым
или содержать старый технический payload,
но старое содержимое НИКОГДА
не считается автоматически актуальной задачей.

Перед каждым новым вызовом Ultra
Codex обязан полностью заменить `03`
новым payload текущего запуска
и проверить его непустоту.

Не создавать второй пользовательский TASK shell.

Не создавать второй пользовательский БЛОК 1.

================================================================================
7. ОБНОВИТЬ ПОСТОЯННЫЕ ДОКУМЕНТЫ ULTRA
================================================================================

`Ultra/00_Главная.md`

Закрепить:

- `03` — launch-payload текущего запуска;
- его формирует Codex;
- пользователь вручную `03` не поддерживает;
- `04` — результат конкретного вызова Ultra.

`Ultra/01_Старт_Ultra.md`

Закрепить:

- канонический project root:
  `P:\Silver_77_Quests`;

- текущий launch-payload
  берётся из `03_ULTRA_TASK.md`;

- `03` полностью формируется Codex
  перед конкретным вызовом;

- старое содержимое предыдущего запуска
  не является текущей задачей;

- Ultra получает через bridge только текст payload;

- если для предметной задачи нужны сведения
  из боевых документов,
  Codex должен включить необходимый контекст
  в payload;

- нельзя предполагать,
  что Ultra сама открыла файлы repository,
  если bridge не дал ей файловых инструментов.

`Ultra/02_Правила_Ultra.md`

Закрепить:

- Ultra выполняет только launch-payload
  текущего вызова из `03`;

- результат возвращает через
  `04_ULTRA_REPORT.md`;

- не помещать сюда dispatcher-логику
  о том, будет ли Codex читать `04`;

- Ultra не пишет AGENT REPORT в БЛОК 2;

- Ultra не чистит БЛОК 1;

- Ultra не пишет TASK_HISTORY автоматически;

- Ultra не меняет БЛОК 3;

- после формирования ULTRA REPORT
  выполнение Ultra заканчивается;

- независимый review прямого режима ULTRA
  выполняет ChatGPT / пользовательский цикл,
  а не Codex dispatcher.

================================================================================
8. ОБНОВИТЬ ОБЩИЙ WORKFLOW
================================================================================

Синхронизировать минимум:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`
- `Documentation_DayZ_Qwest/Ultra/00_Главная.md`
- `Documentation_DayZ_Qwest/Ultra/01_Старт_Ultra.md`
- `Documentation_DayZ_Qwest/Ultra/02_Правила_Ultra.md`

Каноническим владельцем
трёх режимов EXECUTOR
и логики чтения `04`
является:

`Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`

В:

`Documentation_DayZ_Qwest/12_Старт_агента.md`

оставить только короткий указатель
на канонический routing.

В Ultra-документах
оставить только правила,
необходимые самой Ultra.

Не копировать большие одинаковые алгоритмы
во все документы.

Выбрать канонических владельцев правил,
а в остальных документах оставить
короткие ссылки и инварианты.

================================================================================
9. ИСПРАВИТЬ LIFECYCLE / ALLOWLIST НЕОДНОЗНАЧНОСТЬ TASK 181
================================================================================

Для обычной успешной задачи с:

`EXECUTOR: CODEX`

стандартные lifecycle-изменения
должны иметь узкое постоянное служебное разрешение
и НЕ требовать перечисления
в functional allowlist каждой TASK.

Стандартные разрешённые lifecycle-операции:

1. заменить БЛОК 2 новым AGENT REPORT;
2. добавить ровно одну запись текущей TASK в TASK_HISTORY;
3. штатно очистить завершённый БЛОК 1.

Это служебное разрешение
НЕ расширяет functional scope задачи.

Пример:

functional allowlist содержит только:

`migration.js`

Это разрешает менять:

- `migration.js`;

плюс только три стандартные lifecycle-операции.

Это НЕ разрешает менять:

- соседний JS;
- JSON;
- Map;
- runtime;
- Quest;
- Zones;
- Medicine;
- другие функциональные файлы.

БЛОК 3 не изменяется
без отдельного прямого разрешения.

TASK может прямо отключить стандартный lifecycle,
если режим или сама задача этого требуют.

`ANALYSIS ONLY`
автоматически lifecycle не запускает.

================================================================================
10. LIFECYCLE ДЛЯ EXECUTOR: ULTRA
================================================================================

Для:

`EXECUTOR: ULTRA`

стандартный Codex lifecycle
автоматически НЕ выполняется.

Не менять:

- БЛОК 2;
- TASK_HISTORY;
- БЛОК 1;
- БЛОК 3.

Результат прямого запуска Ultra:

`Ultra/04_ULTRA_REPORT.md`

После независимого review
закрытие / очистка текущей Ultra TASK
выполняется отдельным явным следующим действием.

================================================================================
11. УТОЧНИТЬ REVIEW РЕЗУЛЬТАТА ULTRA
================================================================================

Если Ultra выполняла ANALYSIS ONLY
и repository физически не меняла:

ChatGPT проверяет:
- `03_ULTRA_TASK.md`;
- `04_ULTRA_REPORT.md`;
- необходимые источники;
- соответствие отчёта исходной задаче.

Commit для самого анализа Ultra
не требуется.

Если после отчёта Ultra
результат физически применяется в repository:

- изменения применяет отдельный исполнитель,
  которому это разрешено;
- пользователь делает commit/push;
- ChatGPT смотрит актуальный HEAD `main`;
- сообщает commit message и полный SHA;
- без ожидания дополнительного подтверждения
  проверяет exact GitHub diff и изменённые файлы.

В прямом режиме:

`EXECUTOR: ULTRA`

Codex dispatcher
не должен читать ULTRA REPORT ради review.

В режиме:

`EXECUTOR: CODEX + ULTRA`

Codex обязан прочитать и проверить отчёт
до использования результата.

================================================================================
РАЗРЕШЁННЫЙ FUNCTIONAL SCOPE TASK 182
================================================================================

TASK 182 проектирует исправление для:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`
- `Documentation_DayZ_Qwest/Ultra/00_Главная.md`
- `Documentation_DayZ_Qwest/Ultra/01_Старт_Ultra.md`
- `Documentation_DayZ_Qwest/Ultra/02_Правила_Ultra.md`

Другие файлы
без доказанной необходимости
не включать в исправление.

ВАЖНО ДЛЯ ТЕКУЩЕГО EXECUTOR: ULTRA:

Ultra НЕ изменяет эти файлы физически через текущий bridge.

Ultra должна подготовить точный план применения,
replacement-блоки и/или patch
только для этого разрешённого набора.

================================================================================
ЗАПРЕЩЕНО
================================================================================

В рамках текущего прямого запуска Ultra запрещено:

- физически менять БЛОК 2;
- физически менять БЛОК 3;
- очищать БЛОК 1;
- писать TASK_HISTORY;
- менять `41_ANALYSIS_REPORT_TASK 180.md`;
- менять предметные документы 14–40;
- менять Quest / Migration / Map / Zones / Medicine документы;
- менять код;
- менять JSON;
- менять runtime;
- менять PBO/build output;
- менять `.obsidian`;
- удалять или переименовывать документы;
- создавать второй пользовательский источник текущей TASK;
- включать AUTO-DELEGATION;
- выполнять Git write-команды;
- утверждать, что repository физически изменён,
  если Ultra получила только текстовый bridge.

Read-only Git
использовать только на стороне Codex
до формирования payload,
если действительно нужен
для проверки baseline или текущего состояния
и это не изменяет repository.

================================================================================
КОДИРОВКА
================================================================================

Полный:

`Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`

обязателен для этой задачи.

Не считать краткие правила из START достаточными.

При подготовке предлагаемого patch/replacement
Ultra должна соблюдать правила:

- корректная кодировка;
- сохранение кириллицы;
- отсутствие mojibake;
- отсутствие `�`;
- отсутствие массового бессмысленного пересохранения;
- минимальные изменения;
- новые или нормализованные Markdown —
  UTF-8 без BOM.

Если в полученном контексте обнаружен
mojibake / `�` / повторяющиеся `?`,
не угадывать повреждённый текст.

Отразить проблему в отчёте
и потребовать проверку исходного файла
перед применением патча.

================================================================================
ПРОВЕРИТЬ ПРЕДЛАГАЕМОЕ ИСПРАВЛЕНИЕ ПОСЛЕ ПРОЕКТИРОВАНИЯ
================================================================================

СЦЕНАРИЙ A — CODEX

БЛОК 1 начинается с:

`EXECUTOR: CODEX`

Ожидается:

- Codex читает dispatch-header;
- затем читает body;
- выполняет TASK;
- functional allowlist остаётся жёстким;
- стандартный lifecycle имеет
  только узкое служебное разрешение.

--------------------------------------------------

СЦЕНАРИЙ B — ULTRA

БЛОК 1 начинается с:

`EXECUTOR: ULTRA`

Ожидается:

- Codex читает body и REQUIRED-документы
  только для сборки полного payload;

- Codex саму содержательную TASK не выполняет;

- Codex полностью пересобирает
  и проверяет непустоту `03_ULTRA_TASK.md`;

- Codex вызывает `gigachat_file_task`
  только с парой
  `03_ULTRA_TASK.md + 04_ULTRA_REPORT.md`;

- весь необходимый контекст
  передан Ultra внутри `03`;

- Ultra НЕ должна рассчитывать
  на самостоятельное чтение repository;

- Ultra выполняет TASK
  на основе переданного payload;

- Ultra возвращает
  `04_ULTRA_REPORT.md`;

- Codex report не читает;

- БЛОК 1 остаётся неизменным;

- БЛОК 2 / БЛОК 3 / TASK_HISTORY
  остаются неизменными.

--------------------------------------------------

СЦЕНАРИЙ C — CODEX + ULTRA

БЛОК 1 начинается с:

`EXECUTOR: CODEX + ULTRA`

Ожидается:

- Codex остаётся основным исполнителем;

- формирует отдельную подзадачу
  в новом непустом `03`;

- вызывает каноническую пару `03 + 04`;

- после успешного вызова
  читает и критически проверяет `04`;

- только после проверки
  использует результат Ultra.

--------------------------------------------------

СЦЕНАРИЙ D — ПОВРЕЖДЁННЫЙ HEADER

`EXECUTOR` отсутствует
или имеет неизвестное значение.

Ожидается:

- исполнитель не угадывается;
- выполнение не начинается;
- dispatch не начинается;
- запрашивается исправление header.

--------------------------------------------------

СЦЕНАРИЙ E — LIFECYCLE

Functional allowlist:

`migration.js`

Executor:

`CODEX`

Проверить, что разрешены:

- `migration.js`;
- только три стандартные lifecycle-операции.

И НЕ разрешены
никакие другие функциональные файлы.

--------------------------------------------------

СЦЕНАРИЙ F — ANALYSIS ONLY

Проверить,
что стандартный lifecycle
не запускается автоматически.

--------------------------------------------------

ДОПОЛНИТЕЛЬНО ПРОВЕРИТЬ

- активные документы
  не называют `03_ULTRA_TASK.md`
  вторым пользовательским источником TASK;

- активные документы
  не называют `03`
  неиспользуемой compatibility-note;

- при `EXECUTOR: ULTRA`
  Codex не читает `04`;

- при `EXECUTOR: CODEX + ULTRA`
  Codex обязательно читает
  и проверяет `04`;

- каноническая bridge-пара везде одна:
  `03_ULTRA_TASK.md + 04_ULTRA_REPORT.md`;

- нет двух пользовательских владельцев
  текущей TASK;

- обычные Markdown/wiki-links
  не запускают рекурсивное чтение
  всего связанного графа;

- Markdown будущих правок корректен;

- UTF-8 без BOM
  соблюдается там,
  где документ нормализуется;

- предлагаемый patch
  не содержит случайных изменений
  БЛОКОВ 2/3;

- предлагаемый patch
  не выходит за functional allowlist.

================================================================================
ULTRA REPORT
================================================================================

Весь итоговый результат
вернуть через:

`P:\Silver_77_Quests\Documentation_DayZ_Qwest\Ultra\04_ULTRA_REPORT.md`

Формат:

ULTRA REPORT

TASK:
TASK 182

STATUS:
COMPLETED / BLOCKED

READ:
- какие обязательные документы
  были переданы Codex в payload;
- какие дополнительные зависимости
  понадобились и почему.

CONFIRMED BEFORE FIX:
- какие реальные противоречия
  подтверждены по переданному контексту
  до проектирования исправления.

PROPOSED CHANGES:
- точный список файлов;
- что именно предлагается изменить
  в каждом файле.

PATCH / REPLACEMENT:
- готовый diff / patch
  и/или точные replacement-блоки,
  достаточные для последующего применения.

WORKFLOW CONTRACT:
- итоговая схема CODEX;
- итоговая схема ULTRA;
- итоговая схема CODEX + ULTRA;
- где единственный пользовательский источник TASK;
- где технический Ultra payload;
- где Ultra report;
- кто читает `04` в каждом режиме.

LIFECYCLE FIX:
- как устранить неоднозначность
  functional allowlist / lifecycle;
- почему служебное разрешение
  не расширяет functional scope.

SCENARIO CHECKS:
- A — CODEX;
- B — ULTRA;
- C — CODEX + ULTRA;
- D — invalid header;
- E — lifecycle;
- F — ANALYSIS ONLY.

ENCODING CHECK:
- требования UTF-8;
- BOM;
- кириллица;
- mojibake;
- ограничения безопасного применения.

PROBLEMS:
- только реальные проблемы
  или незакрытые противоречия.

CONCLUSION:
- краткий итог;
- можно ли применять подготовленный patch;
- какие проверки обязательно выполнить
  после физического применения.

================================================================================
ПОСЛЕ ЗАПИСИ ULTRA REPORT
================================================================================

Ultra:

- НЕ очищает БЛОК 1;
- НЕ пишет БЛОК 2;
- НЕ пишет БЛОК 3;
- НЕ пишет TASK_HISTORY;
- НЕ выполняет Git write-команды;
- НЕ утверждает,
  что физически изменила repository;
- останавливается.

Codex dispatcher после успешного прямого запуска:

- НЕ открывает `04_ULTRA_REPORT.md`;
- НЕ читает `04_ULTRA_REPORT.md`;
- НЕ анализирует результат;
- НЕ применяет patch;
- возвращает только технический статус вызова;
- останавливается.

Дальше результат отдельно проверяет
пользователь / ChatGPT.

Моды обновлять не нужно.
================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK: TASK 181 — Оптимизация контекста: разделить роли и сократить обязательное чтение.

## STATUS

COMPLETED / DOCUMENTATION ARCHITECTURE.

## BASELINE

- Канонический проект: `P:\Silver_77_Quests`.
- Локальный `main`: commit message `93`, SHA `2a36675cea2474751b32ded5ee24015f8c269415`.
- Полный `ANALYSIS REPORT — TASK 180` прочитан до начала реализации и сопоставлен с БЛОКОМ 1 и текущими документами.
- Git write-команды не выполнялись; commit/push выполняет пользователь.

## CREATED FILES

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_PREPARATION_RULES.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/CONTEXT_ROUTING.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_LIFECYCLE_RULES.md`
- `Documentation_DayZ_Qwest/21_Project_Docs/PROJECT_PATHS.md`

## CHANGED FILES

- `Documentation_DayZ_Qwest/10_Правила_агента.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/00_INDEX.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md` — одна запись TASK 181
- `Documentation_DayZ_Qwest/21_Project_Docs/00_INDEX.md`

## MOVED RULES

- Подготовка TASK, slices, baseline, scope, два блока, уточнения и ориентиры размера перенесены из `12` в `TASK_PREPARATION_RULES.md`.
- Тематическая матрица удалена из `11`; выбор контекста по роли, стадии и операции перенесён в `CONTEXT_ROUTING.md`.
- AGENT REPORT, TASK_HISTORY, BLOCKED, очистка БЛОКА 1 и правило об обновлении модов перенесены в `TASK_LIFECYCLE_RULES.md`.
- Рабочие roots и граница source/packed output перенесены из обязательного ядра в `PROJECT_PATHS.md`.
- Подробный REVIEW остался у существующего канонического владельца `GIT_REVIEW_RULES.md`.

## REMOVED DUPLICATION

- `12_Старт_агента.md` сокращён до обязательного agent-core; подготовка, lifecycle, проектная карта и подробный review больше не читаются исполнителем автоматически.
- `11_Задача_агенту.md` оставлен shell-документом TASK / REPORT / REVIEW без тематической матрицы.
- `30_Единая_система_проекта.md`, БЛОКИ 2/3, TASK_HISTORY и аварийный encoding-протокол исключены из постоянного старта.
- Штатное lifecycle-обновление больше не объявляет каждую code/FIX-задачу полноценной Documentation TASK.
- Тематические документы сохранены цельными владельцами предметных правил; их технические контракты в маршрутизатор не копировались.

## CONTEXT BEFORE / AFTER

- A. Обязательный старт обычного агента, без исходников и БЛОКА 1: `35 415 → 4 926` символов; сокращение `86,1%`.
- B. Маленький Migration/Editor FIX до тематического кода, включая узкий `QUEST_EDITOR_RULES` и lifecycle: `46 584 → 12 994` символа; сокращение `72,1%`.
- Для B исходный маршрут включал старую оболочку 11, полный 12, обязательный 30, `QUEST_EDITOR_RULES` и полный `ENCODING_RULES`; новый ASCII-only маршрут включает короткий 12, `QUEST_EDITOR_RULES` и `TASK_LIFECYCLE_RULES`.

## CHECKS

- Все новые документы подключены к индексам, wiki-links разрешаются.
- `12_Старт_агента.md`: 4 926 символов, целевой диапазон 3–5 тыс. соблюдён.
- Активные управляющие документы не используют `AGENT_TASK_LOOP.md` или неоднозначный `START.md` как текущие источники.
- `30_Единая_система_проекта.md` назначается только крупным межмодульным/архитектурным задачам или прямым указанием.
- БЛОК 1 очищен по новой lifecycle-схеме; БЛОК 2 обновлён; БЛОК 3 не изменён.
- `git diff --check` пройден; присутствуют только предупреждения Git о будущей нормализации LF/CRLF, ошибок whitespace нет.

## ENCODING CHECK

PASS.

- Все новые и изменённые Markdown-файлы строго декодируются как UTF-8 без BOM.
- NUL отсутствует; кириллица читается нормально.
- Примеры mojibake в правилах кодировки сохранены намеренно как диагностические маркеры, а не как повреждение текста.

## PROBLEMS

- До TASK 181 в working tree уже находились нормализация анализа TASK 180/ветки Ultra и внешнее изменение `.obsidian/graph.json`. Они сохранены и не включались в реализацию TASK 181.

## CONCLUSION

TASK 181 завершена. Постоянный контекст разделён по роли, стадии и предметной области без удаления тематических контрактов и safety-core. Код, JSON, runtime, PBO и документы 14–40 не менялись. Моды обновлять не нужно.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
