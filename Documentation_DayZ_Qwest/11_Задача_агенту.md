# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `DayZ .c / Enforce Script / runtime compile` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_ENFORCE_SCRIPT_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`

## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки под заголовком `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не должно оставаться содержимого завершённой задачи.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 151 — Многоступенчатый stuck recovery: проверка результата, reverse escape и единый vanilla-busy guard`.

STATUS:
- `IMPLEMENTED / STATIC CHECK PASSED / WAITING FOR RUNTIME`.
- Git write-команды не выполнялись.

RUNTIME SNAPSHOT:
- `$profile:Silver_77_Migrate/Migration.log` прочитан read-only через фактический server profile.
- Во время snapshot предыдущая сессия уже завершилась и появилась новая; сервер агент не перезапускал.
- Current session на срезе `timeMs=808512`: `SPAWNED=20`, `DEAD=0`, `LIVE/TRACKED=20`, `MISSING WITHOUT DEATH=0`.
- Current session ещё не имела `HOLD_ENTER`, поэтому `MAX HOLD DISTANCE=NOT APPLICABLE`; `HOLD_OUTSIDE=0`, `HOLD_RETURN_WAIT_BUSY=0`, `HOLD_RETURN_START=0`, `HOLD_RETURNED=0`.
- В новой сессии массового исчезновения после TASK 150 не зафиксировано: tracked `20/20`.
- Для завершившейся непосредственно перед ней сессии: `SPAWNED=20`, `DEAD=8`, последний полный batch `LIVE/TRACKED=12`, `MISSING WITHOUT DEATH=0`.
- Предыдущая max logged hold-distance: `201.537 m`, `MIGRATION_TEST_001_RUN_1 / INF_7`; события: `HOLD_OUTSIDE=2`, `HOLD_RETURN_START=2`, `HOLD_RETURNED=1`, `HOLD_RETURN_WAIT_BUSY=0`.

CURRENT RECOVERY CONFIRMATION:
- Существующая цепочка `STUCK_DETECTED / ROUTE_PROGRESS_LOST -> ReleaseRouteControl -> directed stimulus -> stuckRecoveryFreeSeconds -> vanilla idle -> cooldown` сохранена как первый уровень.
- `STUCK_RECOVERY_STIMULUS_OK` по-прежнему означает только успешное создание stimulus, а не физический выход из препятствия.

VANILLA BUSY GUARD:
- Добавлен единый `IsVanillaBusy(controller)`.
- Controller отсутствует, target существует либо mind state не `CALM` -> manager control запрещён.
- Guard используется перед resume после vanilla handoff, recovery и reverse, перед reverse override, перед `RETURN_TO_HOLD` и непосредственно перед route override.
- При busy overrides снимаются/не включаются; natural target, chase и fight имеют приоритет.
- Исправлено сохранение `RETURN_TO_HOLD` intent: повторные busy ticks в `AGGRO` больше не перезаписывают его на `MIGRATION`.

RECOVERY MOVEMENT CHECK:
- В начале incident сохраняются world position, горизонтальный forward к фактическому control/logical target и resume mode.
- После первого free-period, vanilla idle и `AGGRO_COOLDOWN_MS` вычисляется реальный `movedXZ` от recovery start.
- `movedXZ >= stuckMinMovementMeters` -> обычный `STUCK_RECOVERY_RESUME`.
- `NO_MOVEMENT` при `stuckReverseEnabled=1` -> второй уровень; при `0` reverse полностью пропускается.

REVERSE ESCAPE:
- Один incident использует локальную reverse-цель `currentPosition + (-savedForward) * stuckReverseDistanceMeters`.
- Permanent `routePoints`, `migrationTarget` и logical target не изменяются.
- Используются только `OverrideHeading` и `OverrideMovementSpeed`; teleport / `SetPosition` отсутствуют.
- Попытка завершается при смещении не меньше `stuckMinMovementMeters` либо по `stuckReverseMaxSeconds` с результатом `MOVED` / `TIMEOUT`.
- Aggro во время reverse немедленно снимает overrides; context сохраняется, повторный старт ждёт retry, `CALM` и cooldown.

POST-REVERSE FREE:
- После любого `STUCK_REVERSE_END` overrides снимаются и включается `stuckPostReverseFreeSeconds` vanilla freedom.
- После free-period снова обязательны vanilla idle и `AGGRO_COOLDOWN_MS`.
- Затем выполняется `BuildPath()` к исходной logical target и возврат в сохранённый `MIGRATION` либо `RETURN_TO_HOLD`.

CONFIG:
- Добавлены scenario-level поля:
  - `stuckReverseEnabled = 1`;
  - `stuckReverseDistanceMeters = 10.0`;
  - `stuckReverseMaxSeconds = 5.0`;
  - `stuckReverseRetrySeconds = 10.0`;
  - `stuckPostReverseFreeSeconds = 10.0`.
- Оба source scenario обновлены; неположительные float values нормализуются к defaults, enabled приводится к `0/1`.
- Runtime profile не читался для изменения и не переписывался; missing fields получают constructor/Normalize defaults только в памяти.

LOGGING:
- Добавлены event-only markers без tick-spam: `STUCK_RECOVERY_MOVEMENT_CHECK`, `STUCK_REVERSE_WAIT_BUSY`, `STUCK_REVERSE_START`, `STUCK_REVERSE_END`, `STUCK_POST_REVERSE_FREE`, `STUCK_REVERSE_RESUME`.
- Длинные строки строятся пошагово по `DAYZ_ENFORCE_SCRIPT_RULES.md`.

CHANGED FILES:
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateConfig.c`
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateManager.c`
- `Silver_77_Migrate_server/config/MigrationConfig.json`
- `Silver_77_Migrate_server/config/README_RU.md`
- `Documentation_DayZ_Qwest/14_Квест_Миграция.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md` — report и очистка БЛОКА 1.

STATIC CHECKS:
- JSON parse: PASS; оба scenario содержат `1 / 10 / 5 / 10 / 10`.
- Constructor/Normalize defaults: PASS.
- `stuckReverseEnabled=0` bypass: PASS.
- Successful first recovery bypasses reverse: PASS.
- `NO_MOVEMENT` enters `REVERSE_WAIT`: PASS.
- Busy guard precedes reverse/route control: PASS.
- Post-reverse free и rebuild к исходному intent: PASS.
- Новых assignments permanent route/target: `0`.
- Teleport / `SetPosition`: `0`.
- Direct `GetTargetEntity/GetMindState`: только единый guard и read-only state logging.
- Enforce added code lines over 160 chars: `0`; длинных новых single-expression formulas нет.
- Скобки: config `56/56`, manager `182/182`; круглые скобки также сбалансированы.
- `git diff --check`: PASS.

ENCODING CHECK:
- Все изменённые `.c`, `.json`, `.md` прошли strict UTF-8 decode.
- BOM отсутствует, кириллица читается нормально.

PBO / RUNTIME:
- Для acceptance требуется обновить только server-only PBO `Silver_77_Migrate_server`.
- PBO не собирался, сервер не запускался и не перезапускался, runtime profile не изменялся.

PROBLEMS:
- Compile/runtime проверка невозможна без пользовательской пересборки и запуска; результат ожидает runtime acceptance.
- Известный generic missing-entity log spam не исправлялся: он исключён из scope TASK 151.

BLOCK 1 CLEANUP CHECK: PASS
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
