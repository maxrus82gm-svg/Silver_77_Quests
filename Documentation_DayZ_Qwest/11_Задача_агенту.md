# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки `БЛОК 1` должен выглядеть так:

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.

================================================================================
# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 132 — Две одновременные migration-группы + глобальная погода + нарастающая гроза`.

RULES PATH:
- `P:\Silver_77_Quests\Documentation_DayZ_Qwest`

STATUS:
- Реализация завершена и статически проверена.
- PBO не собирался, сервер и runtime profile не запускались.

CHANGED FILES:
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateConfig.c`
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateManager.c`
- `Silver_77_Migrate_server/config/MigrationEvent.json` — создан.
- `Silver_77_Migrate_server/config/MigrationScenario_001.json`
- `Silver_77_Migrate_server/config/MigrationScenario_002.json` — создан.
- `Silver_77_Migrate_server/config/README_RU.md` — создан.
- `Documentation_DayZ_Qwest/15_Сценарий_Миграции_001.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md` — отчёт обновлён, `БЛОК 1` очищен.

CONFIG ARCHITECTURE:
- `MigrationEvent.json` является единственным активным источником общей задержки и погоды.
- `MigrationScenario_001.json` хранит только параметры `MIGRATION_TEST_001`.
- `MigrationScenario_002.json` хранит только параметры `MIGRATION_TEST_002`.
- Source Scenario 001/002 не содержат weather fields и `spawnDelaySeconds`.

SCENARIO 002:
- spawn: `<12874.942383, 130.418289, 13074.552734>`.
- target: `<13091.167969, 121.727158, 13051.032227>`.
- Переданные orientation маркеров не используются для navmesh target; фиксированная infected orientation не менялась.

EVENT / TWO GROUPS:
- Через общий `eventDelaySeconds = 30` начинается один weather transition длительностью `180` секунд.
- В конце transition `StartAllScenarios()` последовательно вызывает оба включённых scenario в рамках одного общего момента без искусственной задержки.
- Обе группы хранят states в общей коллекции, но каждый state имеет собственные `scenarioId`, infected ID, migration target и waypoints.
- Внутри каждого вызова `StartScenario(config)` независимо выбираются spawn/target rotations и рассчитываются offsets/jitter по `infectedCount` именно этой группы.
- Все существенные group-логи содержат `scenario=MIGRATION_TEST_001` или `scenario=MIGRATION_TEST_002`; общая погода использует `[S77Migrate][EVENT]`.
- `logIntervalSeconds` поддерживается отдельно для каждого infected через собственные next-log timestamps.

STORM RAMP / WEATHER:
- Vanilla `SetStorm(density, threshold, timeout)` не имеет transition-параметра.
- Storm ramp начинается на 150-й секунде от старта manager, то есть за последние `60` секунд transition перед spawn на 210-й секунде.
- Используется server-side шаг `10` секунд; density постепенно растёт от `0` до default `0.8`, финальное значение принудительно устанавливается перед spawn.
- При `weatherStormEnabled = 0` density остаётся `0`, callbacks ramp не запускаются.
- Rain задаётся отдельно через `GetRain().Set(...)`; default остаётся `0.0`.
- JSON хранит wind `0.0`, runtime применяет подтверждённый vanilla minimum `0.1 m/s`.
- Сохраняются прежний `MissionWeather`, отсутствие резкого forecast reset и удаление всех event/storm callbacks в `Stop()`.

ADD-ONLY / LEGACY:
- Existing `MigrationEvent.json` получает только отсутствующие top-level fields; значения и порядок существующих полей не пересобираются.
- Повторная проверка без missing fields не пишет файл; malformed Event JSON остаётся нетронутым.
- Новый Event и новый Scenario 002 при отсутствии создаются с полными defaults и валидируются.
- Existing Scenario 001/002 не перезаписываются defaults; malformed scenario не уничтожается.
- Старые weather fields и `spawnDelaySeconds` runtime Scenario 001 автоматически не удаляются и не меняются; они загружаются только как legacy-compatible поля и manager их игнорирует.

README:
- Создан `config/README_RU.md` с русским описанием каждого актуального параметра Event и обоих Scenario, единицами измерения, примерами и пояснением legacy-полей.

VANILLA API:
- По `P:\scripts\3_game\weather.c` подтверждены `WeatherPhenomenon.Set(...)`, `MissionWeather(...)`, `GetMissionWeather()`, `SetStorm(...)` и minimum `0.1 m/s` для `SetWindSpeed(...)`.

VERIFICATION:
- Все три source JSON валидны; Event defaults, обе schema и координаты Scenario 002 проверены.
- Source Scenario 001/002 не содержат active weather/spawnDelay fields.
- Timeline статически подтверждён: weather start `30`, storm ramp start `150`, spawn `210` seconds; ramp step `10` seconds.
- Обе группы запускаются одним `StartAllScenarios()`; formation rotations и target state независимы.
- Add-only markers, malformed-before-write guard и idempotent early return сохранены.
- Скобки и круглые/квадратные скобки Enforce Script сбалансированы.
- Все изменённые task-файлы читаются как строгий UTF-8 без BOM; кириллица отображается нормально.
- `git diff --check` завершён без ошибок.
- Client code, `cfgweather.xml`, time/date и другие моды не менялись.
- Git write-команды не выполнялись.

UNEXPECTED EXTERNAL CHANGE:
- Во время работы Obsidian автоматически изменил `.obsidian/workspace.json`, добавив новые config-файлы в UI state. Агент этот файл не редактировал, не откатывал и не включает его в изменения TASK 132.

BUILD:
- Для runtime-проверки нужен новый PBO только `Silver_77_Migrate_server`.
- `Silver_77_Quests_Client` и `Silver_77_Quests_Server` обновлять не нужно.
>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>


================================================================================
# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
