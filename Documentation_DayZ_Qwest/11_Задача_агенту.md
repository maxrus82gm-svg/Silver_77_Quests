# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `DayZ .c / Enforce Script / runtime compile` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_ENFORCE_SCRIPT_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`

## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки под заголовком `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не должно оставаться содержимого завершённой задачи.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 148 — FIX Formula too complex в S77MigrateManager.c после TASK 146 logging`.

STATUS:
- `FIXED / WAITING FOR RUNTIME COMPILE`.
- Git write-команды не выполнялись.

ROOT CAUSE:
- После TASK 146 в manager оставались длинные single-expression string concatenations с большим количеством literals, member access и `.ToString()`.
- Enforce Script превысил complexity limit одной formula и сообщил ошибку на следующей строке `vector sampledStart;`, а не на фактически проблемном logging expression.

FIRST CONFIRMED FORMULA:
- Подтверждённый `LogError(... "ERROR: AIWorld is unavailable")` в `BuildPath()` разбит на `string logMessage710`, последовательные простые additions и финальный `LogError(logMessage710)`.
- Строки `vector sampledStart` / `vector sampledTarget` и сама path-логика не менялись.

LOG EXPRESSIONS REFACTORED:
- Весь `S77MigrateManager.c` просмотрен, а 51 direct logging expression переведён на пошаговые локальные string builders.
- Аналогично упрощены сложный return в `BuildLogLine()` и начальные expressions существующих `weatherLog`, `formationLog`, `routeProgressLog`, activation/stimulus/logLine builders.
- Обработаны SESSION, weather, spawn, path errors/build, aggro, stuck samples, detection/recovery/stimulus results, route/final activation, hold/return, release/death и periodic state logs.
- В `LogInfo`, `LogError` и `LogStuckDebug` не осталось вызовов с двумя и более chained `+`; максимум по одной физической строке manager теперь равен двум `+` только в коротких вспомогательных expressions вне прямых logging-вызовов.

LOGGING SEMANTICS PRESERVED:
- Последовательность всех 411 string literals до и после refactor совпадает; marker names и порядок фрагментов сохранены.
- Количество вызовов `LogInfo`, `LogError`, `LogStuckDebug`, `OpenFile`, `FPrintln` и `CloseFile` не изменилось.
- `loggingEnabled`, `stuckDebugLoggingEnabled`, RPT output, FileHandle lifecycle и `$profile:Silver_77_Migrate/Migration.log` сохранены.
- TASK 141/142/144 stuck/recovery/watchdog thresholds, stimulus, aggro handoff, final hold и navmesh control не менялись.

TASK 147 PRESERVED:
- `weatherChangeEnabled`, global `weatherEnabled`, `CountWeatherRequestingScenarios()` и batch decision сохранены.
- Marker `WEATHER_DECISION` и число вызовов weather helper не изменились; weather values/config/JSON не редактировались.

ENFORCE SCRIPT RULE ADDED:
- Создан `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_ENFORCE_SCRIPT_RULES.md` после подтверждённого runtime compile error `Formula too complex`.
- Правило закрепляет ограничение formula complexity, пошаговую сборку строк, промежуточные bool/локальные значения, проверку предыдущих строк после неточной позиции compiler error и обязательный complexity-review для любой задачи с DayZ `.c`.
- В матрицу чтения `11_Задача_агенту.md` добавлен маршрут `DayZ .c / Enforce Script / runtime compile` к новому rule-файлу и `ENCODING_RULES.md`.
- Другие моды и DayZ-код вне `S77MigrateManager.c` не рефакторились.

CREATED FILES:
- `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_ENFORCE_SCRIPT_RULES.md`

CHANGED FILES:
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateManager.c`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md`

STATIC CHECKS:
- Direct long logging formulas: `0`.
- String literal sequence: `411/411`, exact ordered match.
- Ключевые migration/weather/logging/FileIO call counts совпадают с baseline.
- Braces `159/159`, parentheses `907/907`, brackets `12/12`.
- Новый Markdown и матрица чтения проверены; ссылка на rule-файл присутствует один раз.
- Все изменённые файлы валидны как UTF-8 без BOM; `git diff --check` не нашёл ошибок.
- `MigrationConfig.json`, runtime profile, PBO и сервер не изменялись/не запускались.

RUNTIME COMPILE CHECKLIST:
1. Пересобрать только `Silver_77_Migrate_server`.
2. Запустить сервер и подтвердить отсутствие `Formula too complex`.
3. Если compiler покажет новую позицию, сохранить точную строку и проверить также несколько предыдущих expressions.
4. После успешного compile проверить создание/append `$profile:Silver_77_Migrate/Migration.log`.
5. Продолжить runtime-проверку stuck/recovery и убедиться, что marker names/события сохранились.

UNEXPECTED CHANGES:
- Во время работы появился diff `.obsidian/workspace.json`; файл находится вне scope, агентом не редактировался и не откатывался.

ENCODING CHECK:
- `S77MigrateManager.c`, новый rule-файл, TASK_HISTORY и рабочий shell читаются как UTF-8 без BOM.
- Кириллица отображается нормально; символов замены, mojibake и текстовых `\n` не добавлено.

BLOCK 1 CLEANUP CHECK: PASS
- Заголовок БЛОКА 1 сохранён ровно один раз.
- TASK 148 удалена из активного БЛОКА 1.
- Оставлены три канонические строки; разделитель и БЛОК 2 сохранены.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
