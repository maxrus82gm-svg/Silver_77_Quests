# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`

## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки под заголовком `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не должно оставаться содержимого завершённой задачи.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 146 — Управляемое логирование Migration и подробная диагностика stuck/recovery в $profile`.

STATUS:
- `IMPLEMENTED / WAITING FOR RUNTIME`.
- Git write-команды не выполнялись.

LOGGING CONFIG:
- В корень `S77MigrateConfig` добавлены глобальные `loggingEnabled` и `stuckDebugLoggingEnabled`; constructor defaults — `1` и `0`, Normalize принимает только `1`, source JSON содержит поля один раз вне `scenarios`.
- Существующий runtime `$profile:Silver_77_Migrate/MigrationConfig.json` не открывался и не переписывался.

PROFILE FILE LOGGING:
- Локальный DayZ API подтверждает `$profile`, `FileMode.APPEND`, `OpenFile`, `FPrintln` и `CloseFile`.
- Manager открывает один server-only handle `$profile:Silver_77_Migrate/Migration.log` при инициализации, пишет append-строки и закрывает handle в `Stop()`.
- `SESSION_START` / `SESSION_END` отделяют штатные сессии; timestamp использует безопасный server-runtime `GetGame().GetTime()` как `timeMs`.

LOGGING HELPERS:
- `LogInfo()` пишет в RPT и profile log только при `loggingEnabled = 1`.
- `LogStuckDebug()` требует одновременно `loggingEnabled = 1` и `stuckDebugLoggingEnabled = 1`.
- `LogError()` всегда пишет в RPT и дополнительно в уже доступный profile log независимо от INFO flag.

STUCK SAMPLE START:
- `SetStuckSample()` создаёт один `STUCK_SAMPLE_START` на фактический sample: scenario/group/id/mode, position A, sample time, configured seconds, threshold и target.

STUCK SAMPLE CHECK:
- После полного existing interval пишется `STUCK_SAMPLE_CHECK` с position A/B, elapsed seconds, `movedXZ`, threshold и `result=MOVING` либо `result=STUCK`.
- Алгоритм TASK 141 не менялся: `GetPosition()` A, ожидание configured `6 sec`, `GetPosition()` B, горизонтальная distance; `< 2 m` — STUCK, `>= 2 m` — MOVING.

SAMPLE RESET DIAGNOSTICS:
- `STUCK_SAMPLE_RESET` пишется только для реально active sample и содержит event reason.
- Добавлены причины `AGGRO`, `BUILD_PATH`, `ROUTE_POINT_CHANGED`, `WAYPOINT_CHANGED`, `BUILD_PATH_RETRY`, `STUCK_RECOVERY`, `HOLD_FREE`, `RETURN_TO_HOLD`, `DEATH` и release; per-tick reset spam отсутствует.

STUCK DETECTED:
- Важный INFO marker содержит scenario/group/id/mode, position A/B, elapsed, `movedXZ`, threshold, relevant target и прежний progressDiagnostic.

STIMULUS ATTEMPT / RESULT:
- Каждый собственный recovery incident пишет `STUCK_RECOVERY_RELEASE`, затем `STUCK_RECOVERY_STIMULUS_ATTEMPT` с cause, current/target/stimulus positions, forward distance, lifetime и strength.
- Bool result фактического `EmitAIStimulus()` сохраняется и даёт `STUCK_RECOVERY_STIMULUS_OK` либо `..._FAILED`; отсутствие usable direction/group даёт `..._SKIPPED` после ATTEMPT.
- `cause=STUCK` и `cause=ROUTE_PROGRESS_LOST` различаются; затем сохраняются `STUCK_RECOVERY_CALM`, WAIT_BUSY при необходимости и `STUCK_RECOVERY_RESUME`.

IMPORTANT LIFECYCLE EVENTS:
- На central helpers переведены event/weather, scenario/group start, spawn, `BUILD_PATH`/`PATH_ERROR`, route point, aggro handoff/resume, activation, hold/return, release/death, periodic unit state и recovery markers.

ERROR BEHAVIOR:
- Manager ERROR остаётся видимым через RPT при `loggingEnabled = 0`; если profile handle открыт, ERROR дублируется в `Migration.log`.
- Ошибки config loader до доступности config/handle остаются прямыми RPT `Print`, поэтому malformed config не скрывается.

PERFORMANCE:
- Movement loop не пишет диагностическую строку каждые `250 ms`.
- Detailed debug создаёт START, один CHECK на завершённый interval и только event-based RESET; отдельные per-infected `CallLater` не добавлены.
- Periodic `LogUnitStates` вообще не планируется при `loggingEnabled = 0`; file handle не открывается/закрывается на каждом событии.

RUNTIME JSON PATCH:
- Верхний уровень существующего runtime JSON:
  - `"loggingEnabled": 1`
  - `"stuckDebugLoggingEnabled": 1`
- В каждом тестируемом scenario оставить:
  - `"stuckDetectionSeconds": 6.0`
  - `"stuckMinMovementMeters": 2.0`
- Текущие TASK 142 watchdog-поля не менять.

CHANGED FILES:
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateManager.c`
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateConfig.c`
- `Silver_77_Migrate_server/config/MigrationConfig.json`
- `Silver_77_Migrate_server/config/README_RU.md`
- `Documentation_DayZ_Qwest/14_Квест_Миграция.md`
- `Documentation_DayZ_Qwest/15_Сценарий_Миграции_001.md`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `Documentation_DayZ_Qwest/11_Задача_агенту.md`

STATIC CHECKS:
- Source JSON валиден; logging fields глобальные и отсутствуют в scenario objects.
- Defaults/Normalize, helper gates, A/B sample fields, reset reasons и bool stimulus result проверены статически.
- TASK 141 `< 2 m / 6 sec`, TASK 142 logical target/thresholds и TASK 144 individual recovery path не изменены.
- FileIO отсутствует в `UpdateRoutes()` и вызывается только logging helpers по событиям/периодическому log schedule.
- Braces/parentheses/brackets, UTF-8 без BOM, Markdown и `git diff --check` проверены.
- Runtime profile, `.obsidian/*`, quest JSON, PBO и сервер не изменялись/не запускались.

RUNTIME CHECKLIST:
1. Вручную применить четыре строки из `RUNTIME JSON PATCH`; watchdog TASK 142 оставить как есть.
2. Запустить migration и подтвердить `Migration.log` плюс те же важные строки в RPT.
3. Для одного `id=` найти START, через ~6 sec CHECK и проверить A/B, elapsed, movedXZ/result.
4. Для STUCK проследить DETECTED → RELEASE → ATTEMPT → ровно один OK/FAILED/SKIPPED → CALM/RESUME либо gated WAIT_BUSY.
5. Повторить incident для того же infected и отдельно с `cause=ROUTE_PROGRESS_LOST`.
6. При debug `0` проверить исчезновение sample markers с сохранением recovery INFO; при logging `0` проверить отсутствие INFO и сохранение ERROR.

RISKS / OPEN QUESTIONS:
- Фактическое создание/append/flush `Migration.log`, порядок строк при crash и визуальная реакция infected требуют runtime-проверки; PBO/сервер в TASK 146 не запускались.
- Rotation/size limit одного append-файла не реализованы и остаются будущей production-задачей.
- Изменения server script требуют пересборки только `Silver_77_Migrate_server` перед runtime-тестом.

UNEXPECTED CHANGES:
- `.obsidian/graph.json` и `.obsidian/workspace.json` уже были изменены до основной работы TASK 146; оба файла вне scope и агентом не редактировались.

ENCODING CHECK:
- Все изменённые текстовые файлы читаются как UTF-8 без BOM; кириллица не повреждена.

BLOCK 1 CLEANUP CHECK: PASS
- Существующий заголовок БЛОКА 1 сохранён в одном экземпляре.
- TASK 146 удалена из активного БЛОКА 1.
- Оставлены три стандартные строки; разделитель и БЛОК 2 сохранены.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
