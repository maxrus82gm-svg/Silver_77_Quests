# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `DayZ .c / Enforce Script / runtime compile` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_ENFORCE_SCRIPT_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`

## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки под заголовком `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не должно оставаться содержимого завершённой задачи.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

TASK 181 — Оптимизация контекста: разделить роли и сократить обязательное чтение

ТИП:
DOCUMENTATION ARCHITECTURE

ЦЕЛЬ:
Реализовать первый основной результат TASK 180: резко уменьшить документационный контекст обычной задачи без потери правил.

После TASK агент не должен при каждом старте читать правила ChatGPT, прошлый AGENT REPORT/REVIEW, проектную карту и большие тематические инструкции, если они не нужны текущей работе.

BASELINE:
GitHub main
Commit message: 92
SHA: 365743c5ef3b772964b7fab61894fd92a16dc2a3

ЧИТАТЬ ПЕРЕД ПРАВКОЙ:
- Documentation_DayZ_Qwest/11_Задача_агенту.md
- Documentation_DayZ_Qwest/12_Старт_агента.md
- Documentation_DayZ_Qwest/30_Единая_система_проекта.md
- Documentation_DayZ_Qwest/10_Правила_агента.md
- Documentation_DayZ_Qwest/20_SplitDoc/00_INDEX.md
- Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md
- Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md

СДЕЛАТЬ:

1. Разделить управляющий контекст по ролям.

Создать:
- `20_SplitDoc/TASK_PREPARATION_RULES.md`
  Только правила для ChatGPT при подготовке TASK: выбор slice, baseline, scope, два блока, крупный план, размер БЛОКА 1 и т.п.

- `20_SplitDoc/CONTEXT_ROUTING.md`
  Каноническая маршрутизация чтения по роли / стадии / операции.
  Различать минимум: TASK PREP, AGENT EXECUTION, REVIEW, DOCUMENTATION, ON DEMAND.
  Обычная ссылка не означает рекурсивное чтение связанных документов.
  Обязательная зависимость должна быть явно обозначена.

- `20_SplitDoc/TASK_LIFECYCLE_RULES.md`
  Только завершение TASK: AGENT REPORT, TASK_HISTORY, BLOCKED, штатная очистка БЛОКА 1, что писать о модах.

- `21_Project_Docs/PROJECT_PATHS.md`
  Актуальные рабочие roots / editor / viewer / source vs packed output.
  Проектные пути не должны находиться в обязательном agent-core.

2. Радикально сократить `12_Старт_агента.md`.

В `12` оставить только короткое обязательное ядро исполнителя:
- выполнять только текущий БЛОК 1;
- scope / allowlist / никаких инициативных соседних правок;
- пользователь контролирует Git;
- TASK после запуска заморожена, уточнения идут отдельным сообщением;
- минимальная защита текста/кодировки;
- при проблеме вне scope остановиться или вынести её в отчёт;
- читать только явно назначенный контекст;
- перед завершением использовать TASK_LIFECYCLE_RULES.

Из `12` перенести, а не копировать:
- правила формирования TASK для ChatGPT;
- compact-task guidance;
- подробные шаблоны;
- проектную карту и packed/source paths;
- подробности REVIEW, уже принадлежащие GIT_REVIEW_RULES;
- подробный lifecycle.

Целевой размер `12` — примерно 3–5 тыс. символов, но это НЕ жёсткий лимит. Нельзя терять safety-rule ради цифры.

3. Изменить стартовое чтение `11`.

При выполнении TASK агент должен начинать не с полного чтения `11`, а с:
- текущего `БЛОКА 1`;
- короткого `12`;
- документов, явно перечисленных в БЛОКЕ 1.

БЛОК 2 не читать автоматически.
БЛОК 3 не читать автоматически.
Они читаются только для связанного продолжения/review или по прямому указанию.

Матрицу тематического чтения убрать из `11` в `CONTEXT_ROUTING.md`.
`11` оставить рабочим shell TASK / REPORT / REVIEW, а не справочником всех правил.

4. Убрать постоянное чтение `30`.

`30_Единая_система_проекта.md` больше не является обязательным чтением каждой TASK.
Он нужен ChatGPT при крупных межмодульных/архитектурных задачах и агенту только если явно указан в БЛОКЕ 1.

5. Устранить двучтение документационных правил.

`DOCUMENTATION_RULES.md` уточнить:
штатное обновление AGENT REPORT / TASK_HISTORY / очистка БЛОКА 1 по TASK_LIFECYCLE_RULES не превращает каждую code-задачу в полноценную Documentation TASK.

Полный DOCUMENTATION_RULES обязателен при:
- создании/реорганизации документов;
- переносе правил;
- изменении документационной архитектуры;
- добавлении постоянных правил.

6. Оптимизировать encoding routing без ослабления защиты.

В коротком agent-core сохранить обязательные правила:
- не перекодировать файл самовольно;
- не повреждать кириллицу;
- при подозрении на mojibake/неясную кодировку остановиться.

Полный `ENCODING_RULES.md` назначать при реальном encoding-risk, восстановлении текста, документации/файлах с кириллицей и когда TASK прямо его требует.
Не заставлять чисто логический ASCII-код читать весь аварийный протокол без причины.

7. Удалить из активных управляющих правил stale-ссылки на:
- `AGENT_TASK_LOOP.md`;
- неоднозначный `START.md`;
если они относятся к текущему workflow, а не являются историческим упоминанием.

Архивные/исторические записи не переписывать.

8. Обновить индексы новых документов и сохранить ОДИН канонический владелец каждого правила.
Перед удалением текста из старого места проверить:
`старое правило → новое место → кто/когда его читает`.

Не допускать ситуации, когда правило исчезло или осталось полноценным дублем в двух документах.

9. Зафиксировать размер TASK в `TASK_PREPARATION_RULES.md`:
- обычная implementation TASK: ориентир 3500–4500 символов;
- FIX может быть короче;
- 5000–6000+ — сигнал проверить декомпозицию;
- это не лимит;
- безопасность и полнота важнее размера;
- не дробить неразделимую операцию ради цифры.

ПРОВЕРИТЬ ЭФФЕКТ:

До и после изменений посчитать символы минимум для:
A. обязательного старта обычного агента;
B. маленького FIX до тематического кода.

В отчёте показать:
CURRENT → NEW
и процент сокращения.

Не считать исходники и сам БЛОК 1 — сравнить именно постоянную документационную нагрузку.

ГРАНИЦЫ:
- код, JSON, runtime, PBO не менять;
- предметные документы 14–40 сейчас не переписывать;
- документ 38 сейчас не дробить;
- TASK_HISTORY не сокращать и не переписывать историю;
- отдельные Task_Records пока не создавать;
- универсальный Project Template пока не создавать;
- не удалять правило только потому, что оно длинное.

РАЗРЕШЕНО:
- Documentation_DayZ_Qwest/11_Задача_агенту.md
- Documentation_DayZ_Qwest/12_Старт_агента.md
- Documentation_DayZ_Qwest/10_Правила_агента.md при необходимости актуализации ссылок
- Documentation_DayZ_Qwest/20_SplitDoc/00_INDEX.md
- Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md
- Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md
- новые TASK_PREPARATION_RULES.md
- новые CONTEXT_ROUTING.md
- новые TASK_LIFECYCLE_RULES.md
- Documentation_DayZ_Qwest/21_Project_Docs/00_INDEX.md
- новый Documentation_DayZ_Qwest/21_Project_Docs/PROJECT_PATHS.md
- Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md

ПРОВЕРИТЬ:
- все новые/изменённые ссылки разрешаются;
- нет активных `AGENT_TASK_LOOP.md` / ложного `START.md`;
- правило не потеряно при переносе;
- нет ненужных полных дублей;
- Markdown;
- UTF-8 без BOM;
- git diff --check.

В AGENT REPORT отдельно дать:
- CREATED FILES;
- MOVED RULES;
- REMOVED DUPLICATION;
- CONTEXT BEFORE / AFTER;
- CHANGED FILES;
- ENCODING CHECK;
- PROBLEMS.

После успеха — одна запись TASK 181 и штатная очистка БЛОКА 1 уже по новой lifecycle-схеме.

Моды обновлять не нужно.
Git write-команды не выполнять.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK: TASK 179 — FIX: привести routeActivationRadii[] к реальному runtime fallback-контракту.

## STATUS

COMPLETED / CONTRACT FIX / WEB WORKSHOP.

## BASELINE

- GitHub main: commit message `90`.
- Полный SHA: `4b5768b87101388568fd8db3fc88bcab04ad3130`.
- Baseline подтверждён read-only Git-проверкой до изменений.
- Commit / push / pull / git add не выполнялись; Git-история, индекс, ветки и настройки не изменялись.

## CREATED FILES

Нет.

## CHANGED FILES

- `JSON_Quvest/migration.js`
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md` — одна запись TASK 179
- `Documentation_DayZ_Qwest/11_Задача_агенту.md` — только этот отчёт и штатная очистка БЛОКА 1

## DONE

- Validation больше не требует `routeActivationRadii[N]` для каждой route point: отсутствующий, `null`, пустой и короткий массив принимаются.
- Effective radius совпадает с runtime `GetRouteActivationRadius(index)`: explicit значение используется только при `> 0`, иначе берётся общий `routeActivationRadius`.
- Route N безопасно показывает effective fallback и помечает его текстом `fallback: общий ...`.
- Первое редактирование отсутствующего Route N создаёт explicit значение без sparse/null; недостающие предыдущие позиции заполняются текущим общим fallback.
- Существующая GROUP snapshot-history сохраняет исходную форму массива: Undo возвращает missing/short baseline, Redo — плотный explicit-массив, Reset — `baseConfig`.

## CONTRACT CHECK

PASS.

- Временный Node contract-test вне итогового repository diff проверил missing, `null`, empty, short и значения `0/-2` как runtime fallback cases.
- Explicit `23` имеет приоритет; отсутствующий или нулевой radius использует общий fallback `17`.
- Редактирование Route 4 при отсутствующем массиве создаёт `[12, 12, 12, 29]` без sparse/null.
- Core-операция истории восстановила исходное отсутствие массива и затем повторно применила плотный массив; clone baseline сохранил missing и short формы для Reset.

## CURRENT CONFIG CHECK

PASS.

- Реальный `GET /api/migration/config` вернул HTTP 200, правильный source identity и две текущие GROUP.
- Канонический `MigrationConfig.json` не изменялся.

## ENCODING CHECK

PASS.

- Все три изменённых файла читаются как UTF-8 без BOM.
- NUL, `U+FFFD`, повторяющиеся знаки вопроса и проверяемые mojibake-маркеры отсутствуют; кириллица отображается нормально.

## GIT DIFF CHECK

PASS для изменений TASK 179.

- `node --check JSON_Quvest/migration.js` пройден.
- `git diff --check` пройден.
- Scope изменений агента ограничен тремя allowlist-файлами.
- В `TASK_HISTORY.md` находится ровно одна запись TASK 179.
- `MigrationConfig.json`, Map, CSS/UI redesign, runtime/PBO и Safe Persistence агентом не изменялись.

## PROBLEMS

Нет.

## CONCLUSION

TASK 179 завершена. Migration Editor теперь повторяет runtime fallback-контракт `routeActivationRadii[]`; исправлен только обнаруженный хвост TASK 178. Моды обновлять не нужно.

## BLOCK 1 CLEANUP CHECK

PASS.

Заголовок БЛОКА 1 сохранён один раз; содержимое TASK 179 удалено; оставлены ровно три канонические строки; разделитель, БЛОК 2 и БЛОК 3 сохранены без duplicate heading.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
