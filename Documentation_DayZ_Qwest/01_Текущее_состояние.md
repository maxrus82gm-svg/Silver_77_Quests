# 01_CURRENT_STATE — Текущее состояние проекта

## Кратко

Проект: Silver_77_Quests для DayZ.

Главная цель:
- система квестов;
- редактор квестов;
- клиентский UI;
- серверная логика;
- документация для работы с агентом.

## Текущий рабочий принцип

- Все правила агента лежат в `Documentation/SplitDoc`.
- Главный стартовый документ: `Documentation/SplitDoc/START.md`.
- Текущая задача для агента лежит в `Documentation/AGENT_TASK_LOOP.md`.
- Obsidian-база `Documentation_DayZ_Qwest` используется как человеческая память проекта: решения, состояние, вопросы, идеи.  
- Реальный путь базы: `P:\Silver_77_Quests\Documentation_DayZ_Qwest`.

## Важное правило

ChatGPT выбирает минимальный набор документов для задачи.
Агент не читает все `SplitDoc` подряд.
