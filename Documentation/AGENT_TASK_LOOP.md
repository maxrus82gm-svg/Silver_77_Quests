# AGENT TASK LOOP

Файл для управления задачами агента.

Главная идея:
- ChatGPT ставит задачу
- пользователь вставляет задачу в этот файл
- агент читает задачу
- агент возвращает отчёт в чат
- пользователь присылает отчёт ChatGPT
- ChatGPT даёт проверку и следующую задачу
- пользователь вручную обновляет этот файл

================================================================================
# 🧭 БЛОК 0 — КАК ПОЛЬЗОВАТЬСЯ ФАЙЛОМ
================================================================================

Этот файл разделён на зоны.

Пользователь обычно редактирует только:

1. БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
2. БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
3. БЛОК 3 — REVIEW CHATGPT
4. БЛОК 5 — ИСТОРИЯ ЗАДАЧ

Агент должен читать только БЛОК 1 и БЛОК 4.

--------------------------------------------------------------------------------
ПРАВИЛО:
--------------------------------------------------------------------------------

Агент НЕ записывает отчёт в этот файл.

Агент возвращает отчёт в чат.

Пользователь вручную переносит чистый отчёт в БЛОК 2.

================================================================================
# 🟦 БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ЗАДАЧИ
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 011 — Точечное восстановление повреждённых text-строк в QuestMenu.layout

Цель:
Безопасно восстановить только повреждённые русские text-строки в `Silver_77_Quests_Client/gui/QuestMenu.layout`, не меняя структуру layout-файла.

Где работать:
- `Silver_77_Quests_Client/gui/QuestMenu.layout`

Что сделать:
1. Перед изменениями проверить текущее состояние Git
2. Открыть `Silver_77_Quests_Client/gui/QuestMenu.layout`
3. Найти только следующие виджеты/поля:
   - `TitleText`
   - `DescriptionPanelLabel`
   - `DescriptionText`
   - `RoutePanelLabel`
   - `DialogPanelLabel`
   - `AcceptButtonText`
   - `CompleteButtonText`
   - `CloseButtonText`
4. Заменить только значение поля `text`:

| Виджет/блок | Новый текст |
|---|---|
| `TitleText` | `КВЕСТЫ` |
| `DescriptionPanelLabel` | `Описание квеста` |
| `DescriptionText` | `Выберите квест из списка` |
| `RoutePanelLabel` | `Маршрут` |
| `DialogPanelLabel` | `Диалог NPC` |
| `AcceptButtonText` | `ВЗЯТЬ КВЕСТ` |
| `CompleteButtonText` | `СДАТЬ КВЕСТ` |
| `CloseButtonText` | `Закрыть рот и уйти` |

5. Не менять размеры, позиции, цвета, классы, имена виджетов и структуру файла
6. Не заменять файл целиком
7. После изменения вернуть отчёт в чат

Важно:
- изменять только `Silver_77_Quests_Client/gui/QuestMenu.layout`
- не трогать `gui/QuestMenu.layout`
- не трогать `SplitMods/`
- не выполнять автоматическое перекодирование всего проекта
- если редактор предлагает изменить кодировку — остановиться и написать в PROBLEMS
- соблюдать БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА

Критерии готовности:
- изменён только `Silver_77_Quests_Client/gui/QuestMenu.layout`
- заменены только повреждённые значения `text`
- структура layout-файла не изменилась
- отчёт возвращён в чат
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ЗАДАЧИ
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

### DONE:
- Идентифицированы повреждённые текстовые строки в `Silver_77_Quests_Client/gui/QuestMenu.layout`
- Для каждой повреждённой строки определены виджет, поле и повреждённое значение
- Найдены возможные нормальные аналоги в резервных копиях
- Учтена подсказка пользователя из БЛОКА 6 для кнопки `CloseButton`
- Составлена карта восстановления текста

### CHANGED FILES:
- Нет

### DIFF:
- Изменений в файлах нет
- Выполнен только анализ содержимого файлов
- Перекодирование или сохранение файлов не выполнялось

### PROBLEMS:
- Все текстовые строки в `Silver_77_Quests_Client/gui/QuestMenu.layout` отображаются как `�����`
- Восстановление нужно выполнять осторожно, без полной замены layout-файла
- Кнопка `CloseButton` требует особого внимания из-за пользовательского текста

### QUESTIONS:
- Нет

### КАРТА ПОВРЕЖДЁННЫХ СТРОК:

| Виджет/блок | Поле текста | Повреждённое значение | Возможный нормальный текст | Источник | Уверенность |
|---|---|---|---|---|---|
| `TitleText` | `text` | `������` | `КВЕСТЫ` | `gui/QuestMenu.layout` | высокая |
| `DescriptionPanelLabel` | `text` | `�������� ������` | `Описание квеста` | `SplitMods/Silver_77_Quests_Client/gui/QuestMenu.layout` | средняя |
| `DescriptionText` | `text` | `�������� ����� �� ������` | `Выберите квест из списка` | `gui/QuestMenu.layout` | высокая |
| `RoutePanelLabel` | `text` | `�������` | `Маршрут` | `SplitMods/Silver_77_Quests_Client/gui/QuestMenu.layout` | средняя |
| `DialogPanelLabel` | `text` | `���������� ������` | `Диалог npc` | `SplitMods/Silver_77_Quests_Client/gui/QuestMenu.layout` | средняя |
| `AcceptButtonText` | `text` | `����� �����` | `ВЗЯТЬ КВЕСТ` | `gui/QuestMenu.layout` | высокая |
| `CompleteButtonText` | `text` | `����� �����` | `СДАТЬ КВЕСТ` | `gui/QuestMenu.layout` | высокая |
| `CloseButtonText` | `text` | `�������` | `Закрыть рот и уйти` | БЛОК 6 — подсказка пользователя | высокая |

### ВЫВОД:
- Восстанавливать файл целиком нельзя
- Нужно точечно восстановить значения `text` у найденных виджетов
- Перед правкой нужно сделать резервную копию или проверить, что изменения отслеживаются Git

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟨 БЛОК 3 — REVIEW CHATGPT
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 010 REVIEW

Статус: принято.

Проверка выполнена корректно:
- повреждённые текстовые строки перечислены
- указаны виджеты и поля текста
- подобраны возможные нормальные значения
- указаны источники восстановления
- учтена подсказка пользователя для `CloseButtonText`

Вывод:
- полная замена `Silver_77_Quests_Client/gui/QuestMenu.layout` запрещена
- восстановление должно быть точечным
- менять нужно только значения `text` у найденных виджетов
- перед изменениями нужно убедиться, что Git видит текущее состояние файла

Следующая задача: TASK 011.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------


================================================================================
# 🟥 БЛОК 4 — ПРАВИЛА ДЛЯ АГЕНТА
================================================================================

Агент должен:

1. Читать задачу только из БЛОКА 1
2. Выполнять только текущую задачу
3. Не менять архитектуру без отдельного указания
4. Не делать рефакторинг без задачи
5. Не удалять файлы без отдельной задачи
6. Не переносить файлы без отдельной задачи
7. Не переименовывать файлы без отдельной задачи
8. Если задача аналитическая — ничего не изменять
9. Если задача требует правки — менять только явно указанные файлы
10. Отчёт возвращать в чат
11. Отчёт в файл не записывать
12. Соблюдать БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
13. Учитывать БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ, если задача связана с UI, layout-файлами или текстами

--------------------------------------------------------------------------------
ФОРМАТ ОТЧЁТА В ЧАТ:
--------------------------------------------------------------------------------

AGENT REPORT

DONE:
-

CHANGED FILES:
-

DIFF:
-

PROBLEMS:
-

QUESTIONS:
-

================================================================================
# 🟪 БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
================================================================================

ВАЖНО:
В проекте используется русскоязычный текст. Кириллицу нельзя повреждать.

Некоторые файлы могут быть сохранены не в UTF-8, а в CP1251 или другой Windows-кодировке.

Особенно осторожно работать с файлами:

- `.layout`
- `.json`
- `.md`
- `.c`
- `.cpp`
- `.txt`
- `.xml`

--------------------------------------------------------------------------------
ПРАВИЛА:
--------------------------------------------------------------------------------

1. НЕ перекодировать файлы без отдельной задачи
2. НЕ сохранять файл заново, если задача только аналитическая
3. НЕ заменять кириллицу на `????`, `�����` или транслит
4. НЕ “чинить” русские строки автоматически
5. Перед изменением файла с кириллицей нужно определить его текущую кодировку
6. Если кириллица отображается как `�����`, это НЕ значит, что текст надо переписать
7. Если кодировка непонятна — остановиться и написать в PROBLEMS
8. Любые правки текста с кириллицей делать только после отдельного подтверждения

--------------------------------------------------------------------------------
ОСОБО ОПАСНЫЕ ФАЙЛЫ:
--------------------------------------------------------------------------------

- `Silver_77_Quests_Client/gui/QuestMenu.layout`
- `Silver_77_Quests_Client/gui/QuestJournal.layout`
- `gui/QuestMenu.layout`
- `gui/QuestJournal.layout`
- `JSON_Quvest/*.json`
- `Documentation/*.md`

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ ОТЧЁТА:
--------------------------------------------------------------------------------

Если задача затрагивает файл с кириллицей, агент обязан указать в отчёте:

- была ли обнаружена кириллица
- отображается ли она нормально
- есть ли риск кодировки
- менялся ли файл
- сохранялся ли файл заново

================================================================================
# ⬛ БЛОК 5 — ИСТОРИЯ ЗАДАЧ
================================================================================

Новые завершённые задачи добавлять вниз.

--------------------------------------------------------------------------------

- TASK 001 — Проверка зависимости клиент/сервер — принято
- TASK 002 — Проверка соответствия имени клиентского мода в CfgPatches — принято
- TASK 003 — Инвентаризация дубликатов монолитной и split-версии — принято с уточнением
- TASK 004 — Проверка содержимого резервных и старых папок без изменений — принято
- TASK 005 — Проверка корневых config.cpp и mod.cpp — принято
- TASK 006 — Проверка корневой папки gui — принято с замечанием по кодировке
- TASK 007 — Проверка кодировки файлов с кириллицей — принято
- TASK 008 — Поиск нормальной версии QuestMenu.layout — принято
- TASK 009 — Сравнение QuestMenu.layout перед восстановлением — принято



================================================================================
# ⬛ БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ
================================================================================


Этот блок содержит ручные подсказки от пользователя по UI, текстам и визуальному поведению.

Агент должен учитывать этот блок, если задача связана с:
- UI
- layout-файлами
- текстами
- визуальными элементами
- восстановлением кириллицы

--------------------------------------------------------------------------------
ПОДСКАЗКИ:
--------------------------------------------------------------------------------

- В `QuestMenu.layout` нижняя серая кнопка / полоса в диалоговом окне должна иметь текст:
  `Закрыть рот и уйти`

- Если агент не уверен в назначении UI-элемента, он должен указать это в `QUESTIONS`, а не исправлять текст самостоятельно.

================================================================================
# КОНЕЦ ФАЙЛА
================================================================================