# AGENT TASK LOOP

Файл для управления задачами агента.

Главная идея:
- ChatGPT ставит задачу
- пользователь вставляет задачу в этот файл
- агент читает задачу
- агент возвращает отчёт в чат
- пользователь присылает отчёт ChatGPT
- ChatGPT даёт проверку и следующую задачу
- пользователь вручную обновляет этот файл


================================================================================
# 🧭 БЛОК 0 — КАК ПОЛЬЗОВАТЬСЯ ФАЙЛОМ
================================================================================

Этот файл — рабочий журнал взаимодействия:

- пользователь
- ChatGPT
- Kiro / агент
- проект Silver_77_Quests

Главная цель файла:

- хранить текущую задачу
- хранить последний отчёт агента
- хранить последний review ChatGPT
- хранить правила работы
- хранить историю задач
- хранить пользовательские подсказки по UI, смыслу и техническому опыту

--------------------------------------------------------------------------------
ОБЩИЙ ПРОЦЕСС:
--------------------------------------------------------------------------------

1. ChatGPT формирует задачу.
2. Пользователь вставляет задачу в БЛОК 1.
3. Агент читает БЛОК 1.
4. Агент выполняет только текущую задачу.
5. Агент возвращает отчёт в чат.
6. Пользователь отправляет отчёт ChatGPT.
7. ChatGPT проверяет отчёт.
8. ChatGPT даёт:
   - чистый AGENT REPORT для БЛОКА 2
   - REVIEW для БЛОКА 3
   - следующую задачу для БЛОКА 1, если она нужна
9. Пользователь вручную обновляет файл.
10. Git commit / push делает только пользователь.

--------------------------------------------------------------------------------
КТО ЗА ЧТО ОТВЕЧАЕТ:
--------------------------------------------------------------------------------

Пользователь:

- принимает финальные решения
- проверяет результат в игре
- контролирует Git
- делает commit / push
- обновляет этот файл вручную
- даёт визуальные, смысловые и технические подсказки

ChatGPT:

- ставит задачи
- формирует безопасные инструкции
- проверяет отчёты агента
- пишет REVIEW
- следит за архитектурой, рисками и порядком задач
- помогает вести этот файл
- помогает извлекать правила из успешно решённых проблем

Агент / Kiro:

- читает текущую задачу из БЛОКА 1
- выполняет только указанную задачу
- возвращает отчёт в чат
- не пишет отчёт в этот файл
- не делает commit / push
- не принимает архитектурные решения без отдельного указания

--------------------------------------------------------------------------------
КАКИЕ БЛОКИ ОБНОВЛЯТЬ:
--------------------------------------------------------------------------------

Пользователь обычно редактирует только:

1. БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
2. БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
3. БЛОК 3 — REVIEW CHATGPT
4. БЛОК 5 — ИСТОРИЯ ЗАДАЧ
5. БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ

БЛОК 4, БЛОК 4.1 и БЛОК 4.2 менять только если меняются правила работы или появляется новый подтверждённый опыт.

--------------------------------------------------------------------------------
КАК ЗАМЕНЯТЬ СОДЕРЖИМОЕ:
--------------------------------------------------------------------------------

В БЛОКЕ 1 заменять только текст между:

## НАЧАЛО ЗАДАЧИ

и

## КОНЕЦ ЗАДАЧИ

В БЛОКЕ 2 заменять только текст между:

## НАЧАЛО ОТЧЁТА

и

## КОНЕЦ ОТЧЁТА

В БЛОКЕ 3 заменять только текст между:

## НАЧАЛО REVIEW

и

## КОНЕЦ REVIEW

Сами строки НАЧАЛО и КОНЕЦ не удалять.

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ АГЕНТА:
--------------------------------------------------------------------------------

Агент НЕ записывает отчёт в этот файл.

Агент возвращает отчёт в чат.

Пользователь вручную переносит чистый отчёт в БЛОК 2 после проверки ChatGPT.

Агент не должен считать задачи из истории, review или подсказок активными.
Активная задача находится только в БЛОКЕ 1.

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ GIT:
--------------------------------------------------------------------------------

Git контролирует только пользователь.

Агенту запрещено:

- делать git commit
- делать git push
- создавать ветки
- делать git reset
- делать git clean
- делать git checkout
- откатывать изменения без отдельного разрешения

Агент может читать Git-статус только если это явно указано в задаче.

--------------------------------------------------------------------------------
ПРАВИЛО ПРОВЕРКИ АКТУАЛЬНОЙ ВЕРСИИ ДЛЯ CHATGPT:
--------------------------------------------------------------------------------

Перед анализом Documentation/AGENT_TASK_LOOP.md ChatGPT должен:

1. Сначала проверить последний коммит ветки main.
2. Сообщить пользователю:
   - короткий SHA коммита
   - название коммита
3. Читать Documentation/AGENT_TASK_LOOP.md именно из этого коммита, а не из кешированной версии main.
4. В начале ответа писать:

   Читаю коммит: <SHA> — <название коммита>

5. Если пользователь видит, что коммит неактуальный, он сообщает актуальное название или SHA последнего коммита.

--------------------------------------------------------------------------------
ТЕКУЩАЯ АКТУАЛЬНАЯ АРХИТЕКТУРА ПРОЕКТА:
--------------------------------------------------------------------------------

Проект Silver_77_Quests сейчас развивается как split Client / Server.

Актуальная клиентская часть:

Silver_77_Quests_Client/

Актуальная серверная часть:

Silver_77_Quests_Server/

Документация проекта:

Documentation/

Старые / резервные части не считать актуальными без отдельной задачи:

- корневая папка scripts/
- корневая папка gui/
- корневые config.cpp и mod.cpp
- SplitMods/
- Support/

--------------------------------------------------------------------------------
ВАЖНО ДЛЯ ЗАДАЧ:
--------------------------------------------------------------------------------

Если задача связана с UI, layout-файлами, кириллицей или текстами, агент должен учитывать:

- БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
- БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ

Если задача связана с RPC, client-server sync, PlayerBase.OnRPC, ParamsReadContext, RPCSingleParam, Param1/Param2/Param3, JSON payload или синхронизацией данных между сервером и клиентом, агент должен учитывать:

- БЛОК 4.2 — DAYZ CLIENT-SERVER RPC И СИНХРОНИЗАЦИЯ

Если задача связана с логикой квестов, JSON-квестами, Offer, Completion, Reward, requiredQuestIds, серверной обработкой квестов или клиентской логикой отображения квестов, агент должен учитывать:

- Documentation/QUEST_LOGIC_SPEC.md

Если задача связана с кодом, агент должен работать только в явно указанных файлах.

Если задача аналитическая, агент ничего не изменяет.

Если задача требует правки, агент меняет только явно указанные файлы.

--------------------------------------------------------------------------------
ПРАВИЛО ПОСЛЕ УСПЕШНОГО ФИКСА:
--------------------------------------------------------------------------------

После успешно решённой сложной проблемы нужно:

1. Закрыть активную задачу в БЛОКЕ 1.
2. Перенести отчёт агента в БЛОК 2.
3. Перенести review ChatGPT в БЛОК 3.
4. Добавить задачу в БЛОК 5 — ИСТОРИЯ ЗАДАЧ.
5. Если из проблемы получен важный опыт, добавить правило в:
   - БЛОК 4
   - БЛОК 4.1
   - БЛОК 4.2
   - БЛОК 6
   в зависимости от темы.

Пример:
TASK 064 дал правило для будущих DayZ RPC-задач:
JSON/string payload нельзя отправлять одной строкой через RPC, нужно использовать chunked sync.


================================================================================
# 🟦 БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
================================================================================

БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Статус:
Активной задачи на выполнение сейчас нет.

Последняя выполненная задача:
TASK 078 — QuestMenu UI layout fix + server JSON sync + scroll/text cleanup.

Статус TASK 078:
Выполнена агентом.
Принята условно.
Требует ручной проверки в игре.

Что было сделано по TASK 078:
- актуальный JSON из редактора синхронизирован в server profile;
- перед синхронизацией сделан backup;
- QuestPanel расширен до size 980 560;
- QuestListbox изменён на position 30 82 и size 250 450;
- DescriptionPanel расширен;
- DialogPanel расширен;
- RoutePanel изменён на size 210 334;
- из левого списка убраны длинные служебные префиксы;
- из route label убран [F];
- DialogPanel должен показывать текущий triggerActions[].dialogText;
- фон QuestMenu / QuestJournal сделан плотнее;
- QuestMenu.layout приведён из cp1251 в UTF-8 без BOM;
- JSON-контракт не менялся;
- поля requiredItems/dialogue не добавлялись;
- stack rules не трогались.

Что НЕ было проверено:
- визуальная проверка в игре после билда клиента;
- поведение Offer NPC;
- поведение Completion NPC;
- поведение Reward NPC;
- читаемость фона в реальной игре;
- не перекрывает ли action prompt окно квеста;
- насколько улучшились DescriptionPanel / DialogPanel;
- хватает ли текущих размеров без полноценного scroll;
- не сломалась ли кириллица после перекодировки QuestMenu.layout.

Главное оставшееся ограничение:
Полноценный scroll для DescriptionPanel и DialogPanel не реализован.
Сейчас улучшение сделано за счёт:
- расширения панелей;
- очистки лишнего текста;
- более плотного фона;
- отказа от лишнего dialog journal fallback.

Если длинные тексты всё ещё обрезаются, нужна отдельная задача:
TASK 079 — полноценная прокрутка DescriptionPanel / DialogPanel / возможно QuestListbox.

--------------------------------------------------------------------------------
ПЛАН НА ЗАВТРА
--------------------------------------------------------------------------------

Начать с проверки TASK 078 в игре.

Шаг 1:
Собрать клиентский PBO, потому что менялись клиентские файлы:
- Silver_77_Quests_Client\scripts\5_Mission\QuestUI.c
- Silver_77_Quests_Client\scripts\5_Mission\QuestJournalUI.c
- Silver_77_Quests_Client\gui\QuestMenu.layout
- Silver_77_Quests_Client\gui\QuestJournal.layout

Шаг 2:
Обновить клиентскую часть мода.

Шаг 3:
Запустить сервер с уже синхронизированным profile JSON.

Шаг 4:
Проверить Offer NPC:
- левый список квестов стал уже и выше;
- слева нет лишнего текста типа [Награда здесь];
- справа RoutePanel шире;
- в RoutePanel нет лишнего [F];
- верхняя панель DescriptionPanel показывает описание, цели, прогресс, награды, статус;
- нижняя панель DialogPanel показывает NPC dialogText;
- фон достаточно плотный;
- action prompt не мешает читать;
- кириллица нормальная.

Шаг 5:
Проверить Completion NPC:
- DescriptionPanel показывает цели / прогресс сдачи;
- DialogPanel показывает текст Completion NPC;
- кнопка сдачи работает как раньше.

Шаг 6:
Проверить Reward NPC:
- DescriptionPanel показывает статус / награду / закрытие;
- DialogPanel показывает текст Reward NPC;
- финальная награда выдаётся;
- квест закрывается корректно.

Шаг 7:
Проверить QuestJournal:
- список квестов читаемый;
- длинные префиксы убраны;
- фон стал плотнее;
- кириллица нормальная;
- описание не заменяется NPC dialogText.

Шаг 8:
Если всё нормально:
закрыть TASK 078 окончательно.

Шаг 9:
Если тексты всё ещё обрезаются:
сформировать TASK 079 — полноценный scroll для DescriptionPanel / DialogPanel.

Шаг 10:
Если action prompt всё ещё перекрывает окно:
сформировать отдельную задачу на скрытие/подавление action prompt во время открытого QuestUI
или усиление overlay-фона.

--------------------------------------------------------------------------------
НЕ ЗАБЫТЬ
--------------------------------------------------------------------------------

Мод — источник правды.
Редактор должен подстраиваться под контракт мода.

Правильный контракт:
- quest.description = описание квеста;
- objectives[] = цели / предметы для сдачи;
- triggerActions[].dialogText = текст NPC;
- rewards[] = награды;
- offerTriggerIds[] / completionTriggerIds[] / rewardTriggerIds[] = роли;
- requiredItems как отдельного поля нет;
- dialogue как отдельного поля нет.

Не добавлять:
- requiredItems[]
- dialogue
- dialogueText вне triggerActions[]

Не ломать:
- NPC Flow;
- Offer / Completion / Reward;
- item-stack-rules.json;
- stack rules;
- JSON_Quvest как основную рабочую папку редактора.

Поправка к TASK 078:
Актуальный JSON должен считаться подготовленным в рабочей папке разработки:
P:\Silver_77_Quests\JSON_Quvest\Silver_77_Quests.json

Копирование в локальный DayZ profile, если оно было сделано, считать локальным тестовым действием, а не переносом на настоящий игровой сервер.

Реальный перенос JSON на сервер пользователь выполняет вручную.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ЗАДАЧИ
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

DONE:
- Расширена структура node в DayZ layout viewer.
- Добавлены parentId и parentName.
- Добавлены поля text, font и props.
- Реализован парсинг text.
- Реализован парсинг font.
- Реализован парсинг quoted props, например "text halign" center.
- Реализован парсинг дополнительных normal props.
- Исправлен calculateAbsolutePositions: отсутствие position больше не останавливает обход children.
- renderLayout теперь использует absX / absY.
- На визуальных блоках отображается имя node.name.
- Если color отсутствует, используется transparent background + видимая dashed border.
- random color убран.
- DOMParser не используется.
- position[] / size[] / color[] не используются.
- Export / Generate / Save не добавлялись.

CHANGED FILES:
- DayZ_layout/dayz_layout_viewer.html

DIFF:
- Расширена модель node.
- Добавлен парсинг text/font/props.
- Исправлен расчёт absolute position.
- Улучшен renderLayout для отображения имён и прозрачных блоков без color.

PROBLEMS:
- В одном из коммитов снова фигурировали изменения Documentation/AGENT_TASK_LOOP.md. Если это было сделано агентом, это нарушение ограничения. Для чистого технического коммита желательно оставить изменённым только DayZ_layout/dayz_layout_viewer.html.

CONCLUSION:
- TASK 065 FIX 7 принят как завершение ядра DayZ layout viewer.
- Viewer уже имеет рабочую основу: парсер реального DayZ .layout, дерево parent/children, absX/absY, text/font/props и базовый визуальный render.
- Следующий шаг — отдельная задача TASK 066 на UI-инспектор.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟨 БЛОК 3 — REVIEW CHATGPT
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 078 REVIEW

Статус:
Принято условно.

Причина условного статуса:
Агент выполнил задачу и дал подробный отчёт, но пользователь ещё не проверил результат в игре.

Что принято:
- JSON из редактора синхронизирован в server profile.
- Backup server profile JSON сделан.
- QuestMenu layout расширен.
- QuestListbox уменьшен по ширине и увеличен по высоте.
- DescriptionPanel / DialogPanel расширены.
- RoutePanel расширен.
- Длинные служебные префиксы в списках убраны.
- [F] из route label убран.
- DialogPanel возвращён к текущему triggerActions[].dialogText.
- Фон сделан плотнее.
- Кодировка QuestMenu.layout приведена к UTF-8 без BOM.
- JSON-контракт не изменён.
- requiredItems/dialogue не добавлялись.

Что осталось проверить:
- реальный вид QuestMenu в игре;
- реальный вид QuestJournal в игре;
- читаемость длинных текстов;
- нужна ли отдельная задача на scroll;
- мешает ли action prompt;
- не сломалась ли кириллица;
- кнопки взять/сдать/закрыть.

Главный риск:
Полноценная прокрутка DescriptionPanel / DialogPanel не сделана.
Это осознанное ограничение TASK 078 и возможная будущая TASK 079.

Вывод:
TASK 078 не закрывать окончательно до ручной проверки в игре.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟥 БЛОК 4 — ПРАВИЛА ДЛЯ АГЕНТА
================================================================================

================================================================================
# 🟥 БЛОК 4 — ПРАВИЛА ДЛЯ АГЕНТА
================================================================================

Агент должен:

1. Читать задачу только из БЛОКА 1.
2. Выполнять только текущую задачу.
3. Не выполнять задачи из истории, review или пользовательских подсказок, если они не перенесены в БЛОК 1.
4. Не менять архитектуру без отдельного указания.
5. Не делать рефакторинг без отдельной задачи.
6. Не удалять файлы без отдельной задачи.
7. Не переносить файлы без отдельной задачи.
8. Не переименовывать файлы без отдельной задачи.
9. Если задача аналитическая — ничего не изменять.
10. Если задача требует правки — менять только явно указанные файлы.
11. Если в задаче указаны конкретные файлы — не выходить за их пределы.
12. Если для решения явно нужен другой файл — сначала указать это в PROBLEMS или QUESTIONS, а не править молча.
13. Отчёт возвращать только в чат.
14. Отчёт в файл не записывать.
15. Соблюдать БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА.
16. Соблюдать БЛОК 4.2 — DAYZ CLIENT-SERVER RPC И СИНХРОНИЗАЦИЯ.
17. Учитывать БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ, если задача связана с UI, layout-файлами или текстами.
18. Учитывать Documentation/QUEST_LOGIC_SPEC.md, если задача связана с логикой квестов, JSON-квестами, редактором квестов, Offer, Completion, Reward, 	requiredQuestIds, серверной обработкой квестов или клиентской логикой отображения квестов.
19. Git контролирует только пользователь.
20. Агент НЕ делает git commit.
21. Агент НЕ делает git push.
22. Агент НЕ создаёт ветки без отдельной задачи.
23. Агент НЕ выполняет git reset, git clean, git checkout без отдельного разрешения.
24. Агент может читать Git-статус только если это явно указано в задаче.
25. Не чинить сразу несколько уровней системы одновременно, если задача диагностическая.
26. Для client-server проблем идти по цепочке: request → server data → server send → client receive → client read → deserialize → apply → UI.
27. Если задача связана с RPC, сначала подтвердить транспорт и формат данных, и только потом менять UI.
28. Не считать UI первопричиной, пока не подтверждено, что данные успешно применены на клиенте.
29. Отчёт должен быть кратким: только факты, без длинных объяснений, если подробный анализ не запрошен отдельно.
30. Для задач по отдельным инструментам в папке DayZ_layout агент должен менять только явно указанные файлы внутри DayZ_layout и не трогать документацию, мод, JSON или layout-файлы проекта.
31. Если полная задача уже записана в БЛОК 1, в чат агенту отправляется только короткая команда. Агент не должен брать задачи из текста чата, если они расходятся с БЛОКОМ 1.

--------------------------------------------------------------------------------
ФОРМАТ ОТЧЁТА В ЧАТ:
--------------------------------------------------------------------------------

Отчёт должен быть коротким и практичным.

AGENT REPORT

DONE:
- что сделано

CHANGED FILES:
- какие файлы изменены

DIFF:
- кратко что изменилось

PROBLEMS:
- только реальные проблемы

QUESTIONS:
- только реальные вопросы

CONCLUSION:
- краткий вывод

--------------------------------------------------------------------------------
ВАЖНО ДЛЯ ОТЧЁТА:
--------------------------------------------------------------------------------

Если всё прошло нормально, не расписывать длинные объяснения.

Если задача аналитическая — дать вывод кратко, но достаточно конкретно.

Если задача связана с риском, кодировкой, удалением файлов, Git или структурой проекта — обязательно указать риск в PROBLEMS.

Если задача связана с логикой квестов, JSON-квестами, редактором квестов, Offer, Completion, Reward или requiredQuestIds — сверять выводы с Documentation/QUEST_LOGIC_SPEC.md.

Если задача связана с DayZ RPC, client-server sync, ParamsReadContext, Param1/Param2/Param3, player data, config sync или JSON payload — сверять выводы с БЛОКОМ 4.2.

Если нужен подробный анализ, пользователь или ChatGPT укажет это отдельно.

================================================================================
# 🟪 БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
================================================================================

================================================================================
# 🟪 БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
================================================================================

ВАЖНО:
В проекте используется русскоязычный текст. Кириллицу нельзя повреждать.

Некоторые файлы могут быть сохранены не в UTF-8, а в CP1251 или другой Windows-кодировке.

Особенно осторожно работать с файлами:

- .layout
- .json
- .md
- .c
- .cpp
- .txt
- .xml

--------------------------------------------------------------------------------
ПРАВИЛА:
--------------------------------------------------------------------------------

1. НЕ перекодировать файлы без отдельной задачи.
2. НЕ сохранять файл заново, если задача только аналитическая.
3. НЕ заменять кириллицу на ????, ����� или транслит.
4. НЕ “чинить” русские строки автоматически.
5. Перед изменением файла с кириллицей нужно определить его текущую кодировку.
6. Если кириллица отображается как �����, это НЕ значит, что текст надо переписать.
7. Если кодировка непонятна — остановиться и написать в PROBLEMS.
8. Любые правки текста с кириллицей делать только после отдельного подтверждения.
9. Не менять кавычки, переносы строк и спецсимволы в JSON без задачи.
10. Не форматировать JSON целиком, если задача требует изменить только одну строку или один блок.
11. При работе с layout-файлами не менять стили, размеры, anchors и visibility без отдельной задачи.
12. При работе с Markdown-документацией не переписывать весь файл, если задача требует заменить только один блок.

--------------------------------------------------------------------------------
ОСОБО ОПАСНЫЕ ФАЙЛЫ:
--------------------------------------------------------------------------------

- Silver_77_Quests_Client/gui/QuestMenu.layout
- Silver_77_Quests_Client/gui/QuestJournal.layout
- gui/QuestMenu.layout
- gui/QuestJournal.layout
- JSON_Quvest/*.json
- Documentation/*.md

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ ОТЧЁТА:
--------------------------------------------------------------------------------

Если задача затрагивает файл с кириллицей, агент обязан указать в отчёте:

- была ли обнаружена кириллица
- отображается ли она нормально
- есть ли риск кодировки
- менялся ли файл
- сохранялся ли файл заново


================================================================================
# 🟫 БЛОК 4.2 — DAYZ CLIENT-SERVER RPC И СИНХРОНИЗАЦИЯ
================================================================================

Этот блок обязателен для задач, связанных с:

- DayZ RPC
- client/server sync
- PlayerBase.OnRPC
- ParamsReadContext
- RPCSingleParam
- Param1 / Param2 / Param3 / Param4
- JSON payload
- config sync
- player data sync
- quest progress sync
- UI, который зависит от server data

--------------------------------------------------------------------------------
ГЛАВНОЕ ПРАВИЛО:
--------------------------------------------------------------------------------

Не отправлять JSON, progress, config, dialog data или другой длинный string payload одной строкой через RPC.

Для DayZ/Enforce это ненадёжно.

Возможная ошибка клиента:

Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!

Если такая ошибка появилась при ctx.Read string payload — не искать первопричину в UI.
Сначала проверить транспорт RPC и перевести передачу payload на chunked sync.

--------------------------------------------------------------------------------
ПРАВИЛО CHUNKED SYNC:
--------------------------------------------------------------------------------

Любые крупные или потенциально сложные данные передавать чанками.

Рекомендуемый формат:

Param3<int, int, string>

param1 = chunkIndex
param2 = totalChunks
param3 = chunkPayload

Рекомендуемый размер чанка:
- 512 байт
- либо уже существующая константа проекта для chunk sync

--------------------------------------------------------------------------------
ОТДЕЛЬНЫЕ BUFFER ДЛЯ РАЗНЫХ ТИПОВ ДАННЫХ:
--------------------------------------------------------------------------------

Для каждого типа payload должен быть отдельный buffer.

Правильно:
- config chunks отдельно
- player data chunks отдельно
- dialog data chunks отдельно
- future inventory/snapshot chunks отдельно

Неправильно:
- складывать config и player data в один общий map buffer
- переиспользовать один expectedChunkCount для разных RPC payload
- собирать разные payload-типы в одной функции без явного разделения

--------------------------------------------------------------------------------
ПРАВИЛО ParamsReadContext:
--------------------------------------------------------------------------------

ctx.Read выполнять прямо внутри соответствующего case в OnRPC.

Не передавать ParamsReadContext в helper-функции.

Правильно:

case SILVER77_QUEST_RPC_PLAYER_DATA:
{
    Param3<int, int, string> data = new Param3<int, int, string>(0, 0, "");
    if (!ctx.Read(data))
    {
        Print("[Silver_77_Quests][CLIENT_PROGRESS_DEBUG] ERROR: ctx.Read failed");
        return;
    }

    string payload = Silver77_StorePlayerDataSyncChunk(data.param1, data.param2, data.param3);
    if (payload == "")
        return;

    Silver77_HandleQuestPlayerDataPayload(payload);
    return;
}

Неправильно:

case SILVER77_QUEST_RPC_PLAYER_DATA:
    Silver77_HandleQuestPlayerData(ctx);
    return;

private void Silver77_HandleQuestPlayerData(ParamsReadContext ctx)
{
    ctx.Read(...);
}

Helper-функции должны получать обычные значения:
- string payload
- int chunkIndex
- int totalChunks
- PlayerQuestData
- Silver77_QuestConfig

--------------------------------------------------------------------------------
ДИАГНОСТИКА RPC ПО ЦЕПОЧКЕ:
--------------------------------------------------------------------------------

При проблемах client-server sync проверять строго по этапам.

1. Клиент отправил request?
Искать client log:
- Sending ... request RPC

2. Сервер получил request?
Искать server RPT:
- Received ... request RPC

3. Сервер загрузил реальные данные?
Искать server RPT:
- LoadPlayerData
- FileExist=true
- progressCount
- questId/status

4. Сервер подготовил payload?
Искать:
- payloadLength
- Serialize...

5. Сервер отправил response?
Искать:
- Sent ... to client
- Sent ... in X chunks

6. Клиент получил chunks?
Искать client RPT:
- Received ... chunk 1/X
- Received ... chunk X/X

7. Клиент собрал payload?
Искать:
- Reassembled ... payload
- SUCCESS: Reassembled ...

8. Клиент десериализовал payload?
Искать:
- JsonSerializer.ReadFromString succeeded
- PlayerQuestData deserialized successfully

9. Клиент применил данные?
Искать:
- ApplySyncedPlayerData
- ApplySyncedPlayerData completed

10. UI обновился после apply?
Искать:
- реальные status active/completed
- кнопки “УЖЕ АКТИВЕН”, “СДАТЬ КВЕСТ”, “НАГРАДА”
- отсутствие ложного available

--------------------------------------------------------------------------------
ЕСЛИ КЛИЕНТ ПАДАЕТ В ctx.Read:
--------------------------------------------------------------------------------

Сначала проверить:

1. Совпадает ли RPC ID на сервере и клиенте.
2. Совпадает ли Param-тип на сервере и клиенте.
3. Не читает ли super.OnRPC или другой обработчик ctx раньше нашего case.
4. Не передаётся ли ParamsReadContext в helper.
5. Не отправляется ли длинная string одной строкой.
6. Нет ли в логе:
   Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!

Если есть String CORRUPTED:
- не начинать с UI
- не начинать с JSON-логики квестов
- не начинать с HasSyncedPlayerData
- не начинать с кнопок
- сначала перевести payload на chunked sync

--------------------------------------------------------------------------------
SERVER IS SOURCE OF TRUTH:
--------------------------------------------------------------------------------

Сервер является источником правды.

Сервер:
- хранит progress
- загружает progress из profile JSON
- меняет quest status
- выдаёт rewards
- сохраняет player data
- отправляет sync клиенту

Клиент:
- запрашивает config/player data
- получает sync
- отображает UI
- не должен считать default available реальным progress
- не должен решать server-side outcome самостоятельно

--------------------------------------------------------------------------------
UI НЕ ДОЛЖЕН МАСКИРОВАТЬ ПРОБЛЕМЫ SYNC:
--------------------------------------------------------------------------------

Если HasSyncedPlayerData == false:
- UI может показывать loading
- UI не должен отображать окончательные статусы
- UI не должен активировать кнопки на основе default available
- UI не должен создавать впечатление, что квест реально available, если server sync ещё не получен

Функции типа EnsurePlayerProgress/GetQuestStatus не должны подменять отсутствие server sync настоящим progress.

Default available допустим только как временная заглушка, но не как источник истины.

--------------------------------------------------------------------------------
МИНИМАЛЬНЫЕ ЗАДАЧИ:
--------------------------------------------------------------------------------

При RPC/client-server проблемах не менять сразу:

- UI
- JSON
- server progress
- client manager
- RPC format
- layout

одновременно.

Делать маленькие задачи:

1. Подтвердить request.
2. Подтвердить server data.
3. Подтвердить server send.
4. Подтвердить client receive.
5. Подтвердить ctx.Read.
6. Подтвердить deserialize.
7. Подтвердить ApplySyncedData.
8. Подтвердить UI update.

После каждого шага проверять server/client logs.

--------------------------------------------------------------------------------
ОПЫТ TASK 064:
--------------------------------------------------------------------------------

Проблема:
Player data JSON отправлялся одной строкой через RPC.
Клиент падал:

Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!
Silver_77_Quests/scripts/4_World/questclientrpc.c:70 Function OnRPC

Что помогло:
Player data sync переведён на chunked sync, как config sync.

Результат:
Клиент начал получать реальные статусы.
UI начал показывать корректное состояние, например “УЖЕ АКТИВЕН”.

Правило на будущее:
Если через RPC передаётся JSON/string payload — сразу проектировать chunked sync.

================================================================================
# ⬛ БЛОК 5 — ИСТОРИЯ ЗАДАЧ
================================================================================

Новые завершённые задачи добавлять вниз.

--------------------------------------------------------------------------------

- TASK 001 — Проверка зависимости клиент/сервер — принято
- TASK 002 — Проверка соответствия имени клиентского мода в CfgPatches — принято
- TASK 003 — Инвентаризация дубликатов монолитной и split-версии — принято с уточнением
- TASK 004 — Проверка содержимого резервных и старых папок без изменений — принято
- TASK 005 — Проверка корневых config.cpp и mod.cpp — принято
- TASK 006 — Проверка корневой папки gui — принято с замечанием по кодировке
- TASK 007 — Проверка кодировки файлов с кириллицей — принято
- TASK 008 — Поиск нормальной версии QuestMenu.layout — принято
- TASK 009 — Сравнение QuestMenu.layout перед восстановлением — принято
- TASK 010 — Составление карты повреждённых строк QuestMenu.layout — принято
- TASK 011 — Попытка восстановления text-строк — остановлено безопасно из-за риска кодировки
- TASK 012 — Выбор безопасного способа восстановления QuestMenu.layout — принято
- TASK 013 — Подготовка безопасного скрипта восстановления QuestMenu.layout — принято предварительно
- TASK 014 — Code review скрипта восстановления QuestMenu.layout — принято
- TASK 015 — Финальная проверка кода restore_questmenu_text.py перед запуском — принято
- TASK 016 — Ручной запуск restore_questmenu_text.py и проверка результата — принято
- TASK 017 — Проверка восстановленного QuestMenu.layout без изменений — принято
- TASK 018 — Уборка временных Python/cache/backup файлов после восстановления — принято
- TASK 019 — Проверка результата восстановления QuestMenu.layout в проекте без изменений — принято
- TASK 020 — Диагностика CloseButton и CloseButtonText без изменений — принято
- TASK 021 — Подключение CloseButtonText в QuestUI.c — принято
- TASK 022 — Сравнение стилей CloseButton с рабочими кнопками — принято
- TASK 023 — Включение CloseButton и CloseButtonText в UpdateButtons — принято
- TASK 024 — Ручная проверка CloseButton в игре — принято
- TASK 059 — Диагностика OnRPC case без вызова Silver77_HandleQuestPlayerData(ctx) — принято как диагностический шаг
- TASK 060 — Перенос ctx.Read player data прямо в OnRPC case — принято как архитектурная правка
- TASK 061 — Диагностическая проверка Param1<string> вместо Param3 — временный тест, позже откатан
- TASK 063 — Возврат player data RPC на Param3 после диагностики — принято
- TASK 064 — Player data RPC переведён на chunked sync как config sync — принято. Исправлена ошибка “String CORRUPTED - FIX OnStoreLoad()”. В игре подтверждено: UI получает реальные статусы квестов и показывает “УЖЕ АКТИВЕН”.
- TASK 065 — DayZ layout viewer — начата разработка отдельного read-only просмотрщика .layout файлов в DayZ_layout/.
- TASK 065 FIX 1–4 — несколько неудачных подходов к viewer: plain text viewer, XML/DOMParser, неверный формат position[] / size[] / color[]. Отклонено.
- TASK 065 FIX 5 — парсер переведён на stack-подход для вложенных виджетов. Частично принято как промежуточный шаг.
- TASK 065 FIX 6 — добавлено ядро дерева parent/children и absX/absY. Частично принято как шаг по ядру.
- TASK 065 FIX 7 — принято. Ядро DayZ layout viewer доведено: parentId/parentName, text/font/props, безопасный absX/absY, подписи node.name, transparent border без color.
- TASK 078 — выполнена, принята условно. Синхронизирован актуальный JSON из редактора в server profile с backup. Расширен QuestMenu: QuestPanel 980x560, QuestListbox 250x450, DescriptionPanel/DialogPanel шире, RoutePanel 210x334. Убраны длинные префиксы и [F] из квестового UI, DialogPanel показывает текущий triggerActions[].dialogText, фон стал плотнее, QuestMenu.layout приведён к UTF-8 без BOM. JSON-контракт не менялся. Полноценный scroll для DescriptionPanel/DialogPanel не реализован и остаётся возможной отдельной задачей.


================================================================================
# ⬛ БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ
================================================================================


БЛОК 6 — ОПЫТ / ПОДСКАЗКИ ПОЛЬЗОВАТЕЛЯ

--------------------------------------------------------------------------------
ОБЩИЙ РЕЖИМ РАБОТЫ С АГЕНТОМ
--------------------------------------------------------------------------------

- Полная задача всегда находится в БЛОКЕ 1.
- В чат агенту отправляется только короткая команда:
  "Выполни текущую задачу из БЛОКА 1."
- Агент должен читать задачу из БЛОКА 1 и выполнять только её.
- Если задача аналитическая — агент ничего не меняет.
- Если задача требует правки — агент меняет только явно разрешённые файлы.
- Git контролирует пользователь.
- Агент не должен делать коммиты без отдельного указания.
- Агент не должен менять документацию, если это явно не разрешено задачей.
- Если агенту разрешено писать отчёт в Documentation/AGENT_TASK_LOOP.md, он может менять только разрешённые блоки:
  - БЛОК 1 — статус задачи
  - БЛОК 2 — AGENT REPORT
  - БЛОК 3 — REVIEW / ANALYSIS, если это предусмотрено задачей
- Агент не должен менять БЛОК 4 / БЛОК 5 / БЛОК 6 без отдельного указания.

--------------------------------------------------------------------------------
ОПЫТ ПО DayZ .layout VIEWER
--------------------------------------------------------------------------------

- DayZ .layout в проекте НЕ XML.
- Нельзя использовать DOMParser.
- Нельзя использовать формат:
  position[] = {x, y}
  size[] = {w, h}
  color[] = {r, g, b, a}

- Реальный формат DayZ .layout в проекте такой:

  FrameWidgetClass QuestMenuRoot {
   ignorepointer 0
   color 0.1765 0.1765 0.1765 0.9
   position 0 0
   size 1 1
   halign center_ref
   valign center_ref
   hexactpos 1
   vexactpos 1
   hexactsize 0
   vexactsize 0
   {
    PanelWidgetClass QuestPanel {
     position 0 0
     size 920 560
     halign center_ref
     valign center_ref
     hexactpos 1
     vexactpos 1
     hexactsize 1
     vexactsize 1
    }
   }
  }

- Начало виджета выглядит так:

  <WidgetClassType> <ElementName> {

- Примеры:

  FrameWidgetClass QuestMenuRoot {
  PanelWidgetClass QuestPanel {
  TextListboxWidgetClass QuestListbox {
  ButtonWidgetClass AcceptButton {
  TextWidgetClass AcceptButtonText {
  MultilineTextWidgetClass DescriptionText {

- Свойства идут отдельными строками:

  position 30 82
  size 330 250
  color 0.0824 0.0824 0.0824 0.96
  text "ВЗЯТЬ КВЕСТ"
  font "gui/fonts/metron22"
  ignorepointer 0
  halign left_ref
  valign top_ref
  hexactpos 1
  vexactpos 1
  hexactsize 1
  vexactsize 1
  "text halign" center
  "text valign" center

- Отдельная строка "{" НЕ создаёт node.
  Это только начало блока children.

- Отдельная строка "}" закрывает текущий блок.

- Для корректного парсинга нужен blockStack, а не простой nodeStack.

- Причина:
  В реальных .layout есть отдельные child-блоки:

  PanelWidgetClass QuestPanel {
   position 0 0
   size 920 560
   {
    TextWidgetClass TitleText {
     ...
    }
   }
  }

  Если делать nodeStack.pop() на каждую "}", можно закрыть не тот node.
  Поэтому stack должен различать:
  - node block
  - group block

- Правильная идея:

  blockStack.push({ kind: "node", node })
  blockStack.push({ kind: "group" })

  При поиске текущего parent нужно брать последний blockStack item с kind === "node".

--------------------------------------------------------------------------------
ОПЫТ ПО DayZ .layout: PIXELS / PERCENT / FINAL RECT
--------------------------------------------------------------------------------

- DayZ layout нельзя считать только как:
  absX = parent.absX + position.x
  absY = parent.absY + position.y

- Правильная модель:

  parentRect + local position + local size + align + exact flags = finalRect

- parseLayout() должен только читать .layout и строить дерево nodes.
- computeFinalRects() должен считать итоговые прямоугольники.
- renderLayout() должен только рисовать готовые finalRect.

- renderLayout НЕ должен сам придумывать координаты.
- renderLayout НЕ должен менять смысл position/size.
- renderLayout НЕ должен содержать layout math.
- renderLayout должен рисовать:

  node.finalRect.x * renderScale
  node.finalRect.y * renderScale
  node.finalRect.w * renderScale
  node.finalRect.h * renderScale

- В проекте принято правило:

  exact = 1 → значение в пикселях
  exact = 0 → значение как доля parentRect / проценты

- То есть:

  hexactpos 1 = position.x в пикселях
  vexactpos 1 = position.y в пикселях
  hexactsize 1 = size.w в пикселях
  vexactsize 1 = size.h в пикселях

  hexactpos 0 = position.x как доля ширины parentRect
  vexactpos 0 = position.y как доля высоты parentRect
  hexactsize 0 = size.w как доля ширины parentRect
  vexactsize 0 = size.h как доля высоты parentRect

- Пример пиксельного элемента:

  TextListboxWidgetClass QuestListbox {
   position 30 82
   size 330 250
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
  }

  Значит:
  localX = 30 px
  localY = 82 px
  width = 330 px
  height = 250 px

- Пример относительного root:

  FrameWidgetClass QuestMenuRoot {
   position 0 0
   size 1 1
   hexactpos 0
   vexactpos 0
   hexactsize 0
   vexactsize 0
  }

  Это НЕ 1px x 1px.
  Это 100% parent / viewport.

  Если viewport = 1280x720:
  finalRect.w = 1280
  finalRect.h = 720

- Для size:

  if hexactsize == 1:
      width = node.size.w
  else:
      width = parentRect.w * node.size.w

  if vexactsize == 1:
      height = node.size.h
  else:
      height = parentRect.h * node.size.h

- Для position:

  if hexactpos == 1:
      localX = node.position.x
  else:
      localX = parentRect.w * node.position.x

  if vexactpos == 1:
      localY = node.position.y
  else:
      localY = parentRect.h * node.position.y

- Для halign:

  left_ref:
      x = parentRect.x + localX

  center_ref:
      x = parentRect.x + (parentRect.w - width) / 2 + localX

  right_ref:
      x = parentRect.x + parentRect.w - width - localX

- Для valign:

  top_ref:
      y = parentRect.y + localY

  center_ref:
      y = parentRect.y + (parentRect.h - height) / 2 + localY

  bottom_ref:
      y = parentRect.y + parentRect.h - height - localY

- Нельзя принудительно clamp/обрезать child внутри parentRect на этапе layout math.

- Причина:
  Элемент может выходить за пределы parent.
  Обрезка/clip — это отдельное поведение контейнера.
  Если насильно менять x/y/w/h, finalRect перестаёт соответствовать layout.

--------------------------------------------------------------------------------
ОПЫТ ПО VIEWPORT
--------------------------------------------------------------------------------

- Center_ref зависит от размера viewport.
- Если viewport отличается от игрового окна, элемент будет центрироваться иначе.
- Поэтому viewer должен иметь viewport presets.

- Полезные presets:
  1000x600
  1280x720
  1366x768
  1600x900
  1920x1080
  2560x1440

- visual-container должен иметь размер:

  width = viewportWidth * renderScale
  height = viewportHeight * renderScale

- renderScale влияет только на отображение в браузере.
- renderScale НЕ должен влиять на finalRect.

--------------------------------------------------------------------------------
ОПЫТ ПО Z-ORDER / ПЕРЕКРЫТИЮ
--------------------------------------------------------------------------------

- Parent должен быть ниже child.
- Children должны быть поверх parent.
- TextWidgetClass должен быть поверх PanelWidgetClass / ButtonWidgetClass.
- Но текст не должен хаотично перекрывать всё без учёта порядка.
- Нужен стабильный порядок:

  depth + sourceOrder + controlled text priority

- При парсинге полезно сохранять:

  node.sourceOrder = allNodes.length
  node.depth = parent ? parent.depth + 1 : 0

- Возможная формула:

  z = depth * 100000 + sourceOrder * 10 + textPriority

- TextWidgetClass / MultilineTextWidgetClass могут иметь небольшой приоритет, чтобы текст был поверх кнопки/панели.

--------------------------------------------------------------------------------
ОПЫТ ПО TEXT WIDGET
--------------------------------------------------------------------------------

- Debug label node.name и игровой текст node.text — разные вещи.
- Debug label нужен только для отладки.
- Игровой текст должен браться из node.text.

- Для TextWidgetClass:
  - отображать node.text, если он есть
  - если node.text нет, node.name показывать только в debug mode
  - background по умолчанию transparent
  - border только debug
  - использовать "text halign"
  - использовать "text valign"
  - использовать font size из metronXX, если возможно

- Для MultilineTextWidgetClass:
  - отображать node.text, если он есть
  - использовать white-space: pre-wrap
  - overflow hidden
  - text-align по "text halign"
  - vertical align по "text valign"

- Нельзя всегда рисовать текст через:
  fixed 12px + transform translate(-50%, -50%)

- Лучше использовать flex-layer внутри finalRect:

  display: flex
  justify-content: center / flex-start / flex-end
  align-items: center / flex-start / flex-end

- Если font содержит metron22:
  font-size примерно 22px

- Если font содержит metron18:
  font-size примерно 18px

- Если font содержит metron16:
  font-size примерно 16px

- Если font содержит metron14:
  font-size примерно 14px

- Если font содержит metron12:
  font-size примерно 12px

--------------------------------------------------------------------------------
ОПЫТ ПО КОДИРОВКАМ .layout
--------------------------------------------------------------------------------

- В проекте разные .layout файлы могут быть сохранены в разных кодировках.

- Примеры:
  QuestJournal.layout может корректно открываться как UTF-8.
  QuestMenu.layout может корректно открываться как Windows-1251.

- Нельзя считать, что все .layout файлы проекта имеют одну кодировку.

- Viewer должен позволять выбирать encoding вручную:
  - UTF-8
  - Windows-1251

- Если русский текст отображается как:
  �����
  или как кракозябры,
  нужно переключить Encoding и перечитать файл.

- Не менять кодировку реального .layout файла автоматически.
- Не пересохранять .layout без отдельной задачи.
- Не пытаться “починить” исходник, если задача только про viewer.

- Для браузерного viewer можно использовать:

  TextDecoder("utf-8")
  TextDecoder("windows-1251")

- При смене Encoding viewer должен заново декодировать выбранный файл из исходных bytes, а не пытаться перекодировать уже испорченную строку.

--------------------------------------------------------------------------------
ОПЫТ ПО DAYZ QUEST MENU / QUEST JOURNAL
--------------------------------------------------------------------------------

- QuestJournal.layout уже отображается достаточно близко к игре после:
  - viewport presets
  - correct finalRect
  - Windows-1251 / UTF-8 selector
  - debug controls
  - text render через flex

- QuestMenu.layout сложнее, потому что в нём больше вложенных панелей:
  - QuestPanel
  - QuestListbox
  - DescriptionPanel
  - DescriptionPanelLabel
  - DescriptionText
  - RoutePanel
  - RoutePanelLabel
  - TriggerRouteListbox
  - DialogPanel
  - DialogPanelLabel
  - DialogText
  - AcceptButton
  - AcceptButtonText
  - CompleteButton
  - CompleteButtonText
  - CloseButton
  - CloseButtonText

- Если QuestMenu выглядит хаотично:
  1. Проверить Encoding.
  2. Выключить Show debug labels.
  3. Оставить Show borders включённым.
  4. Проверить viewport 1280x720.
  5. Запустить Run sanity checks.
  6. Смотреть finalRect конкретных элементов в Inspector.

- Для QuestMenu.layout при viewport 1280x720 sanity-check:

  QuestPanel:
  x = 180
  y = 80
  w = 920
  h = 560

  QuestListbox:
  x = 210
  y = 162
  w = 330
  h = 250

  AcceptButton:
  x = 570
  y = 500
  w = 220
  h = 48

  AcceptButtonText:
  x = 570
  y = 500
  w = 220
  h = 48

- Для QuestJournal.layout при viewport 1280x720 sanity-check:

  QuestJournalPanel:
  x = 180
  y = 80
  w = 920
  h = 560

  QuestListbox:
  x = 210
  y = 162
  w = 330
  h = 386

  DescriptionText:
  x = 570
  y = 162
  w = 500
  h = 386

  CloseButtonText:
  x = 570
  y = 570
  w = 500
  h = 48

--------------------------------------------------------------------------------
ОПЫТ ПО ИНСПЕКТОРУ VIEWER-А
--------------------------------------------------------------------------------

- Inspector нужен, чтобы не гадать по картинке.
- Если элемент кажется “не там”, нужно смотреть:
  - name
  - type
  - parentName
  - position
  - size
  - finalRect
  - halign / valign
  - hexactpos / vexactpos
  - hexactsize / vexactsize
  - text
  - font
  - props

- Element List нужен для быстрого выбора node.
- Клик по DOM-блоку должен выбирать node.
- Клик по списку должен выбирать node.
- Selected node должен подсвечиваться.

- Copy selected info полезен для передачи данных в чат или агенту.

--------------------------------------------------------------------------------
ПАМЯТКА ПО TASK 078 / QUESTMENU UI
--------------------------------------------------------------------------------

После TASK 078 обязательно начать с ручной проверки в игре.

Не начинать новую задачу до проверки:
- Offer NPC;
- Completion NPC;
- Reward NPC;
- QuestJournal.

Главное проверить глазами:
1. Стал ли QuestMenu реально удобнее.
2. Не мешает ли action prompt.
3. Достаточно ли плотный фон.
4. Не обрезается ли критично DescriptionPanel.
5. Не обрезается ли критично DialogPanel.
6. Нет ли лишнего [F].
7. Нет ли лишнего “Награда здесь” в левом списке.
8. Не сломалась ли кириллица.
9. Работают ли кнопки.

Если всё хорошо:
TASK 078 закрыть окончательно.

Если длинные тексты всё ещё обрезаются:
следующая задача TASK 079:
полноценная прокрутка DescriptionPanel / DialogPanel.

Если action prompt всё ещё поверх интерфейса:
следующая отдельная задача:
скрывать/подавлять action prompt на время открытого QuestUI
или делать более плотный overlay.

Если кириллица сломалась:
следующая отдельная задача:
проверить кодировку layout/c/client/ui/json и привести к единому формату.

--------------------------------------------------------------------------------
ВАЖНОЕ ПРАВИЛО
--------------------------------------------------------------------------------

QuestMenu layout и QuestUI.c надо проверять вместе.

Не считать, что всё решается только .layout.
Если проблема в размере/позиции/цвете — это layout.
Если проблема в тексте/префиксах/[F]/маппинге Description/Dialog — это QuestUI.c.
Если проблема в данных — это JSON/QuestData/ServerManager.
Если проблема в сохранении прогресса — это server profile progress.

--------------------------------------------------------------------------------
ПОТЕНЦИАЛЬНЫЕ СЛЕДУЮЩИЕ ЗАДАЧИ
--------------------------------------------------------------------------------

TASK 079 — Полноценная прокрутка DescriptionPanel / DialogPanel.
TASK 080 — Подавление action prompt при открытом QuestUI.
TASK 081 — Финальная проверка кодировок Quest UI / Journal UI / layout.
TASK 082 — Полировка QuestJournal после проверки в игре.

--------------------------------------------------------------------------------
ОПЫТ ПО PLACEHOLDER-ТЕКСТАМ В МЕНЮ
--------------------------------------------------------------------------------

- Некоторые тексты в .layout могут быть не финальным игровым текстом, а placeholder / default text.

- Пример:
  "Выберите квест из списка"

- Такой текст может лежать прямо в TextWidgetClass / MultilineTextWidgetClass.
- Это нормально, если он находится внутри DescriptionPanel / DescriptionText.
- Логика:
  пока квест не выбран, DescriptionText показывает подсказку.

- Не надо сразу считать это ошибкой позиционирования.
- Нужно проверить:
  - какой node содержит этот text
  - какой у node parent
  - какой finalRect
  - заменяется ли этот текст игровым кодом после выбора квеста

- Если текст находится в DescriptionText внутри DescriptionPanel — это ожидаемо.
- Если текст находится в неправильном parent или finalRect — тогда это проблема layout.

--------------------------------------------------------------------------------
ОПЫТ ПО READ-ONLY РЕЖИМУ
--------------------------------------------------------------------------------

- DayZ layout viewer должен быть read-only.
- Он может:
  - читать выбранный .layout
  - парсить его
  - строить visual render
  - показывать inspector
  - копировать selected info в clipboard
  - запускать sanity checks

- Он НЕ должен:
  - сохранять .layout
  - форматировать .layout
  - изменять .layout
  - менять кодировку исходного файла
  - генерировать layout-код
  - делать export
  - писать в файлы мода
  - менять JSON

--------------------------------------------------------------------------------
ОПЫТ ПО РАБОТЕ С РЕАЛЬНЫМИ ФАЙЛАМИ ДЛЯ ПРОВЕРКИ
--------------------------------------------------------------------------------

- Реальные файлы для read-only проверки viewer-а:

  P:\Silver_77_Quests\Silver_77_Quests_Client\gui\QuestMenu.layout
  P:\Silver_77_Quests\Silver_77_Quests_Client\gui\QuestJournal.layout

- Эти файлы можно использовать как input для viewer.
- Эти файлы нельзя менять без отдельной задачи.
- Эти файлы нельзя пересохранять ради исправления кодировки без отдельной задачи.
- Если нужно проверить кодировку — делать это только через Encoding selector viewer-а.

--------------------------------------------------------------------------------
ОПЫТ ПО ТЕКУЩЕМУ СОСТОЯНИЮ VIEWER-А
--------------------------------------------------------------------------------

- После TASK 069 viewer стал рабочим инструментом диагностики:
  - Encoding selector
  - viewport presets
  - renderScale
  - Show debug labels
  - Show borders
  - Show grid
  - element list
  - inspector panel
  - selected node highlight
  - copy selected info
  - sanity checks
  - blockStack parser для node/group blocks

- Viewer уже можно использовать для предметного анализа меню.
- Дальнейшие задачи по меню желательно делать через inspector:
  не “кажется съехало”, а:
  - какой node
  - какой parent
  - какой finalRect
  - какой expected finalRect
  - какой text
  - какая кодировка
  
  
--------------------------------------------------------------------------------
ОПЫТ ПО QUEST JSON EDITOR / ВИЗУАЛЬНАЯ КОНЦЕПЦИЯ / STACK RULES
--------------------------------------------------------------------------------

Визуальная концепция Quest JSON Editor в целом считается правильной.

Редактор должен быть удобной оболочкой над реальным JSON-контрактом мода, а не отдельной схемой данных, которая расходится с runtime-логикой.

Главное правило:
мод является источником правды.
редактор должен формировать JSON строго под то, что реально читает мод.

--------------------------------------------------------------------------------
ПРАВИЛЬНАЯ МОДЕЛЬ РОЛЕЙ
--------------------------------------------------------------------------------

Текущая концепция NPC Flow считается правильной:

1. Offer
- место / NPC, где игрок берёт квест
- стартовый диалог
- условия доступности
- может выдавать стартовые предметы через giveItems
- не должен быть финальным закрытием квеста

2. Completion
- промежуточный этап / сдача части задачи
- может быть 0, 1 или несколько
- может быть на другом NPC / trigger
- может иметь свой triggerActions[].dialogText
- может принимать objectives
- может выдавать промежуточную награду через triggerActions[].rewards

3. Reward
- финальное закрытие квеста
- обычно исходный NPC, но может быть другой trigger
- выдаёт финальную награду
- закрывает квест

Текущая runtime-модель мода:
- Offer и Reward фактически single-trigger роли
- Completion может быть несколько
- это совпадает с желаемой логикой:
  один Offer → несколько Completion → один Reward

--------------------------------------------------------------------------------
РЕАЛЬНЫЙ JSON-КОНТРАКТ
--------------------------------------------------------------------------------

Не вводить ложные поля.

В контракте нет отдельного поля:

requiredItems

Его роль выполняет:

quests[].objectives[]

Особенно:
objectives[] с type == "item"

Правильная терминология:
- Objectives
- Цели
- Цели: предметы
- Что нужно принести / сдать

Неправильная терминология, если она подразумевает отдельное JSON-поле:
- requiredItems[]
- отдельный блок requiredItems в JSON

--------------------------------------------------------------------------------

В контракте нет отдельного поля:

dialogue

Реальный текст NPC хранится здесь:

triggerActions[].dialogText

Правильная терминология:
- Dialog Text
- Текст диалога NPC
- triggerActions[].dialogText

Неправильная терминология, если она подразумевает отдельное JSON-поле:
- dialogue
- dialogueText как отдельная сущность вне triggerActions

--------------------------------------------------------------------------------
РАЗДЕЛЕНИЕ DESCRIPTION И DIALOG
--------------------------------------------------------------------------------

В UI мода и редакторе нужно строго разделять смысл:

Description / DescriptionPanel:
- описание квеста
- смысл задачи
- цели
- прогресс
- что нужно принести
- что уже сдано
- награды
- требования
- статус квеста

Dialog / DialogPanel:
- что говорит NPC
- текст текущего action-блока
- triggerActions[].dialogText
- контекст разговора Offer / Completion / Reward

Правильный маппинг:

quest.description / objectives / rewards / progress / status
→ DescriptionPanel / DescriptionText

triggerActions[].dialogText
→ DialogPanel / DialogText

Нельзя использовать triggerActions[].dialogText как основное описание квеста в DescriptionPanel.

Если нужен диалоговый журнал, он может быть отдельной частью DialogPanel, но не должен подменять DescriptionPanel.

--------------------------------------------------------------------------------
СПРАВОЧНИК СТЕКОВ
--------------------------------------------------------------------------------

Справочник стеков item-stack-rules.json является важной частью редактора.

Он нужен, чтобы редактор мог правильно считать количество предметов с учётом стеков.

Это особенно важно для серверов с модами, где размер стака может отличаться от vanilla DayZ.

Примеры:
- патроны
- деньги
- дрова
- палки
- вода
- топливо
- любые модовые предметы с изменённым размером стака

Правило:
не удалять и не упрощать справочник стеков.
не заменять его жёстко прописанными vanilla-значениями.
не считать, что quantity всегда означает количество отдельных физических предметов.

Для item-objectives учитывать:
- quantity
- useItemQuantity
- allowPartialTurnIn
- removeOnComplete
- item-stack-rules.json

Смысл:
редактор должен помогать пользователю правильно рассчитать требования к предметам до запуска игры.

--------------------------------------------------------------------------------
ЧТО НЕ ЛОМАТЬ
--------------------------------------------------------------------------------

Не переделывать визуальную концепцию редактора с нуля без отдельной задачи.

Считать правильными направлениями:
- NPC Flow
- Offer / Completion / Reward
- Objectives вместо requiredItems
- triggerActions[].dialogText вместо dialogue
- Rewards как отдельный блок наград
- Stack Rules / справочник стеков
- визуальное разделение блоков по ролям

Не добавлять новые JSON-поля без отдельного решения:
- requiredItems[]
- dialogue
- dialogueText вне triggerActions[]
- rewardBlock
- completionBlock
- offerBlock

Если нужно расширять функционал, сначала проверить:
1. поддерживает ли это мод;
2. где это читается в client/server коде;
3. как это должно выглядеть в JSON;
4. только потом добавлять UI в редактор.

--------------------------------------------------------------------------------
ПРАВИЛО ПО QUEST JSON / РАЗРАБОТКА И СЕРВЕР
--------------------------------------------------------------------------------

Основной рабочий JSON для разработки квестов находится здесь:

P:\Silver_77_Quests\JSON_Quvest\Silver_77_Quests.json

Это канонический файл, который формирует Quest JSON Editor.

Папка:

P:\Silver_77_Quests\JSON_Quvest

является рабочей папкой разработки редактора и квестов.

Пользователь сам вручную копирует актуальный JSON из этой папки на реальный игровой сервер, когда считает изменения готовыми.

Важно:
Локальный DayZ profile на машине разработки НЕ считать production-сервером.

Например путь вида:

C:\Users\REX\AppData\Local\DayZ\Silver_77_Quests\Silver_77_Quests.json

может быть только локальной тестовой копией, но не главным серверным источником правды, если пользователь явно не сказал обратное.

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ АГЕНТОВ
--------------------------------------------------------------------------------

Агентам запрещено автоматически копировать quest JSON в server profile или в любую папку сервера без явной команды пользователя.

Нельзя считать, что локальный DayZ profile является настоящим игровым сервером.

Любой перенос JSON на сервер выполняет пользователь вручную, если он явно не дал агенту другую задачу.

Правильная схема:

1. Редактор квестов сохраняет JSON в:
   P:\Silver_77_Quests\JSON_Quvest\Silver_77_Quests.json

2. Этот файл считается актуальной dev-версией квестов.

3. Пользователь проверяет/правит его.

4. Пользователь сам копирует этот JSON на игровой сервер.

5. Серверный profile / production-папки не трогать без прямого разрешения.

--------------------------------------------------------------------------------
ЧТО МОЖНО ДЕЛАТЬ
--------------------------------------------------------------------------------

Можно:
- читать JSON из P:\Silver_77_Quests\JSON_Quvest;
- проверять его структуру;
- валидировать его;
- сравнивать с другими копиями;
- сообщать, что серверная копия отличается;
- предлагать пользователю вручную перенести файл.

Нельзя без прямой команды:
- перезаписывать JSON в server profile;
- перезаписывать JSON на реальном сервере;
- удалять серверный прогресс;
- чистить server profile;
- считать локальный profile production-источником;
- вшивать JSON в PBO.

--------------------------------------------------------------------------------
ПРАВИЛЬНАЯ ФОРМУЛИРОВКА В ОТЧЁТАХ
--------------------------------------------------------------------------------

Если агент видит, что актуальный JSON находится в JSON_Quvest, писать так:

"Актуальная dev-версия JSON находится в:
P:\Silver_77_Quests\JSON_Quvest\Silver_77_Quests.json.
Для production-сервера пользователь переносит этот файл вручную."

Не писать:
"JSON синхронизирован на сервер",
если агент не работал с реальным серверным путём, явно указанным пользователем.

Если была синхронизация только в локальный DayZ profile, писать:

"JSON был скопирован только в локальный DayZ profile для теста. Это не считается переносом на реальный игровой сервер."

================================================================================
# КОНЕЦ ФАЙЛА
================================================================================