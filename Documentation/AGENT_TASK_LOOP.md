# AGENT TASK LOOP

Файл для управления задачами агента.

Главная идея:
- ChatGPT ставит задачу
- пользователь вставляет задачу в этот файл
- агент читает задачу
- агент возвращает отчёт в чат
- пользователь присылает отчёт ChatGPT
- ChatGPT даёт проверку и следующую задачу
- пользователь вручную обновляет этот файл

================================================================================
# 🧭 БЛОК 0 — КАК ПОЛЬЗОВАТЬСЯ ФАЙЛОМ
================================================================================

Этот файл — рабочий журнал взаимодействия:

- пользователь
- ChatGPT
- Kiro / агент
- проект `Silver_77_Quests`

Главная цель файла:
- хранить текущую задачу
- хранить последний отчёт агента
- хранить последний review ChatGPT
- хранить правила работы
- хранить историю задач
- хранить пользовательские подсказки по UI и смыслу

--------------------------------------------------------------------------------
ОБЩИЙ ПРОЦЕСС:
--------------------------------------------------------------------------------

1. ChatGPT формирует задачу
2. Пользователь вставляет задачу в БЛОК 1
3. Агент читает БЛОК 1
4. Агент выполняет задачу
5. Агент возвращает отчёт в чат
6. Пользователь отправляет отчёт ChatGPT
7. ChatGPT проверяет отчёт
8. ChatGPT даёт:
   - чистый AGENT REPORT для БЛОКА 2
   - REVIEW для БЛОКА 3
   - следующую задачу для БЛОКА 1
9. Пользователь вручную обновляет файл
10. Git commit / push делает только пользователь

--------------------------------------------------------------------------------
КТО ЗА ЧТО ОТВЕЧАЕТ:
--------------------------------------------------------------------------------

Пользователь:
- принимает финальные решения
- проверяет результат в игре
- контролирует Git
- делает commit / push
- даёт визуальные и смысловые подсказки

ChatGPT:
- ставит задачи
- формирует безопасные инструкции
- проверяет отчёты агента
- пишет REVIEW
- следит за архитектурой, рисками и порядком задач
- помогает вести этот файл

Агент / Kiro:
- читает текущую задачу
- выполняет только указанную задачу
- возвращает отчёт в чат
- не пишет отчёт в этот файл
- не делает commit / push

--------------------------------------------------------------------------------
КАКИЕ БЛОКИ ОБНОВЛЯТЬ:
--------------------------------------------------------------------------------

Пользователь обычно редактирует только:

1. БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
2. БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
3. БЛОК 3 — REVIEW CHATGPT
4. БЛОК 5 — ИСТОРИЯ ЗАДАЧ
5. БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ

БЛОК 4 и БЛОК 4.1 менять только если меняются правила работы.

--------------------------------------------------------------------------------
КАК ЗАМЕНЯТЬ СОДЕРЖИМОЕ:
--------------------------------------------------------------------------------

В БЛОКЕ 1 заменять только текст между:

## НАЧАЛО ЗАДАЧИ

и

## КОНЕЦ ЗАДАЧИ

В БЛОКЕ 2 заменять только текст между:

## НАЧАЛО ОТЧЁТА

и

## КОНЕЦ ОТЧЁТА

В БЛОКЕ 3 заменять только текст между:

## НАЧАЛО REVIEW

и

## КОНЕЦ REVIEW

Сами строки `НАЧАЛО` и `КОНЕЦ` не удалять.

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ АГЕНТА:
--------------------------------------------------------------------------------

Агент НЕ записывает отчёт в этот файл.

Агент возвращает отчёт в чат.

Пользователь вручную переносит чистый отчёт в БЛОК 2 после проверки ChatGPT.

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ GIT:
--------------------------------------------------------------------------------

Git контролирует только пользователь.

Агенту запрещено:
- делать `git commit`
- делать `git push`
- создавать ветки
- делать `git reset`
- делать `git clean`
- делать `git checkout`

Агент может читать Git-статус только если это явно указано в задаче.

--------------------------------------------------------------------------------
ПРАВИЛО ПРОВЕРКИ АКТУАЛЬНОЙ ВЕРСИИ ДЛЯ CHATGPT:
--------------------------------------------------------------------------------

Перед анализом `Documentation/AGENT_TASK_LOOP.md` ChatGPT должен:

1. Сначала проверить последний коммит ветки `main`
2. Сообщить пользователю:
   - короткий SHA коммита
   - название коммита
3. Читать `Documentation/AGENT_TASK_LOOP.md` именно из этого коммита, а не из кешированной версии `main`
4. В начале ответа писать:

   `Читаю коммит: <SHA> — <название коммита>`

5. Если пользователь видит, что коммит неактуальный, он сообщает актуальное название или SHA последнего коммита

--------------------------------------------------------------------------------
ТЕКУЩАЯ АКТУАЛЬНАЯ АРХИТЕКТУРА ПРОЕКТА:
--------------------------------------------------------------------------------

Проект `Silver_77_Quests` сейчас развивается как split Client / Server.

Актуальная клиентская часть:

`Silver_77_Quests_Client/`

Актуальная серверная часть:

`Silver_77_Quests_Server/`

Старые / резервные части не считать актуальными без отдельной задачи:

- корневая папка `scripts/`
- корневая папка `gui/`
- корневые `config.cpp` и `mod.cpp`
- `SplitMods/`
- `Support/`

--------------------------------------------------------------------------------
ВАЖНО:
--------------------------------------------------------------------------------

Если задача связана с UI, layout-файлами, кириллицей или текстами, агент должен учитывать:

- БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
- БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ

Если задача связана с кодом, агент должен работать только в явно указанных файлах.

Если задача аналитическая, агент ничего не изменяет.
================================================================================

================================================================================
# 🟦 БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ЗАДАЧИ
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 037 — Создание спецификации логики квестов

Цель:
Создать отдельный документ, который описывает правильную логику квестовой системы, чтобы агенты и редактор квестов не путали Offer, Completion, Reward и requiredQuestIds.

Где работать:
- `Documentation/QUEST_LOGIC_SPEC.md`

Что сделать:
1. Создать файл `Documentation/QUEST_LOGIC_SPEC.md`
2. Заполнить его спецификацией логики квестов
3. Описать:
   - главную модель Quest
   - `requiredQuestIds`
   - Offer
   - Completion
   - Reward
   - простые квесты без Completion
   - цепочные квесты с Completion
   - изоляцию квестов друг от друга
   - правила для редактора JSON
   - правила для игрового кода
4. Не менять другие файлы
5. Вернуть краткий отчёт в чат

Текст спецификации:

# QUEST LOGIC SPEC

Документ описывает базовую логику квестовой системы `Silver_77_Quests`.

Этот документ является источником истины для:
- игрового кода
- JSON-квестов
- редактора квестов
- задач для агентов

---

## 1. Главная модель

Каждый квест является независимой сущностью.

Один квест не должен случайно блокировать, завершать или изменять другой квест.

Каждый квест может иметь:

- `requiredQuestIds[]` — внешние зависимости от других квестов
- Offer — обязательный этап взятия квеста
- Completion[] — опциональные промежуточные этапы
- Reward — обязательный этап закрытия квеста и выдачи награды

Общая модель:

Quest
- requiredQuestIds[]
- Offer
- Completion[]
- Reward

---

## 2. requiredQuestIds

`requiredQuestIds` описывает внешние зависимости между разными квестами.

Если `requiredQuestIds` пустой:
- у квеста нет зависимостей
- квест может быть виден и доступен без прохождения других квестов

Если `requiredQuestIds` не пустой:
- все указанные квесты должны иметь статус `completed`
- только после этого текущий квест может стать видимым и доступным для взятия

Важно:
- `requiredQuestIds` влияет только на доступность квеста
- `requiredQuestIds` НЕ является Completion-этапом
- `requiredQuestIds` НЕ должен использоваться для логики сдачи текущего квеста

Пример:
- `quest_15` имеет `requiredQuestIds: ["quest_12"]`
- значит `quest_15` не должен быть доступен, пока `quest_12` не завершён

---

## 3. Offer

Offer — обязательный этап.

Offer определяет, где игрок может взять квест.

Offer:
- не закрывает квест
- не выдаёт финальную награду
- не считается Completion-этапом
- только переводит квест в состояние `active`

Пример:
- игрок берёт квест у Ворона
- `offerTriggerIds` содержит `hunter_trigger`

---

## 4. Completion

Completion — опциональный этап.

Completion используется только если у квеста есть промежуточные обязательные этапы.

Если Completion-этапов нет:
- Completion считается отсутствующим
- количество обязательных Completion-этапов равно 0
- Reward может закрыть квест после выполнения основных условий/objectives

Если Completion-этапы есть:
- каждый Completion-блок относится только к текущему questId
- каждый Completion-блок должен быть выполнен
- пока выполнены не все Completion-блоки, Reward не должен закрывать квест

Логика:
- `requiredCompletionCount` = количество Completion-блоков
- `completedCompletionCount` = количество выполненных Completion-блоков
- `remainingCompletionCount = requiredCompletionCount - completedCompletionCount`
- Reward доступен только если `remainingCompletionCount == 0`

Важно:
- пустой `completionTriggerIds` означает, что Completion-этапов нет
- пустой `completionTriggerIds` НЕ должен означать, что квест нельзя сдавать нигде
- если Completion отсутствует, Reward должен иметь право закрыть квест

---

## 5. Reward

Reward — обязательный этап.

Reward определяет, где квест закрывается и где игрок получает награду.

Reward:
- закрывает квест
- выдаёт награду
- переводит квест в состояние `completed`

Если Completion-этапов нет:
- Reward может закрыть квест напрямую после выполнения objectives

Если Completion-этапы есть:
- Reward может закрыть квест только после выполнения всех Completion-этапов

Если Completion-этапы ещё не выполнены:
- Reward должен быть недоступен
- UI должен объяснить, что сначала нужно выполнить промежуточные этапы

---

## 6. Простые квесты без Completion

Простой квест:

Offer -> Reward

Пример:
- взять квест у Ворона
- принести предметы Ворону
- получить награду у Ворона

Для такого квеста:
- Offer обязателен
- Completion отсутствует
- Reward обязателен
- `completionTriggerIds` может быть пустым
- `rewardTriggerIds` должен указывать, где закрывать квест

Ожидаемое поведение:
- квест можно взять через Offer
- после выполнения objectives квест можно закрыть через Reward
- пустой Completion не блокирует Reward

---

## 7. Цепочные квесты с Completion

Цепочный квест:

Offer -> Completion[] -> Reward

Пример:
- взять квест у Ворона
- выполнить этап у Распутина
- вернуться к Ворону за наградой

Для такого квеста:
- Offer обязателен
- Completion включён
- должен быть один или несколько Completion-блоков
- Reward обязателен

Ожидаемое поведение:
- квест можно взять через Offer
- каждый Completion-блок должен быть выполнен
- Reward недоступен, пока не выполнены все Completion-блоки
- после выполнения всех Completion-блоков Reward закрывает квест

---

## 8. Изоляция квестов

Каждый questId должен обрабатываться отдельно.

Квесты не должны блокировать друг друга автоматически.

Один NPC или trigger может иметь несколько questIds.

Порядок `questIds` у trigger:
- может влиять на порядок отображения
- не должен ломать принятие квеста
- не должен ломать сдачу квеста
- не должен создавать скрытую зависимость между квестами

Связь между разными квестами допускается только через:
- `requiredQuestIds`

Если квест A должен быть завершён перед квестом B:
- это должно быть явно указано в `requiredQuestIds` квеста B

---

## 9. Правила для JSON

В JSON должны быть явно и корректно заполнены:

- `requiredQuestIds`
- `offerTriggerIds`
- `completionTriggerIds`
- `rewardTriggerIds`
- `triggerActions`
- `objectives`
- `rewards`

Правила:
- `requiredQuestIds: []` означает, что внешних зависимостей нет
- `completionTriggerIds: []` означает, что промежуточных Completion-этапов нет
- `rewardTriggerIds` должен быть заполнен всегда
- `offerTriggerIds` должен быть заполнен всегда
- если Completion-этапы есть, они должны быть связаны с текущим questId
- если Completion-этапов нет, Reward должен закрывать квест напрямую

---

## 10. Правила для редактора квестов

Редактор должен явно разделять:

- Offer
- Completion
- Reward
- Required completed quests

Русское название для `requiredQuestIds`:
- “Требуемые завершённые квесты”

Описание для редактора:
- если список пустой, квест доступен без прохождения других квестов
- если список заполнен, все указанные квесты должны быть завершены, иначе этот квест не будет доступен

Completion в редакторе:
- должен быть опциональным
- если Completion выключен, Completion-блоки не создаются
- если Completion включён, должен быть создан один или несколько Completion-блоков
- каждый Completion-блок должен быть связан с текущим questId

Reward в редакторе:
- обязателен
- должен быть создан всегда
- если Completion выключен, Reward закрывает квест напрямую
- если Completion включён, Reward закрывает квест только после всех Completion-блоков

---

## 11. Правила для игрового кода

Игровой код должен различать:

- Completion отсутствует
- Completion есть, но ещё не выполнен

Это разные состояния.

Правильная логика:

Если `completionTriggerIds` пустой:
- Completion отсутствует
- Reward может закрыть квест после выполнения objectives

Если `completionTriggerIds` не пустой:
- Completion есть
- Reward должен проверить, выполнены ли все Completion-этапы

Игровой код не должен считать пустой `completionTriggerIds` ошибкой.

Игровой код не должен блокировать Reward только потому, что `completionTriggerIds` пустой.

---

## 12. Короткое правило

Главное правило:

Offer — взять квест.
Completion — выполнить промежуточные этапы, если они есть.
Reward — закрыть квест и выдать награду.
requiredQuestIds — открыть доступ к квесту только после завершения других квестов.

Важно:
Completion относится к этапам внутри текущего квеста.
requiredQuestIds относится к зависимостям между разными квестами.

Важно:
Пустой Completion не блокирует Reward.

Важно:
Пустой requiredQuestIds не блокирует доступ к квесту.

Важно:
Квесты не должны случайно переплетаться между собой.

Важно:
Все связи между квестами должны быть явными.

Важно:
Если квест зависит от другого квеста, это должно быть указано в `requiredQuestIds`.

Критерии готовности:
- создан `Documentation/QUEST_LOGIC_SPEC.md`
- документ содержит описанную модель Offer / Completion / Reward / requiredQuestIds
- другие файлы не изменены
- отчёт возвращён в чат

Важно:
- изменить только `Documentation/QUEST_LOGIC_SPEC.md`
- не менять код
- не менять JSON
- не менять редактор квестов
- Git не трогать
- commit/push не делать

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ЗАДАЧИ
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

### DONE:
- Проверены все предметные упоминания картошки в `JSON_Quvest/Silver_77_Quests.json`
- Подтверждено, что картошка везде использует `PotatoSeed`
- Изменён порядок квестов у `hunter_trigger`
- `quest_hunter_1` теперь стоит первым
- `quest_hunter_2` теперь стоит вторым

### CHANGED FILES:
- `JSON_Quvest/Silver_77_Quests.json`

### DIFF:
- У `hunter_trigger.questIds` изменён порядок:
  - было: `quest_hunter_2`, `quest_hunter_1`
  - стало: `quest_hunter_1`, `quest_hunter_2`

### PROBLEMS:
- Нет

### QUESTIONS:
- Нет

### ВЫВОД:
- JSON по картошке уже был корректен
- Подозрительный порядок квестов Ворона исправлен
- Следующий шаг — собрать моды и проверить Ворона, Рыбака и квесты с `PotatoSeed` в игре

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟨 БЛОК 3 — REVIEW CHATGPT
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 034 REVIEW

Статус: принято.

Проверка выполнена корректно:
- JSON проверен на предметные упоминания картошки
- подтверждено, что используется `PotatoSeed`
- у `hunter_trigger` исправлен порядок `questIds`
- теперь `quest_hunter_1` идёт перед `quest_hunter_2`
- изменён только `JSON_Quvest/Silver_77_Quests.json`

Вывод:
- дефолтный JSON приведён к ожидаемому состоянию для проверки
- теперь можно тестировать Ворона и Рыбака в игре
- если после сохранения через редактор порядок снова изменится, нужна отдельная задача на редактор квестов

Следующая задача: TASK 035.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------


================================================================================
# 🟥 БЛОК 4 — ПРАВИЛА ДЛЯ АГЕНТА
================================================================================

Агент должен:

1. Читать задачу только из БЛОКА 1
2. Выполнять только текущую задачу
3. Не менять архитектуру без отдельного указания
4. Не делать рефакторинг без задачи
5. Не удалять файлы без отдельной задачи
6. Не переносить файлы без отдельной задачи
7. Не переименовывать файлы без отдельной задачи
8. Если задача аналитическая — ничего не изменять
9. Если задача требует правки — менять только явно указанные файлы
10. Отчёт возвращать в чат
11. Отчёт в файл не записывать
12. Соблюдать БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
13. Учитывать БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ, если задача связана с UI, layout-файлами или текстами
14. Git контролирует пользователь
15. Агент НЕ делает `git commit`
16. Агент НЕ делает `git push`
17. Агент НЕ создаёт ветки без отдельной задачи
18. Агент НЕ выполняет `git reset`, `git clean`, `git checkout` без отдельного разрешения
19. Агент может читать Git-статус только если это явно указано в задаче
20. Отчёт должен быть кратким: только факты, без длинных объяснений, если подробный анализ не запрошен отдельно

--------------------------------------------------------------------------------
ФОРМАТ ОТЧЁТА В ЧАТ:
--------------------------------------------------------------------------------

Отчёт должен быть коротким и практичным.

Не писать длинные рассуждения, если задача отдельно не просит подробный анализ.

AGENT REPORT

DONE:
- что сделано

CHANGED FILES:
- какие файлы изменены

DIFF:
- кратко что изменилось

PROBLEMS:
- только реальные проблемы

QUESTIONS:
- только реальные вопросы

--------------------------------------------------------------------------------
ВАЖНО ДЛЯ ОТЧЁТА:
--------------------------------------------------------------------------------

Если всё прошло нормально, не расписывать длинные объяснения.

Если задача аналитическая — дать вывод кратко, но достаточно конкретно.

Если задача связана с риском, кодировкой, удалением файлов, Git или структурой проекта — обязательно указать риск в PROBLEMS.

Если нужен подробный анализ, пользователь или ChatGPT укажет это отдельно.

================================================================================
# 🟪 БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
================================================================================

ВАЖНО:
В проекте используется русскоязычный текст. Кириллицу нельзя повреждать.

Некоторые файлы могут быть сохранены не в UTF-8, а в CP1251 или другой Windows-кодировке.

Особенно осторожно работать с файлами:

- `.layout`
- `.json`
- `.md`
- `.c`
- `.cpp`
- `.txt`
- `.xml`

--------------------------------------------------------------------------------
ПРАВИЛА:
--------------------------------------------------------------------------------

1. НЕ перекодировать файлы без отдельной задачи
2. НЕ сохранять файл заново, если задача только аналитическая
3. НЕ заменять кириллицу на `????`, `�����` или транслит
4. НЕ “чинить” русские строки автоматически
5. Перед изменением файла с кириллицей нужно определить его текущую кодировку
6. Если кириллица отображается как `�����`, это НЕ значит, что текст надо переписать
7. Если кодировка непонятна — остановиться и написать в PROBLEMS
8. Любые правки текста с кириллицей делать только после отдельного подтверждения

--------------------------------------------------------------------------------
ОСОБО ОПАСНЫЕ ФАЙЛЫ:
--------------------------------------------------------------------------------

- `Silver_77_Quests_Client/gui/QuestMenu.layout`
- `Silver_77_Quests_Client/gui/QuestJournal.layout`
- `gui/QuestMenu.layout`
- `gui/QuestJournal.layout`
- `JSON_Quvest/*.json`
- `Documentation/*.md`

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ ОТЧЁТА:
--------------------------------------------------------------------------------

Если задача затрагивает файл с кириллицей, агент обязан указать в отчёте:

- была ли обнаружена кириллица
- отображается ли она нормально
- есть ли риск кодировки
- менялся ли файл
- сохранялся ли файл заново

================================================================================
# ⬛ БЛОК 5 — ИСТОРИЯ ЗАДАЧ
================================================================================

Новые завершённые задачи добавлять вниз.

--------------------------------------------------------------------------------

- TASK 001 — Проверка зависимости клиент/сервер — принято
- TASK 002 — Проверка соответствия имени клиентского мода в CfgPatches — принято
- TASK 003 — Инвентаризация дубликатов монолитной и split-версии — принято с уточнением
- TASK 004 — Проверка содержимого резервных и старых папок без изменений — принято
- TASK 005 — Проверка корневых config.cpp и mod.cpp — принято
- TASK 006 — Проверка корневой папки gui — принято с замечанием по кодировке
- TASK 007 — Проверка кодировки файлов с кириллицей — принято
- TASK 008 — Поиск нормальной версии QuestMenu.layout — принято
- TASK 009 — Сравнение QuestMenu.layout перед восстановлением — принято
- TASK 010 — Составление карты повреждённых строк QuestMenu.layout — принято
- TASK 011 — Попытка восстановления text-строк — остановлено безопасно из-за риска кодировки
- TASK 012 — Выбор безопасного способа восстановления QuestMenu.layout — принято
- TASK 013 — Подготовка безопасного скрипта восстановления QuestMenu.layout — принято предварительно
- TASK 014 — Code review скрипта восстановления QuestMenu.layout — принято
- TASK 015 — Финальная проверка кода restore_questmenu_text.py перед запуском — принято
- TASK 016 — Ручной запуск restore_questmenu_text.py и проверка результата — принято
- TASK 017 — Проверка восстановленного QuestMenu.layout без изменений — принято
- TASK 018 — Уборка временных Python/cache/backup файлов после восстановления — принято
- TASK 019 — Проверка результата восстановления QuestMenu.layout в проекте без изменений — принято
- TASK 020 — Диагностика CloseButton и CloseButtonText без изменений — принято
- TASK 021 — Подключение CloseButtonText в QuestUI.c — принято
- TASK 022 — Сравнение стилей CloseButton с рабочими кнопками — принято
- TASK 023 — Включение CloseButton и CloseButtonText в UpdateButtons — принято
- TASK 024 — Ручная проверка CloseButton в игре — принято



================================================================================
# ⬛ БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ
================================================================================


Этот блок содержит ручные подсказки от пользователя по UI, текстам и визуальному поведению.

Агент должен учитывать этот блок, если задача связана с:
- UI
- layout-файлами
- текстами
- визуальными элементами
- восстановлением кириллицы

--------------------------------------------------------------------------------
ПОДСКАЗКИ:
--------------------------------------------------------------------------------

- В `QuestMenu.layout` нижняя серая кнопка / полоса в диалоговом окне должна иметь текст:
  `Закрыть рот и уйти`

- По проверке в игре нижняя серая кнопка диалога раньше была видна только при наведении мышки.
  Без наведения кнопка почти или полностью не была видна.
  Текст `Закрыть рот и уйти` на ней не отображался.
  Нужно было сравнить стили и структуру `CloseButton` / `CloseButtonText` с рабочими кнопками `AcceptButton` / `AcceptButtonText` и `CompleteButton` / `CompleteButtonText`.

- Если агент не уверен в назначении UI-элемента, он должен указать это в `QUESTIONS`, а не исправлять текст самостоятельно.

- По проверке в игре после TASK 023 нижняя кнопка диалога работает:
  текст `Закрыть рот и уйти` отображается,
  кнопка видна,
  кнопка закрывает диалог.

- Причина проблемы была в `QuestUI.c`:
  `CloseButton` и `CloseButtonText` не были полноценно обработаны в `UpdateButtons()`.

- Для похожих UI-проблем сначала сравнивать проблемный элемент с рабочими аналогами:
  `AcceptButton` / `AcceptButtonText`,
  `CompleteButton` / `CompleteButtonText`,
  `CloseButton` / `CloseButtonText`,
  а затем проверять обработку этих элементов в коде.

================================================================================
# КОНЕЦ ФАЙЛА
================================================================================