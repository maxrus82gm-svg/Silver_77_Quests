# AGENT TASK LOOP

Файл для управления задачами агента.

Главная идея:
- ChatGPT ставит задачу
- пользователь вставляет задачу в этот файл
- агент читает задачу
- агент возвращает отчёт в чат
- пользователь присылает отчёт ChatGPT
- ChatGPT даёт проверку и следующую задачу
- пользователь вручную обновляет этот файл

================================================================================
# 🧭 БЛОК 0 — КАК ПОЛЬЗОВАТЬСЯ ФАЙЛОМ
================================================================================

================================================================================
# 🧭 БЛОК 0 — КАК ПОЛЬЗОВАТЬСЯ ФАЙЛОМ
================================================================================

Этот файл — рабочий журнал взаимодействия:

- пользователь
- ChatGPT
- Kiro / агент
- проект Silver_77_Quests

Главная цель файла:

- хранить текущую задачу
- хранить последний отчёт агента
- хранить последний review ChatGPT
- хранить правила работы
- хранить историю задач
- хранить пользовательские подсказки по UI, смыслу и техническому опыту

--------------------------------------------------------------------------------
ОБЩИЙ ПРОЦЕСС:
--------------------------------------------------------------------------------

1. ChatGPT формирует задачу.
2. Пользователь вставляет задачу в БЛОК 1.
3. Агент читает БЛОК 1.
4. Агент выполняет только текущую задачу.
5. Агент возвращает отчёт в чат.
6. Пользователь отправляет отчёт ChatGPT.
7. ChatGPT проверяет отчёт.
8. ChatGPT даёт:
   - чистый AGENT REPORT для БЛОКА 2
   - REVIEW для БЛОКА 3
   - следующую задачу для БЛОКА 1, если она нужна
9. Пользователь вручную обновляет файл.
10. Git commit / push делает только пользователь.

--------------------------------------------------------------------------------
КТО ЗА ЧТО ОТВЕЧАЕТ:
--------------------------------------------------------------------------------

Пользователь:

- принимает финальные решения
- проверяет результат в игре
- контролирует Git
- делает commit / push
- обновляет этот файл вручную
- даёт визуальные, смысловые и технические подсказки

ChatGPT:

- ставит задачи
- формирует безопасные инструкции
- проверяет отчёты агента
- пишет REVIEW
- следит за архитектурой, рисками и порядком задач
- помогает вести этот файл
- помогает извлекать правила из успешно решённых проблем

Агент / Kiro:

- читает текущую задачу из БЛОКА 1
- выполняет только указанную задачу
- возвращает отчёт в чат
- не пишет отчёт в этот файл
- не делает commit / push
- не принимает архитектурные решения без отдельного указания

--------------------------------------------------------------------------------
КАКИЕ БЛОКИ ОБНОВЛЯТЬ:
--------------------------------------------------------------------------------

Пользователь обычно редактирует только:

1. БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
2. БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
3. БЛОК 3 — REVIEW CHATGPT
4. БЛОК 5 — ИСТОРИЯ ЗАДАЧ
5. БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ

БЛОК 4, БЛОК 4.1 и БЛОК 4.2 менять только если меняются правила работы или появляется новый подтверждённый опыт.

--------------------------------------------------------------------------------
КАК ЗАМЕНЯТЬ СОДЕРЖИМОЕ:
--------------------------------------------------------------------------------

В БЛОКЕ 1 заменять только текст между:

## НАЧАЛО ЗАДАЧИ

и

## КОНЕЦ ЗАДАЧИ

В БЛОКЕ 2 заменять только текст между:

## НАЧАЛО ОТЧЁТА

и

## КОНЕЦ ОТЧЁТА

В БЛОКЕ 3 заменять только текст между:

## НАЧАЛО REVIEW

и

## КОНЕЦ REVIEW

Сами строки НАЧАЛО и КОНЕЦ не удалять.

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ АГЕНТА:
--------------------------------------------------------------------------------

Агент НЕ записывает отчёт в этот файл.

Агент возвращает отчёт в чат.

Пользователь вручную переносит чистый отчёт в БЛОК 2 после проверки ChatGPT.

Агент не должен считать задачи из истории, review или подсказок активными.
Активная задача находится только в БЛОКЕ 1.

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ GIT:
--------------------------------------------------------------------------------

Git контролирует только пользователь.

Агенту запрещено:

- делать git commit
- делать git push
- создавать ветки
- делать git reset
- делать git clean
- делать git checkout
- откатывать изменения без отдельного разрешения

Агент может читать Git-статус только если это явно указано в задаче.

--------------------------------------------------------------------------------
ПРАВИЛО ПРОВЕРКИ АКТУАЛЬНОЙ ВЕРСИИ ДЛЯ CHATGPT:
--------------------------------------------------------------------------------

Перед анализом Documentation/AGENT_TASK_LOOP.md ChatGPT должен:

1. Сначала проверить последний коммит ветки main.
2. Сообщить пользователю:
   - короткий SHA коммита
   - название коммита
3. Читать Documentation/AGENT_TASK_LOOP.md именно из этого коммита, а не из кешированной версии main.
4. В начале ответа писать:

   Читаю коммит: <SHA> — <название коммита>

5. Если пользователь видит, что коммит неактуальный, он сообщает актуальное название или SHA последнего коммита.

--------------------------------------------------------------------------------
ТЕКУЩАЯ АКТУАЛЬНАЯ АРХИТЕКТУРА ПРОЕКТА:
--------------------------------------------------------------------------------

Проект Silver_77_Quests сейчас развивается как split Client / Server.

Актуальная клиентская часть:

Silver_77_Quests_Client/

Актуальная серверная часть:

Silver_77_Quests_Server/

Документация проекта:

Documentation/

Старые / резервные части не считать актуальными без отдельной задачи:

- корневая папка scripts/
- корневая папка gui/
- корневые config.cpp и mod.cpp
- SplitMods/
- Support/

--------------------------------------------------------------------------------
ВАЖНО ДЛЯ ЗАДАЧ:
--------------------------------------------------------------------------------

Если задача связана с UI, layout-файлами, кириллицей или текстами, агент должен учитывать:

- БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
- БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ

Если задача связана с RPC, client-server sync, PlayerBase.OnRPC, ParamsReadContext, RPCSingleParam, Param1/Param2/Param3, JSON payload или синхронизацией данных между сервером и клиентом, агент должен учитывать:

- БЛОК 4.2 — DAYZ CLIENT-SERVER RPC И СИНХРОНИЗАЦИЯ

Если задача связана с логикой квестов, JSON-квестами, Offer, Completion, Reward, requiredQuestIds, серверной обработкой квестов или клиентской логикой отображения квестов, агент должен учитывать:

- Documentation/QUEST_LOGIC_SPEC.md

Если задача связана с кодом, агент должен работать только в явно указанных файлах.

Если задача аналитическая, агент ничего не изменяет.

Если задача требует правки, агент меняет только явно указанные файлы.

--------------------------------------------------------------------------------
ПРАВИЛО ПОСЛЕ УСПЕШНОГО ФИКСА:
--------------------------------------------------------------------------------

После успешно решённой сложной проблемы нужно:

1. Закрыть активную задачу в БЛОКЕ 1.
2. Перенести отчёт агента в БЛОК 2.
3. Перенести review ChatGPT в БЛОК 3.
4. Добавить задачу в БЛОК 5 — ИСТОРИЯ ЗАДАЧ.
5. Если из проблемы получен важный опыт, добавить правило в:
   - БЛОК 4
   - БЛОК 4.1
   - БЛОК 4.2
   - БЛОК 6
   в зависимости от темы.

Пример:
TASK 064 дал правило для будущих DayZ RPC-задач:
JSON/string payload нельзя отправлять одной строкой через RPC, нужно использовать chunked sync.


================================================================================
# 🟦 БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ДЛЯ АГЕНТА
================================================================================

БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

TASK 065 FIX 6 — Исправить ядро DayZ layout viewer: parseLayout должен строить дерево nodes с parent/children

Цель:
Исправить только ядро парсинга в DayZ_layout/dayz_layout_viewer.html.
Нужно, чтобы parseLayout(text) строил правильное дерево UI-элементов DayZ .layout с parent/children и absolute position.

ВАЖНО:
Это НЕ задача на красоту UI.
Это НЕ задача на полный редактор.
Это НЕ задача на генерацию layout-кода.
Сейчас нужно исправить именно модель данных парсера.

Где работать:
ТОЛЬКО:
DayZ_layout/dayz_layout_viewer.html

Запрещено менять:
- Documentation/AGENT_TASK_LOOP.md
- Documentation/QUEST_LOGIC_SPEC.md
- Silver_77_Quests_Client/
- Silver_77_Quests_Server/
- любые .layout файлы проекта
- любые .json файлы
- любые файлы вне DayZ_layout/
- Git

Текущая проблема:
В текущей версии уже есть nodeStack, но это пока не полноценное дерево.
Сейчас узлы кладутся в stack и потом pop, но не создаются нормальные связи:
- id
- parentId
- parentName
- children
- absX
- absY
- text
- font
- props

Из-за этого viewer не может:
- показать родителя элемента
- правильно посчитать absolute position
- вывести дерево/список элементов
- показать свойства выбранного элемента
- стабильно работать с вложенными виджетами

ПОСТУЛАТ 1:
DayZ .layout — это дерево виджетов.

Пример:

PanelWidgetClass QuestPanel {
 position 0 0
 size 920 560
 {
  TextListboxWidgetClass QuestListbox {
   position 30 82
   size 330 250
  }

  ButtonWidgetClass AcceptButton {
   position 390 420
   size 220 48
   {
    TextWidgetClass AcceptButtonText {
     position 0 0
     size 1 1
     text "ВЗЯТЬ КВЕСТ"
    }
   }
  }
 }
}

Правильные связи:
- QuestPanel — parent для QuestListbox и AcceptButton
- AcceptButton — parent для AcceptButtonText
- QuestListbox parentName = QuestPanel
- AcceptButtonText parentName = AcceptButton

ПОСТУЛАТ 2:
Нельзя использовать один currentElement.

Почему:
1. Найден QuestPanel → currentElement = QuestPanel
2. Найден QuestListbox → currentElement = QuestListbox
3. QuestPanel потерян как активный parent
4. Найден AcceptButton → currentElement = AcceptButton
5. Найден AcceptButtonText → currentElement = AcceptButtonText
6. AcceptButton потерян как parent
7. Невозможно построить parent/children
8. Невозможно корректно посчитать absolute position

ПОСТУЛАТ 3:
Нужен stack текущего пути вложенности.

Правильная идея:
- stack хранит путь от root к текущему виджету
- stack[stack.length - 1] — текущий активный виджет
- новый WidgetClass становится child текущего верхнего элемента stack
- при строке "}" верхний элемент закрывается через stack.pop()
- свойства пишутся в текущий верхний элемент stack

Пример работы stack:

Строка:
PanelWidgetClass QuestPanel {

Действие:
- parent = null или QuestMenuRoot
- создать node QuestPanel
- nodes.push(QuestPanel)
- parent.children.push(QuestPanel), если parent есть
- stack.push(QuestPanel)

Строка:
TextListboxWidgetClass QuestListbox {

Действие:
- parent = stack top = QuestPanel
- создать node QuestListbox
- QuestListbox.parentName = "QuestPanel"
- QuestPanel.children.push(QuestListbox)
- nodes.push(QuestListbox)
- stack.push(QuestListbox)

Строка:
}

Действие:
- stack.pop()
- закрыли QuestListbox
- текущий parent снова QuestPanel

ПОСТУЛАТ 4:
Отдельная строка "{" НЕ создаёт node.

Пример:

PanelWidgetClass QuestPanel {
 position 0 0
 size 920 560
 {
  TextWidgetClass TitleText {
   ...
  }
 }
}

Строка "{" после свойств QuestPanel — это просто блок children.
Её нужно пропустить.

ПОСТУЛАТ 5:
Строка "}" закрывает текущий node.

Если stack не пустой:
stack.pop()

Если stack пустой:
ничего не делать, не падать.

РЕАЛЬНЫЙ ФОРМАТ СВОЙСТВ:

position:
position 30 82

size:
size 330 250

color:
color 0.0824 0.0824 0.0824 0.96

text:
text "ВЗЯТЬ КВЕСТ"

font:
font "gui/fonts/metron22"

обычные свойства:
ignorepointer 0
halign left_ref
valign top_ref
hexactpos 1
vexactpos 1
hexactsize 1
vexactsize 1

свойства в кавычках:
"exact text" 1
"text halign" center
"text valign" center

Что сделать:

1. В DayZ_layout/dayz_layout_viewer.html реализовать или заменить функцию parseLayout(text).

2. parseLayout(text) должна возвращать массив nodes.

3. Каждый node должен иметь структуру:

{
  id: number,
  type: string,
  name: string,
  parentId: number или null,
  parentName: string,
  children: [],
  position: null или { x:number, y:number },
  size: null или { w:number, h:number },
  color: null или { r:number, g:number, b:number, a:number },
  text: string,
  font: string,
  props: {},
  absX: number,
  absY: number
}

4. Алгоритм parseLayout(text):

function parseLayout(text) {
  const lines = text.split(/\r?\n/);
  const nodes = [];
  const stack = [];
  let nextId = 1;

  for each rawLine:
    const line = rawLine.trim();

    if line is empty:
      continue;

    if line starts with "//":
      continue;

    if line === "{":
      continue;

    if line === "}":
      if stack.length > 0:
        stack.pop();
      continue;

    const widgetMatch = line.match(/^([A-Za-z0-9_]+WidgetClass)\s+([A-Za-z0-9_]+)\s*\{$/);

    if widgetMatch:
      const parent = stack.length > 0 ? stack[stack.length - 1] : null;

      const node = {
        id: nextId++,
        type: widgetMatch[1],
        name: widgetMatch[2],
        parentId: parent ? parent.id : null,
        parentName: parent ? parent.name : "",
        children: [],
        position: null,
        size: null,
        color: null,
        text: "",
        font: "",
        props: {},
        absX: 0,
        absY: 0
      };

      if parent:
        parent.children.push(node);

      nodes.push(node);
      stack.push(node);
      continue;

    if stack.length === 0:
      continue;

    const current = stack[stack.length - 1];

    parse property line into current.

  computeAbsolutePositions(nodes);
  return nodes;
}

5. Парсинг свойств:

position:
const m = line.match(/^position\s+([-0-9.]+)\s+([-0-9.]+)/)
current.position = { x: parseFloat(m[1]), y: parseFloat(m[2]) }

size:
const m = line.match(/^size\s+([-0-9.]+)\s+([-0-9.]+)/)
current.size = { w: parseFloat(m[1]), h: parseFloat(m[2]) }

color:
const m = line.match(/^color\s+([-0-9.]+)\s+([-0-9.]+)\s+([-0-9.]+)\s+([-0-9.]+)/)
current.color = {
  r: parseFloat(m[1]),
  g: parseFloat(m[2]),
  b: parseFloat(m[3]),
  a: parseFloat(m[4])
}

text:
const m = line.match(/^text\s+"(.*)"$/)
current.text = m[1]

font:
const m = line.match(/^font\s+"(.*)"$/)
current.font = m[1]

quoted prop:
const m = line.match(/^"([^"]+)"\s+(.*)$/)
current.props[m[1]] = m[2]

normal prop:
const m = line.match(/^([A-Za-z0-9_]+)\s+(.*)$/)
current.props[m[1]] = m[2]

6. Реализовать computeAbsolutePositions(nodes):

function computeAbsolutePositions(nodes) {
  const byId = new Map(nodes.map(n => [n.id, n]));

  for each node in nodes:
    const localX = node.position ? node.position.x : 0;
    const localY = node.position ? node.position.y : 0;

    if node.parentId:
      const parent = byId.get(node.parentId);
      node.absX = (parent ? parent.absX : 0) + localX;
      node.absY = (parent ? parent.absY : 0) + localY;
    else:
      node.absX = localX;
      node.absY = localY;
}

7. Почему absolute position нужен:

Если:
QuestPanel position 0 0
QuestListbox position 30 82

То:
QuestListbox.absX = QuestPanel.absX + 30
QuestListbox.absY = QuestPanel.absY + 82

Если потом появится вложенный текст:
AcceptButton position 390 420
AcceptButtonText position 0 0

То:
AcceptButtonText.absX = AcceptButton.absX + 0
AcceptButtonText.absY = AcceptButton.absY + 0

8. Обновить renderLayout(nodes), чтобы он использовал:
- node.absX
- node.absY
- node.size.w
- node.size.h

9. В renderLayout(nodes):
- рисовать только nodes с size
- если node.color есть — использовать rgba из DayZ color
- если node.color нет — background transparent + visible border
- НЕ использовать random color
- на блоке показать node.name
- если node.type === "TextWidgetClass" и node.text не пустой, показать node.name + ": " + node.text

10. Добавить минимальную отладку в UI:
После парсинга вывести количество найденных nodes:
Найдено элементов: X

11. Минимально проверить в коде, что parseLayout на QuestMenu.layout должен находить:
- QuestMenuRoot
- QuestPanel
- QuestListbox
- DescriptionPanel
- RoutePanel
- DialogPanel
- AcceptButton
- CompleteButton
- CloseButton

12. В этой задаче НЕ обязательно идеально доделывать:
- красивую панель свойств
- сложный UI
- идеальный список элементов

Но если они уже есть — не ломать.

13. В этой задаче обязательно:
- parseLayout строит nodes с parent/children
- computeAbsolutePositions считает absX/absY
- renderLayout использует absX/absY
- нет currentElement как основной логики
- нет random color
- нет DOMParser
- нет position[] / size[] / color[]

14. Нельзя:
- менять Documentation/AGENT_TASK_LOOP.md
- менять файлы мода
- менять .layout файлы проекта
- менять JSON
- создавать тестовые файлы
- оставлять тестовые файлы
- использовать localStorage
- использовать внешние библиотеки
- использовать CDN
- отправлять данные в сеть
- генерировать layout-код
- сохранять файлы

Критерии готовности:

1. Изменён только файл:
DayZ_layout/dayz_layout_viewer.html

2. В коде есть функция parseLayout(text).

3. В коде есть функция computeAbsolutePositions(nodes).

4. parseLayout использует stack nodes.

5. Node содержит:
id, type, name, parentId, parentName, children, position, size, color, text, font, props, absX, absY.

6. Свойства пишутся в stack[stack.length - 1].

7. renderLayout использует node.absX / node.absY.

8. Нет DOMParser.

9. Нет position[] / size[] / color[].

10. Нет random color.

11. AGENT_TASK_LOOP.md не изменён.

12. Файлы мода не изменены.

Формат отчёта:

AGENT REPORT

DONE:
- что сделано

CHANGED FILES:
- какие файлы изменены

DIFF:
- кратко

PROBLEMS:
- реальные проблемы, если есть

CONCLUSION:
- краткий вывод


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ЗАДАЧИ
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

DONE:
- Player data RPC переведён на chunked sync по аналогии с config sync.
- Сервер больше не отправляет весь player data JSON одной строкой через RPC.
- Добавлена серверная отправка player data чанками через Param3<int,int,string>.
- param1 = chunkIndex.
- param2 = totalChunks.
- param3 = chunkPayload.
- Использован безопасный размер чанка 512 байт / размер config sync.
- На клиенте добавлен отдельный buffer для player data chunks.
- Клиент собирает player data payload отдельно от config payload.
- ctx.Read выполняется прямо внутри OnRPC case SILVER77_QUEST_RPC_PLAYER_DATA.
- ParamsReadContext не передаётся в helper-функции.
- После сборки полного payload вызывается Silver77_HandleQuestPlayerDataPayload(payload).
- Config sync не затронут.
- Серверная бизнес-логика progress не менялась.
- JSON и layout не менялись.

CHANGED FILES:
- Silver_77_Quests_Server/scripts/4_World/QuestServerManager.c
- Silver_77_Quests_Client/scripts/4_World/QuestClientRPC.c

DIFF:
- Server: прямая отправка Param3<int,int,string>(0,1,payload) заменена на SendPlayerDataPayloadChunks(player, payload).
- Server: добавлена отправка player data чанками.
- Client: добавлены g_Silver77_PlayerDataSyncChunks и g_Silver77_PlayerDataSyncExpectedChunkCount.
- Client: добавлены Silver77_ResetPlayerDataSyncBuffer() и Silver77_StorePlayerDataSyncChunk().
- Client: OnRPC case SILVER77_QUEST_RPC_PLAYER_DATA теперь читает chunk, собирает payload и только после сборки вызывает обработчик payload.

PROBLEMS:
- Нет.

CONCLUSION:
- TASK 064 выполнен.
- Ошибка “String CORRUPTED - FIX OnStoreLoad()” устранена.
- Player data sync работает через chunked RPC.
- В игре подтверждено: UI получает реальные статусы квестов и показывает “УЖЕ АКТИВЕН”.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟨 БЛОК 3 — REVIEW CHATGPT
================================================================================

Заменять только содержимое между:

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 064 REVIEW

Статус:
Принято.

Что было:
Клиент падал при чтении player data RPC:

Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!
Silver_77_Quests/scripts/4_World/questclientrpc.c:70 Function OnRPC

Сервер при этом корректно:
- получал quest progress request RPC;
- загружал player progress из JSON;
- видел реальные статусы active/completed;
- сериализовал PlayerQuestData;
- отправлял payload клиенту.

Проблема:
Player data JSON отправлялся одной строкой через RPC.
Для DayZ/Enforce такой способ оказался ненадёжным: строка повреждалась при передаче/чтении RPC.

Решение:
Player data sync переведён на chunked sync по аналогии с config sync.

Итоговая архитектура:
- сервер режет player data payload на chunks;
- каждый chunk отправляется через Param3<int,int,string>;
- клиент читает ctx прямо в OnRPC case;
- клиент собирает chunks в отдельный player data buffer;
- после сборки полного payload вызывает Silver77_HandleQuestPlayerDataPayload(payload);
- ParamsReadContext не передаётся в helper-функции.

Проверка в игре:
- ошибка String CORRUPTED больше не проявляется;
- UI начал получать реальные статусы квестов;
- вместо повторного “ВЗЯТЬ КВЕСТ” отображается корректное состояние “УЖЕ АКТИВЕН”.

Вывод:
TASK 064 успешно решил проблему client-server sync.
Полученный опыт нужно закрепить в правилах агента: любые JSON/string payload для DayZ RPC передавать чанками, а не одной строкой.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

--------------------------------------------------------------------------------

================================================================================
# 🟥 БЛОК 4 — ПРАВИЛА ДЛЯ АГЕНТА
================================================================================

================================================================================
# 🟥 БЛОК 4 — ПРАВИЛА ДЛЯ АГЕНТА
================================================================================

Агент должен:

1. Читать задачу только из БЛОКА 1.
2. Выполнять только текущую задачу.
3. Не выполнять задачи из истории, review или пользовательских подсказок, если они не перенесены в БЛОК 1.
4. Не менять архитектуру без отдельного указания.
5. Не делать рефакторинг без отдельной задачи.
6. Не удалять файлы без отдельной задачи.
7. Не переносить файлы без отдельной задачи.
8. Не переименовывать файлы без отдельной задачи.
9. Если задача аналитическая — ничего не изменять.
10. Если задача требует правки — менять только явно указанные файлы.
11. Если в задаче указаны конкретные файлы — не выходить за их пределы.
12. Если для решения явно нужен другой файл — сначала указать это в PROBLEMS или QUESTIONS, а не править молча.
13. Отчёт возвращать только в чат.
14. Отчёт в файл не записывать.
15. Соблюдать БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА.
16. Соблюдать БЛОК 4.2 — DAYZ CLIENT-SERVER RPC И СИНХРОНИЗАЦИЯ.
17. Учитывать БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ, если задача связана с UI, layout-файлами или текстами.
18. Учитывать Documentation/QUEST_LOGIC_SPEC.md, если задача связана с логикой квестов, JSON-квестами, редактором квестов, Offer, Completion, Reward, requiredQuestIds, серверной обработкой квестов или клиентской логикой отображения квестов.
19. Git контролирует только пользователь.
20. Агент НЕ делает git commit.
21. Агент НЕ делает git push.
22. Агент НЕ создаёт ветки без отдельной задачи.
23. Агент НЕ выполняет git reset, git clean, git checkout без отдельного разрешения.
24. Агент может читать Git-статус только если это явно указано в задаче.
25. Не чинить сразу несколько уровней системы одновременно, если задача диагностическая.
26. Для client-server проблем идти по цепочке: request → server data → server send → client receive → client read → deserialize → apply → UI.
27. Если задача связана с RPC, сначала подтвердить транспорт и формат данных, и только потом менять UI.
28. Не считать UI первопричиной, пока не подтверждено, что данные успешно применены на клиенте.
29. Отчёт должен быть кратким: только факты, без длинных объяснений, если подробный анализ не запрошен отдельно.

--------------------------------------------------------------------------------
ФОРМАТ ОТЧЁТА В ЧАТ:
--------------------------------------------------------------------------------

Отчёт должен быть коротким и практичным.

AGENT REPORT

DONE:
- что сделано

CHANGED FILES:
- какие файлы изменены

DIFF:
- кратко что изменилось

PROBLEMS:
- только реальные проблемы

QUESTIONS:
- только реальные вопросы

CONCLUSION:
- краткий вывод

--------------------------------------------------------------------------------
ВАЖНО ДЛЯ ОТЧЁТА:
--------------------------------------------------------------------------------

Если всё прошло нормально, не расписывать длинные объяснения.

Если задача аналитическая — дать вывод кратко, но достаточно конкретно.

Если задача связана с риском, кодировкой, удалением файлов, Git или структурой проекта — обязательно указать риск в PROBLEMS.

Если задача связана с логикой квестов, JSON-квестами, редактором квестов, Offer, Completion, Reward или requiredQuestIds — сверять выводы с Documentation/QUEST_LOGIC_SPEC.md.

Если задача связана с DayZ RPC, client-server sync, ParamsReadContext, Param1/Param2/Param3, player data, config sync или JSON payload — сверять выводы с БЛОКОМ 4.2.

Если нужен подробный анализ, пользователь или ChatGPT укажет это отдельно.

================================================================================
# 🟪 БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
================================================================================

================================================================================
# 🟪 БЛОК 4.1 — КОДИРОВКА И КИРИЛЛИЦА
================================================================================

ВАЖНО:
В проекте используется русскоязычный текст. Кириллицу нельзя повреждать.

Некоторые файлы могут быть сохранены не в UTF-8, а в CP1251 или другой Windows-кодировке.

Особенно осторожно работать с файлами:

- .layout
- .json
- .md
- .c
- .cpp
- .txt
- .xml

--------------------------------------------------------------------------------
ПРАВИЛА:
--------------------------------------------------------------------------------

1. НЕ перекодировать файлы без отдельной задачи.
2. НЕ сохранять файл заново, если задача только аналитическая.
3. НЕ заменять кириллицу на ????, ����� или транслит.
4. НЕ “чинить” русские строки автоматически.
5. Перед изменением файла с кириллицей нужно определить его текущую кодировку.
6. Если кириллица отображается как �����, это НЕ значит, что текст надо переписать.
7. Если кодировка непонятна — остановиться и написать в PROBLEMS.
8. Любые правки текста с кириллицей делать только после отдельного подтверждения.
9. Не менять кавычки, переносы строк и спецсимволы в JSON без задачи.
10. Не форматировать JSON целиком, если задача требует изменить только одну строку или один блок.
11. При работе с layout-файлами не менять стили, размеры, anchors и visibility без отдельной задачи.
12. При работе с Markdown-документацией не переписывать весь файл, если задача требует заменить только один блок.

--------------------------------------------------------------------------------
ОСОБО ОПАСНЫЕ ФАЙЛЫ:
--------------------------------------------------------------------------------

- Silver_77_Quests_Client/gui/QuestMenu.layout
- Silver_77_Quests_Client/gui/QuestJournal.layout
- gui/QuestMenu.layout
- gui/QuestJournal.layout
- JSON_Quvest/*.json
- Documentation/*.md

--------------------------------------------------------------------------------
ПРАВИЛО ДЛЯ ОТЧЁТА:
--------------------------------------------------------------------------------

Если задача затрагивает файл с кириллицей, агент обязан указать в отчёте:

- была ли обнаружена кириллица
- отображается ли она нормально
- есть ли риск кодировки
- менялся ли файл
- сохранялся ли файл заново


================================================================================
# 🟫 БЛОК 4.2 — DAYZ CLIENT-SERVER RPC И СИНХРОНИЗАЦИЯ
================================================================================

Этот блок обязателен для задач, связанных с:

- DayZ RPC
- client/server sync
- PlayerBase.OnRPC
- ParamsReadContext
- RPCSingleParam
- Param1 / Param2 / Param3 / Param4
- JSON payload
- config sync
- player data sync
- quest progress sync
- UI, который зависит от server data

--------------------------------------------------------------------------------
ГЛАВНОЕ ПРАВИЛО:
--------------------------------------------------------------------------------

Не отправлять JSON, progress, config, dialog data или другой длинный string payload одной строкой через RPC.

Для DayZ/Enforce это ненадёжно.

Возможная ошибка клиента:

Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!

Если такая ошибка появилась при ctx.Read string payload — не искать первопричину в UI.
Сначала проверить транспорт RPC и перевести передачу payload на chunked sync.

--------------------------------------------------------------------------------
ПРАВИЛО CHUNKED SYNC:
--------------------------------------------------------------------------------

Любые крупные или потенциально сложные данные передавать чанками.

Рекомендуемый формат:

Param3<int, int, string>

param1 = chunkIndex
param2 = totalChunks
param3 = chunkPayload

Рекомендуемый размер чанка:
- 512 байт
- либо уже существующая константа проекта для chunk sync

--------------------------------------------------------------------------------
ОТДЕЛЬНЫЕ BUFFER ДЛЯ РАЗНЫХ ТИПОВ ДАННЫХ:
--------------------------------------------------------------------------------

Для каждого типа payload должен быть отдельный buffer.

Правильно:
- config chunks отдельно
- player data chunks отдельно
- dialog data chunks отдельно
- future inventory/snapshot chunks отдельно

Неправильно:
- складывать config и player data в один общий map buffer
- переиспользовать один expectedChunkCount для разных RPC payload
- собирать разные payload-типы в одной функции без явного разделения

--------------------------------------------------------------------------------
ПРАВИЛО ParamsReadContext:
--------------------------------------------------------------------------------

ctx.Read выполнять прямо внутри соответствующего case в OnRPC.

Не передавать ParamsReadContext в helper-функции.

Правильно:

case SILVER77_QUEST_RPC_PLAYER_DATA:
{
    Param3<int, int, string> data = new Param3<int, int, string>(0, 0, "");
    if (!ctx.Read(data))
    {
        Print("[Silver_77_Quests][CLIENT_PROGRESS_DEBUG] ERROR: ctx.Read failed");
        return;
    }

    string payload = Silver77_StorePlayerDataSyncChunk(data.param1, data.param2, data.param3);
    if (payload == "")
        return;

    Silver77_HandleQuestPlayerDataPayload(payload);
    return;
}

Неправильно:

case SILVER77_QUEST_RPC_PLAYER_DATA:
    Silver77_HandleQuestPlayerData(ctx);
    return;

private void Silver77_HandleQuestPlayerData(ParamsReadContext ctx)
{
    ctx.Read(...);
}

Helper-функции должны получать обычные значения:
- string payload
- int chunkIndex
- int totalChunks
- PlayerQuestData
- Silver77_QuestConfig

--------------------------------------------------------------------------------
ДИАГНОСТИКА RPC ПО ЦЕПОЧКЕ:
--------------------------------------------------------------------------------

При проблемах client-server sync проверять строго по этапам.

1. Клиент отправил request?
Искать client log:
- Sending ... request RPC

2. Сервер получил request?
Искать server RPT:
- Received ... request RPC

3. Сервер загрузил реальные данные?
Искать server RPT:
- LoadPlayerData
- FileExist=true
- progressCount
- questId/status

4. Сервер подготовил payload?
Искать:
- payloadLength
- Serialize...

5. Сервер отправил response?
Искать:
- Sent ... to client
- Sent ... in X chunks

6. Клиент получил chunks?
Искать client RPT:
- Received ... chunk 1/X
- Received ... chunk X/X

7. Клиент собрал payload?
Искать:
- Reassembled ... payload
- SUCCESS: Reassembled ...

8. Клиент десериализовал payload?
Искать:
- JsonSerializer.ReadFromString succeeded
- PlayerQuestData deserialized successfully

9. Клиент применил данные?
Искать:
- ApplySyncedPlayerData
- ApplySyncedPlayerData completed

10. UI обновился после apply?
Искать:
- реальные status active/completed
- кнопки “УЖЕ АКТИВЕН”, “СДАТЬ КВЕСТ”, “НАГРАДА”
- отсутствие ложного available

--------------------------------------------------------------------------------
ЕСЛИ КЛИЕНТ ПАДАЕТ В ctx.Read:
--------------------------------------------------------------------------------

Сначала проверить:

1. Совпадает ли RPC ID на сервере и клиенте.
2. Совпадает ли Param-тип на сервере и клиенте.
3. Не читает ли super.OnRPC или другой обработчик ctx раньше нашего case.
4. Не передаётся ли ParamsReadContext в helper.
5. Не отправляется ли длинная string одной строкой.
6. Нет ли в логе:
   Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!

Если есть String CORRUPTED:
- не начинать с UI
- не начинать с JSON-логики квестов
- не начинать с HasSyncedPlayerData
- не начинать с кнопок
- сначала перевести payload на chunked sync

--------------------------------------------------------------------------------
SERVER IS SOURCE OF TRUTH:
--------------------------------------------------------------------------------

Сервер является источником правды.

Сервер:
- хранит progress
- загружает progress из profile JSON
- меняет quest status
- выдаёт rewards
- сохраняет player data
- отправляет sync клиенту

Клиент:
- запрашивает config/player data
- получает sync
- отображает UI
- не должен считать default available реальным progress
- не должен решать server-side outcome самостоятельно

--------------------------------------------------------------------------------
UI НЕ ДОЛЖЕН МАСКИРОВАТЬ ПРОБЛЕМЫ SYNC:
--------------------------------------------------------------------------------

Если HasSyncedPlayerData == false:
- UI может показывать loading
- UI не должен отображать окончательные статусы
- UI не должен активировать кнопки на основе default available
- UI не должен создавать впечатление, что квест реально available, если server sync ещё не получен

Функции типа EnsurePlayerProgress/GetQuestStatus не должны подменять отсутствие server sync настоящим progress.

Default available допустим только как временная заглушка, но не как источник истины.

--------------------------------------------------------------------------------
МИНИМАЛЬНЫЕ ЗАДАЧИ:
--------------------------------------------------------------------------------

При RPC/client-server проблемах не менять сразу:

- UI
- JSON
- server progress
- client manager
- RPC format
- layout

одновременно.

Делать маленькие задачи:

1. Подтвердить request.
2. Подтвердить server data.
3. Подтвердить server send.
4. Подтвердить client receive.
5. Подтвердить ctx.Read.
6. Подтвердить deserialize.
7. Подтвердить ApplySyncedData.
8. Подтвердить UI update.

После каждого шага проверять server/client logs.

--------------------------------------------------------------------------------
ОПЫТ TASK 064:
--------------------------------------------------------------------------------

Проблема:
Player data JSON отправлялся одной строкой через RPC.
Клиент падал:

Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!
Silver_77_Quests/scripts/4_World/questclientrpc.c:70 Function OnRPC

Что помогло:
Player data sync переведён на chunked sync, как config sync.

Результат:
Клиент начал получать реальные статусы.
UI начал показывать корректное состояние, например “УЖЕ АКТИВЕН”.

Правило на будущее:
Если через RPC передаётся JSON/string payload — сразу проектировать chunked sync.

================================================================================
# ⬛ БЛОК 5 — ИСТОРИЯ ЗАДАЧ
================================================================================

Новые завершённые задачи добавлять вниз.

--------------------------------------------------------------------------------

- TASK 001 — Проверка зависимости клиент/сервер — принято
- TASK 002 — Проверка соответствия имени клиентского мода в CfgPatches — принято
- TASK 003 — Инвентаризация дубликатов монолитной и split-версии — принято с уточнением
- TASK 004 — Проверка содержимого резервных и старых папок без изменений — принято
- TASK 005 — Проверка корневых config.cpp и mod.cpp — принято
- TASK 006 — Проверка корневой папки gui — принято с замечанием по кодировке
- TASK 007 — Проверка кодировки файлов с кириллицей — принято
- TASK 008 — Поиск нормальной версии QuestMenu.layout — принято
- TASK 009 — Сравнение QuestMenu.layout перед восстановлением — принято
- TASK 010 — Составление карты повреждённых строк QuestMenu.layout — принято
- TASK 011 — Попытка восстановления text-строк — остановлено безопасно из-за риска кодировки
- TASK 012 — Выбор безопасного способа восстановления QuestMenu.layout — принято
- TASK 013 — Подготовка безопасного скрипта восстановления QuestMenu.layout — принято предварительно
- TASK 014 — Code review скрипта восстановления QuestMenu.layout — принято
- TASK 015 — Финальная проверка кода restore_questmenu_text.py перед запуском — принято
- TASK 016 — Ручной запуск restore_questmenu_text.py и проверка результата — принято
- TASK 017 — Проверка восстановленного QuestMenu.layout без изменений — принято
- TASK 018 — Уборка временных Python/cache/backup файлов после восстановления — принято
- TASK 019 — Проверка результата восстановления QuestMenu.layout в проекте без изменений — принято
- TASK 020 — Диагностика CloseButton и CloseButtonText без изменений — принято
- TASK 021 — Подключение CloseButtonText в QuestUI.c — принято
- TASK 022 — Сравнение стилей CloseButton с рабочими кнопками — принято
- TASK 023 — Включение CloseButton и CloseButtonText в UpdateButtons — принято
- TASK 024 — Ручная проверка CloseButton в игре — принято
- TASK 059 — Диагностика OnRPC case без вызова Silver77_HandleQuestPlayerData(ctx) — принято как диагностический шаг
- TASK 060 — Перенос ctx.Read player data прямо в OnRPC case — принято как архитектурная правка
- TASK 061 — Диагностическая проверка Param1<string> вместо Param3 — временный тест, позже откатан
- TASK 063 — Возврат player data RPC на Param3 после диагностики — принято
- TASK 064 — Player data RPC переведён на chunked sync как config sync — принято. Исправлена ошибка “String CORRUPTED - FIX OnStoreLoad()”. В игре подтверждено: UI получает реальные статусы квестов и показывает “УЖЕ АКТИВЕН”.



================================================================================
# ⬛ БЛОК 6 — ПОДСКАЗКИ ОТ ПОЛЬЗОВАТЕЛЯ
================================================================================


Этот блок содержит ручные подсказки от пользователя по UI, текстам и визуальному поведению.

Агент должен учитывать этот блок, если задача связана с:
- UI
- layout-файлами
- текстами
- визуальными элементами
- восстановлением кириллицы

--------------------------------------------------------------------------------
ПОДСКАЗКИ:
--------------------------------------------------------------------------------

- В `QuestMenu.layout` нижняя серая кнопка / полоса в диалоговом окне должна иметь текст:
  `Закрыть рот и уйти`

- По проверке в игре нижняя серая кнопка диалога раньше была видна только при наведении мышки.
  Без наведения кнопка почти или полностью не была видна.
  Текст `Закрыть рот и уйти` на ней не отображался.
  Нужно было сравнить стили и структуру `CloseButton` / `CloseButtonText` с рабочими кнопками `AcceptButton` / `AcceptButtonText` и `CompleteButton` / `CompleteButtonText`.

- Если агент не уверен в назначении UI-элемента, он должен указать это в `QUESTIONS`, а не исправлять текст самостоятельно.

- По проверке в игре после TASK 023 нижняя кнопка диалога работает:
  текст `Закрыть рот и уйти` отображается,
  кнопка видна,
  кнопка закрывает диалог.

- Причина проблемы была в `QuestUI.c`:
  `CloseButton` и `CloseButtonText` не были полноценно обработаны в `UpdateButtons()`.

- Для похожих UI-проблем сначала сравнивать проблемный элемент с рабочими аналогами:
  `AcceptButton` / `AcceptButtonText`,
  `CompleteButton` / `CompleteButtonText`,
  `CloseButton` / `CloseButtonText`,
  а затем проверять обработку этих элементов в коде.
  
  --------------------------------------------------------------------------------
ОПЫТ ПО CLIENT-SERVER SYNC:
--------------------------------------------------------------------------------

- При проблемах с квестовыми статусами сначала проверять не UI, а цепочку server/client sync:
  request → server load → server send → client receive → ctx.Read → deserialize → ApplySyncedPlayerData → UI update.

- Если серверный progress правильный, но UI показывает available, это не всегда проблема UI.
  Сначала проверить, дошёл ли server sync до клиента и был ли вызван ApplySyncedPlayerData.

- Ошибка:
  Reason: !!! String CORRUPTED - FIX OnStoreLoad() !!!
  при чтении RPC означает риск повреждения string payload.
  В таком случае не передавать JSON одной строкой, а использовать chunked sync.

- Для похожих модов и будущих систем:
  config, player progress, dialog data, NPC data, inventory snapshots и другие JSON payload передавать чанками через Param3<int,int,string>.

- ParamsReadContext читать прямо в OnRPC case.
  В helper-функции передавать уже обычные значения, например string payload.

- TASK 064 подтвердил рабочий подход:
  player data sync чанками решил проблему, и UI начал показывать реальные статусы квестов.

================================================================================
# КОНЕЦ ФАЙЛА
================================================================================